(() => {
  "use strict";

  const STORAGE = {
    activeCase: "quiet-department.case.v3",
    notes: "quiet-department.notes.v1",
    quotes: "quiet-department.quotes.v1",
    checklist: "quiet-department.checklist.v1",
    settings: "quiet-department.settings.v1",
  };

  const VIEW_ORDER = [
    "overview",
    "board",
    "archive",
    "computer",
    "interviews",
    "lab",
    "analysis",
    "report",
  ];

  const BOARD_LINK_TYPES = [
    "контакт",
    "время",
    "возможность",
    "мотив",
    "подтверждает",
    "противоречит",
    "исключает",
  ];

  const DEFAULT_SETTINGS = {
    theme: "classic",
    clickSounds: true,
    effectsVolume: 70,
    holdDuration: 1000,
  };

  const THEME_AUDIO_PROFILES = {
    classic: { click: 520, accent: 780, wave: "triangle", noise: 0.012 },
    "old-jazz": { click: 390, accent: 590, wave: "sine", noise: 0.026 },
    cyberpunk: { click: 730, accent: 1180, wave: "square", noise: 0.008 },
    "high-contrast-white": { click: 660, accent: 990, wave: "sine", noise: 0.006 },
    "high-contrast-black": { click: 310, accent: 930, wave: "triangle", noise: 0.01 },
  };

  const DIFFICULTIES = {
    tutorial: {
      label: "Туториал",
      suspects: 2,
      puzzles: 3,
      noise: 0,
      deadEnds: 0,
      taskSeconds: 10,
      hint: "Стикеры ведут по базовым действиям и их можно скрыть.",
    },
    observer: {
      label: "Наблюдатель",
      suspects: 4,
      puzzles: 3,
      noise: 12,
      deadEnds: 0,
      taskSeconds: 18,
      hint: "Подсказки указывают направление.",
    },
    detective: {
      label: "Детектив",
      suspects: 5,
      puzzles: 5,
      noise: 24,
      deadEnds: 2,
      taskSeconds: 28,
      hint: "В деле есть убедительные, но конечные цепочки конфликтов.",
    },
    inspector: {
      label: "Инспектор",
      suspects: 6,
      puzzles: 6,
      noise: 38,
      deadEnds: 3,
      taskSeconds: 42,
      hint: "Ложные цепочки пересекаются с реальными и используют те же факты.",
    },
  };

  const TUTORIAL_STEPS = [
    {
      view: "overview",
      target: '[data-view="archive"]',
      title: "Дело — это не готовый ответ",
      body: ["В обучении только два подозреваемых и нет фонового шума. Игра не помечает виновного: вы строите проверяемую цепочку из людей, времени и источников.", "Начните с архива. Он хранит материалы, но не делает за вас вывод."],
      action: "Открыть архив",
    },
    {
      view: "archive",
      target: ".archive-card",
      title: "Архив: источник, а не приговор",
      body: ["Откройте любой документ. Сверяйте, кто написал материал, когда он создан и что именно он подтверждает. Даже правдивая запись может не объяснять событие."],
      trigger: "evidence-open",
    },
    {
      view: "archive",
      target: '[data-action="add-to-board"]',
      title: "На доску попадает только то, что вы хотите сопоставить",
      body: ["Добавьте открытый материал на доску. Там можно удержать карточку для полного чтения, выбрать две карточки, задать тип связи и записать, почему она кажется вам значимой."],
      trigger: "board-add",
    },
    {
      view: "board",
      target: "#case-board",
      title: "Связь — это ваша рабочая гипотеза",
      body: ["Кликните две карточки, выберите характер связи и нажмите «Связать». Контакт, мотив или совпадение во времени сами по себе не доказывают вину — отмечайте границы вывода."],
      trigger: "board-link",
    },
    {
      view: "board",
      target: '[data-view="interviews"]',
      title: "Теперь спросим людей",
      body: ["Допрос — не проверка правильных реплик. Люди защищают личные интересы, ошибаются в памяти и могут намеренно лгать о второстепенном. Наблюдения курсивом — повод проверить слова, а не доказательство."],
      action: "Перейти к допросу",
    },
    {
      view: "interviews",
      target: ".question-button:not([disabled])",
      title: "Допрос: ищите расхождение с независимым источником",
      body: ["Задайте вопрос. Запишите сказанное, затем сравните его с документами, временем и цифровыми следами. Ложь о личном конфликте может быть настоящей ложью, но не обязательно объясняет главное событие."],
      trigger: "interview-question",
    },
    {
      view: "interviews",
      target: '[data-view="computer"]',
      title: "Компьютер хранит технические следы",
      body: ["Откройте рабочий компьютер. Почта, файлы, поиск и журнал сети дают разные виды данных. Устройство, псевдоним или общий терминал не равны личности без независимой привязки."],
      action: "Открыть компьютер",
    },
    {
      view: "computer",
      target: '[data-computer-app="tools"]',
      title: "Инструменты делают операцию, а не вывод",
      body: ["Откройте «Инструменты». Они извлекают технические признаки, но не отвечают, чей это след и что он значит для версии."],
      trigger: "computer-tools",
    },
    {
      view: "computer",
      target: '[data-computer-tool="strings"]',
      title: "Ищите скрытые данные",
      body: ["Запустите «Строки файла». Результат покажет нулевые символы — это подсказка перенести материал в лабораторию. Декодинг раскрывает данные, но не заменяет проверку их источника."],
      trigger: "computer-strings",
    },
    {
      view: "computer",
      target: '[data-view="lab"]',
      title: "Декодинг в лаборатории",
      body: ["Лаборатория помогает проверить время, скрытые символы и логические условия. Она не выбирает подозреваемого: её вывод нужно соединить с людьми и другими материалами."],
      action: "Открыть лабораторию",
    },
    {
      view: "lab",
      target: '[data-puzzle-kind="digital"]',
      title: "Скрытые символы требуют ручной проверки",
      body: ["Откройте цифровую задачу. Посмотрите на исходный фрагмент, извлеките нулевые символы и сопоставьте полученный ответ с контекстом дела."],
      trigger: "digital-open",
    },
    {
      view: "lab",
      target: '[data-action="extract-zero"]',
      title: "Декодировать — не значит доказать",
      body: ["Извлеките нулевые символы. После декодинга обязательно спросите: кто мог создать след, на каком устройстве и чем это подтверждено независимо?"],
      trigger: "digital-decode",
    },
    {
      view: "lab",
      target: '[data-view="report"]',
      title: "Соберите версию",
      body: ["В версии вы формулируете не догадку, а цепочку: ответственное лицо, мотив, механизм и 3–5 материалов. В объяснении отделите причину, маскировку, случайность и границы того, что действительно доказано."],
      action: "Открыть версию",
    },
    {
      view: "report",
      target: "#report-form",
      title: "Финальная проверка принадлежит вам",
      body: ["Заполните версию, когда будете готовы. Даже в туториале можно ошибиться: система оценит связность, а после этого вы сможете вернуться к материалам и усилить слабые звенья."],
      trigger: "report-submit",
    },
    {
      view: "report",
      target: ".result-card",
      title: "Базовое обучение завершено",
      body: ["Теперь вы умеете собирать материалы, связывать их, читать показания с осторожностью, работать с компьютером и декодингом, а затем формулировать собственную версию. В обычных сложностях цепочки будут длиннее и шумнее."],
      action: "Завершить обучение",
      final: true,
    },
  ];

  const PERSON_NAME_BANK = {
    male: {
      first: ["Алексей", "Виктор", "Лев", "Михаил", "Роман", "Олег", "Денис", "Артур", "Павел", "Илья", "Глеб", "Борис", "Сергей", "Никита", "Фёдор", "Ян", "Матвей", "Кирилл", "Евгений", "Степан"],
      last: ["Волков", "Северин", "Крылов", "Ракитин", "Громов", "Соколов", "Рудин", "Туманов", "Чернов", "Яров", "Астахов", "Орлов", "Белов", "Костин", "Марков", "Долин", "Лавров", "Ершов", "Галин", "Сазонов"],
    },
    female: {
      first: ["Анна", "Марина", "Нина", "Елена", "Вера", "Инга", "Лидия", "Софья", "Тамара", "Кира", "Алиса", "Лариса", "Дарья", "Оксана", "Полина", "Юлия", "Таисия", "Рита", "Валерия", "Зоя"],
      last: ["Миронова", "Орлова", "Белова", "Ланская", "Веденеева", "Арсеньева", "Левина", "Зорина", "Мельникова", "Лукина", "Астахова", "Рудина", "Костина", "Маркова", "Лаврова", "Ершова", "Галина", "Сазонова", "Долина", "Волкова"],
    },
  };

  const ROLE_FORMS = {
    "владелец клуба": "владелица клуба", "саксофонист": "саксофонистка", "музыкальный продюсер": "музыкальный продюсер",
    "пианист": "пианистка", "бар-менеджер": "бар-менеджер", "звукорежиссёр": "звукорежиссёр", "частный коллекционер": "частная коллекционерка", "ночной курьер": "ночная курьерка",
    "старший архивист": "старшая архивистка", "реставратор рукописей": "реставратор рукописей", "исследователь фондов": "исследовательница фондов",
    "заместитель директора": "заместительница директора", "ночной охранник": "ночная охранница", "реставратор": "реставратор", "историк": "историк", "системный администратор": "системная администраторка", "курьер ведомства": "курьер ведомства",
    "финансовый консультант": "финансовая консультантка", "гостиничный аудитор": "гостиничная аудиторка", "иностранный посредник": "иностранная посредница",
    "администратор": "администратор", "консьерж": "консьерж", "переводчик": "переводчица", "постоялец": "постоялица", "водитель": "водитель", "организатор конференции": "организатор конференции",
    "радиоинженер": "радиоинженер", "ведущий ночного эфира": "ведущая ночного эфира", "коллекционер аппаратуры": "коллекционерка аппаратуры",
    "радиолюбитель": "радиолюбительница", "техник связи": "техник связи", "журналист": "журналистка", "охранник объекта": "охранница объекта", "продавец аппаратуры": "продавец аппаратуры", "бывший военный связист": "бывшая военная связистка",
    "владелица соседнего бара": "владелица соседнего бара",
    "руководитель лаборатории": "руководительница лаборатории", "лаборант химического анализа": "лаборантка химического анализа", "инженер по калибровке": "инженерка по калибровке",
    "специалист по биобезопасности": "специалистка по биобезопасности", "оператор криохранилища": "операторка криохранилища", "техник вакуумных систем": "техник вакуумных систем",
    "координатор поставок": "координаторка поставок", "стажёр-исследователь": "стажёрка-исследовательница", "инспектор качества": "инспекторка качества",
    "куратор музейной коллекции": "кураторка музейной коллекции", "фондовый хранитель": "фондовая хранительница", "инженер экспозиции": "инженерка экспозиции",
    "специалист по страховой оценке": "специалистка по страховой оценке", "реставратор техники": "реставраторка техники", "диспетчер терминала": "диспетчерка терминала",
    "лоцман": "лоцманка", "механик причала": "механик причала", "оператор грузового контроля": "операторка грузового контроля", "экспедитор": "экспедиторка",
    "судовой электрик": "судовая электрика", "инспектор портовой безопасности": "инспекторка портовой безопасности",
    "эксперт по трасологии": "экспертка по трасологии", "аналитик цифровых образов": "аналитик цифровых образов", "специалист по цепочке хранения": "специалистка по цепочке хранения",
    "химик-эксперт": "химик-экспертка", "координатор полевых проверок": "координаторка полевых проверок", "архивист оперативного фонда": "архивистка оперативного фонда",
  };

  const PERSONALITIES = [
    {
      id: "controller", male: "контролирующий", female: "контролирующая",
      signal: "исправляет формулировки, возвращает разговор к точным определениям",
      opening: "Давайте сразу договоримся о терминах. Тогда я отвечу точно.",
      cadence: "коротко и властно", tell: "перестаёт поправлять собеседника, когда разговор касается личной цены конфликта",
    },
    {
      id: "performer", male: "демонстративный", female: "демонстративная",
      signal: "снимает напряжение выразительными деталями и заранее предлагает трактовку",
      opening: "Вы, конечно, уже всё решили. Но хотя бы выслушайте, как это выглядело изнутри.",
      cadence: "образно, с театральными паузами", tell: "называет цвета и звуки, но избегает проверяемых минут",
    },
    {
      id: "anxious", male: "тревожный", female: "тревожная",
      signal: "после каждого ответа проверяет реакцию и уточняет, правильно ли понят",
      opening: "Я отвечу. Только, пожалуйста, не вырывайте отдельную фразу из контекста.",
      cadence: "с оговорками и самопоправками", tell: "точно помнит безопасные детали и путает те, за которые чувствует ответственность",
    },
    {
      id: "analytic", male: "аналитический", female: "аналитическая",
      signal: "отвечает точно, но строго в границах поставленного вопроса",
      opening: "Задавайте по одному вопросу. Время, источник и степень уверенности — отдельно.",
      cadence: "сухо, структурно, без оценок", tell: "пропускает причинные связки и оставляет собеседнику неверный вывод",
    },
    {
      id: "adaptive", male: "адаптивный", female: "адаптивная",
      signal: "быстро подстраивает лексику и эмоциональную температуру под собеседника",
      opening: "Хорошо. Будем говорить так, как вам удобнее. Что именно вы хотите понять?",
      cadence: "мягко и зеркально", tell: "повторяет слова следователя там, где собственная версия ещё не закреплена",
    },
    {
      id: "reserved", male: "замкнутый", female: "замкнутая",
      signal: "замедляется на эмоциональных темах, выдерживает долгие паузы",
      opening: "Спрашивайте. Если не помню — так и скажу. Додумывать за вас не стану.",
      cadence: "редко, прямыми фразами", tell: "телесная реакция появляется только при упоминании конкретного человека",
    },
  ];

  const SCENARIOS = [
    {
      id: "club",
      titles: ["Последний сет", "Синяя пауза", "После третьего аккорда"],
      location: "джаз-клуб «Медная луна»",
      district: "старый речной квартал",
      victimRoles: ["владелец клуба", "саксофонист", "музыкальный продюсер"],
      roles: ["пианист", "бар-менеджер", "звукорежиссёр", "владелица соседнего бара", "частный коллекционер", "ночной курьер"],
      methods: ["яд в бокале", "удар тяжёлым предметом", "инсценированная передозировка"],
      circumstances: [
        "После закрытого ночного сета человека нашли в кабинете без признаков взлома.",
        "Музыка оборвалась на середине записи; к утру один из участников был мёртв.",
      ],
      clients: ["адвокат семьи", "совладелец клуба", "страховой синдикат «Аврора»"],
      sectors: ["музыкальный бизнес", "частные коллекции", "ночная экономика"],
      cacheWords: ["рояль", "сцена", "подвал"],
      labPuzzleIds: ["mail-header"],
    },
    {
      id: "archive",
      titles: ["Пепел каталога", "Нулевая полка", "Дело о пустом конверте"],
      location: "муниципальный архив на улице Радио",
      district: "административный центр",
      victimRoles: ["старший архивист", "реставратор рукописей", "исследователь фондов"],
      roles: ["заместитель директора", "ночной охранник", "реставратор", "историк", "системный администратор", "курьер ведомства"],
      methods: ["удушение", "отравление парами реагента", "падение, выданное за несчастный случай"],
      circumstances: [
        "Из опечатанного фонда исчез конверт, а ответственного сотрудника нашли у служебного лифта.",
        "Камера пропустила одиннадцать минут; ровно в это окно погиб хранитель каталога.",
      ],
      clients: ["попечительский совет", "городская прокуратура", "реставратор спорного фонда"],
      sectors: ["архивное дело", "муниципальные закупки", "генеалогия"],
      cacheWords: ["архив", "лифт", "полка"],
      labPuzzleIds: ["custody-seal"],
    },
    {
      id: "hotel",
      titles: ["Номер без гостя", "Тень в зимнем саду", "Четыре минуты до полуночи"],
      location: "отель «Северный экспресс»",
      district: "вокзальная площадь",
      victimRoles: ["финансовый консультант", "гостиничный аудитор", "иностранный посредник"],
      roles: ["администратор", "консьерж", "переводчик", "постоялец", "водитель", "организатор конференции"],
      methods: ["инъекция редкого препарата", "удар статуэткой", "отравление угарным газом"],
      circumstances: [
        "Гость не покидал номер по камерам, но его тело обнаружили в закрытом зимнем саду.",
        "Номер был заперт изнутри; цифровой ключ использовался уже после установленного времени смерти.",
      ],
      clients: ["управляющая компания отеля", "консульский отдел", "деловой партнёр погибшего"],
      sectors: ["гостиничный бизнес", "международные расчёты", "корпоративный аудит"],
      cacheWords: ["балкон", "лифт", "фонтан"],
      labPuzzleIds: ["spectral-control"],
    },
    {
      id: "radio",
      titles: ["Тишина на 7.042", "Последняя передача", "Частота без позывного"],
      location: "заброшенная радиостанция «Маяк-4»",
      district: "северная промышленная зона",
      victimRoles: ["радиоинженер", "ведущий ночного эфира", "коллекционер аппаратуры"],
      roles: ["радиолюбитель", "техник связи", "журналист", "охранник объекта", "продавец аппаратуры", "бывший военный связист"],
      methods: ["поражение током", "удар корпусом передатчика", "удушение кабелем"],
      circumstances: [
        "После неизвестной передачи в эфире нашли тело у работающего передатчика.",
        "Станция считалась обесточенной, однако ночью кто-то трижды вышел на старой частоте.",
      ],
      clients: ["редакция независимого радио", "владелец промзоны", "союз радиолюбителей"],
      sectors: ["телекоммуникации", "коллекционная электроника", "локальная журналистика"],
      cacheWords: ["антенна", "подвал", "мачта"],
      labPuzzleIds: ["signal-triangulation"],
    },
    {
      id: "laboratory",
      titles: ["Контрольный образец", "Смена в минус двадцать", "Третий журнал калибровки"],
      location: "лаборатория материалов «Каскад-12»",
      district: "исследовательский квартал у грузовой ветки",
      victimRoles: ["руководитель лаборатории", "лаборант химического анализа", "инженер по калибровке"],
      roles: ["специалист по биобезопасности", "оператор криохранилища", "техник вакуумных систем", "координатор поставок", "стажёр-исследователь", "инспектор качества"],
      methods: ["подмена контрольного образца", "локальное отравление парами реагента", "саботаж криогенного контура"],
      circumstances: [
        "После ночного цикла контроля сотрудник не вышел из чистой зоны, а серия образцов оказалась формально безупречной.",
        "В журнале реактора есть штатная ошибка, но вторая запись о калибровке появилась уже после аварийного останова.",
      ],
      clients: ["независимый этический комитет", "поставщик контрольных образцов", "страховой пул исследовательских объектов"],
      sectors: ["материаловедение", "промышленная безопасность", "грантовые исследования"],
      cacheWords: ["термостат", "вытяжка", "криошкаф"],
      labPuzzleIds: ["spectral-control", "custody-seal"],
    },
    {
      id: "museum",
      titles: ["Витрина после закрытия", "Опись с пустой строкой", "Модель, которой не было"],
      location: "музей техники «Вектор»",
      district: "квартал старых мастерских",
      victimRoles: ["куратор музейной коллекции", "фондовый хранитель", "инженер экспозиции"],
      roles: ["специалист по страховой оценке", "реставратор техники", "ночной охранник", "историк", "курьер ведомства", "системный администратор"],
      methods: ["подмена экспоната", "скрытое повреждение витрины", "инсценированное падение со стремянки"],
      circumstances: [
        "После закрытой инвентаризации один экспонат оказался точной копией, а ответственный куратор пропал из камер на девять минут.",
        "Страховой акт оформлен до предполагаемого повреждения, но часть данных в нём подлинна.",
      ],
      clients: ["общество друзей музея", "страховой синдикат «Аврора»", "наследники частной коллекции"],
      sectors: ["музейное дело", "страховая оценка", "частные коллекции"],
      cacheWords: ["витрина", "макет", "мастерская"],
      labPuzzleIds: ["mail-header"],
    },
    {
      id: "terminal",
      titles: ["Прилив в служебной зоне", "Накладная без груза", "Последний рейс «Орфея»"],
      location: "речной терминал «Нижняя пристань»",
      district: "южная грузовая набережная",
      victimRoles: ["диспетчер терминала", "лоцман", "оператор грузового контроля"],
      roles: ["механик причала", "экспедитор", "судовой электрик", "инспектор портовой безопасности", "водитель", "ночной курьер"],
      methods: ["переадресация груза через закрытый причал", "саботаж сигнального контура", "вывоз человека в служебном контейнере"],
      circumstances: [
        "В сменном журнале сходятся все веса, кроме одной паллеты; после её выгрузки ответственный диспетчер перестал выходить на связь.",
        "Тепловизор увидел движение на закрытом причале, но номер судна в этот момент значился в другом доке.",
      ],
      clients: ["портовая администрация", "союз речных перевозчиков", "страховщик груза"],
      sectors: ["речная логистика", "портовая безопасность", "контрактные перевозки"],
      cacheWords: ["причал", "кнехт", "контейнер"],
    },
  ];

  const MOTIVES = [
    "сокрытие хищения",
    "месть за давнее предательство",
    "страх разоблачения",
    "борьба за наследство",
    "шантаж",
    "защита профессиональной репутации",
    "сокрытие подмены документов",
    "принуждение к молчанию ради третьего лица",
    "устранение свидетеля старого преступления",
    "навязчивая потребность восстановить контроль",
    "попытка вернуть незаконно присвоенную собственность",
    "ревность, ошибочно принятая за финансовый конфликт",
    "идеологическая убеждённость",
    "паническая реакция без первоначального намерения убить",
    "отсутствие рационального мотива",
  ];

  const CRIME_PATTERNS = [
    {
      method: "отравление сердечным гликозидом в микродозе",
      apparent: "внезапная сердечная недостаточность",
      staging: "лекарство жертвы переложено в карман, чтобы смерть выглядела следствием забывчивости",
      incidental: "жертва выпила меньше обычного, поэтому токсическое окно сдвинулось почти на двадцать минут",
      trace: "на резьбе флакона остался растительный воск, которого нет в аптечном препарате",
    },
    {
      method: "перекрытие дыхания мягким материалом без выраженных наружных следов",
      apparent: "падение с последующей аспирацией",
      staging: "стул опрокинут уже после смерти, а на пол пролита вода",
      incidental: "автоматическая вентиляция включилась раньше расписания и изменила температуру тела",
      trace: "в глубине воротника найдено волокно, отсутствующее на ковровом покрытии",
    },
    {
      method: "инъекция быстродействующего гипогликемического препарата",
      apparent: "передозировка седативным средством",
      staging: "рядом оставлен чужой блистер с частичным отпечатком случайного посетителя",
      incidental: "плотный ужин замедлил развитие симптомов и разрушил расчёт преступника по времени",
      trace: "прокол скрыт под старой ссадиной, но направление канала не соответствует бытовой травме",
    },
    {
      method: "удар узким тяжёлым предметом через сложенную ткань",
      apparent: "травма при падении на архитектурный выступ",
      staging: "тело перемещено на полтора метра, а сломанный предмет подложен под руку",
      incidental: "робот-уборщик пересёк первичный след и перенёс микрочастицы в соседний коридор",
      trace: "форма внутреннего кровоизлияния уже и глубже, чем поверхность предполагаемого выступа",
    },
    {
      method: "локальное воздействие угарного газа из переносного каталитического источника",
      apparent: "неисправность старой отопительной системы",
      staging: "датчик отключён под видом плановой замены батареи",
      incidental: "сквозняк создал вторую зону накопления газа и временно поставил под угрозу свидетеля",
      trace: "копоть на решётке содержит катализатор, не используемый в системе здания",
    },
    {
      method: "поражение током через заранее повреждённое заземление",
      apparent: "случайная неисправность оборудования",
      staging: "в журнал ремонта добавлена запись задним числом с действующего аккаунта невиновного сотрудника",
      incidental: "скачок напряжения в соседнем квартале произошёл в тот же час и создал убедительное совпадение",
      trace: "изоляция надрезана инструментом, а не разрушена температурой или усталостью материала",
    },
    {
      method: "комбинация седативного аэрозоля и механического удушения",
      apparent: "токсическая реакция на чистящий реагент",
      staging: "контейнер бытовой химии оставлен открытым, накладная на поставку подменена",
      incidental: "у жертвы была редкая чувствительность к растворителю, поэтому часть симптомов действительно совпала с ложной версией",
      trace: "концентрация реагента в воздухе недостаточна для смерти, но в крови присутствует другой метаболит",
    },
    {
      method: "смертельная травма в ходе незапланированной борьбы",
      apparent: "заранее подготовленное убийство ради документов",
      staging: "в панике участник уничтожил запись и тем самым создал видимость продуманного сокрытия",
      incidental: "первоначальная встреча была посвящена шантажу, но намерения убивать до конфликта не было",
      trace: "расположение следов борьбы противоречит версии о внезапном нападении со спины",
    },
  ];

  const CONFLICT_PATTERNS = [
    {
      title: "Скрытый долг и публичная ссора",
      hook: "перевод между двумя участниками совпал по дате с громким конфликтом",
      turn: "деньги оказались возвратом общего залога, а ссора касалась третьего человека",
      closure: "исходная квитанция и сообщение посредника замыкают цепочку вне окна преступления",
    },
    {
      title: "Поддельная рекомендация",
      hook: "один участник помог другому получить должность по документу с ложной подписью",
      turn: "центральный участник знал о подделке и использовал её как рычаг, поэтому оба скрывали контакт",
      closure: "доступ к шаблону был у третьего отдела; связь объясняет ложь, но не средство преступления",
    },
    {
      title: "Ревность без романа",
      hook: "частые ночные звонки создают впечатление тайной связи и сильного личного мотива",
      turn: "звонки относились к группе взаимопомощи, где один участник защищал чужую конфиденциальность",
      closure: "содержание подтверждено независимым координатором; конфликт реален, но не связан с основным событием",
    },
    {
      title: "Чужой пропуск",
      hook: "персональный идентификатор одного участника использовал другой, ранее отрицавший знакомство",
      turn: "они скрывали не преступление, а запрещённую подработку в закрытом помещении",
      closure: "камера погрузочной зоны фиксирует обоих после критического окна в другом корпусе",
    },
    {
      title: "Угроза, адресованная не тому человеку",
      hook: "голосовое сообщение содержит фразу «он до утра не доживёт»",
      turn: "местоимение относилось к тяжело больному родственнику, о котором собеседник не хотел говорить",
      closure: "медицинский вызов и полный контекст сообщения исключают прямую угрозу центральному участнику",
    },
    {
      title: "Лабораторный протокол без подписи",
      hook: "в архиве есть лист контроля с инициалами двух участников и исправленным номером партии",
      turn: "исправление скрывало не событие, а ошибку в учебной серии, за которую оба боялись дисциплинарного взыскания",
      closure: "пломба контрольного образца и независимый журнал прибора помещают учебную серию за три дня до критического окна",
    },
    {
      title: "Семейный адрес в накладной",
      hook: "служебный груз оформлен на адрес родственника одного из участников",
      turn: "адрес использовался как временный пункт хранения после затопления склада, о чём никто не сообщил официально",
      closure: "фотография разгрузки и страховой акт показывают, что спорная поставка была завершена до основного события",
    },
    {
      title: "Удалённый черновик статьи",
      hook: "журналистка и технический сотрудник одновременно удалили один и тот же черновик публикации",
      turn: "текст содержал персональные данные свидетеля, а не компромат на центрального участника",
      closure: "редакционная резервная копия показывает независимую причину удаления и иной временной диапазон",
    },
    {
      title: "Резервная смена без оплаты",
      hook: "два работника отрицали ночное присутствие, хотя табель и камера погрузочной зоны их подтверждают",
      turn: "они закрывали несанкционированную подработку, чтобы не потерять основной договор",
      closure: "маршрут служебного фургона фиксирует их в соседнем корпусе во время критического окна",
    },
    {
      title: "Коллекционер и пустая витрина",
      hook: "частный коллекционер заранее запросил оценку предмета, который позже исчез из фонда",
      turn: "запрос касался копии для выставки, а оригинал видел только страховой эксперт",
      closure: "номера в каталоге, печать музея и независимый снимок экспозиции разделяют две почти идентичные вещи",
    },
  ];

  const CASE_ARCHETYPES = [
    {
      id: "death",
      label: "сомнительная смерть",
      targetLabel: "Жертва",
      outcomeLabel: "смерть",
      mechanismLabel: "причина смерти",
      objective: "установить непосредственную причину, отделить инсценировку и доказать личность причастного",
      patterns: CRIME_PATTERNS,
    },
    {
      id: "disappearance",
      label: "исчезновение человека",
      targetLabel: "Пропавший человек",
      outcomeLabel: "исчезновение",
      mechanismLabel: "способ перемещения и сокрытия",
      objective: "восстановить последний добровольный маршрут и определить, где он превратился в принудительный",
      patterns: [
        { method: "вывоз в служебном фургоне под видом плановой доставки", apparent: "добровольный отъезд после личного конфликта", staging: "счёт за билет куплен с устройства пропавшего уже после исчезновения", incidental: "в тот же вечер человек действительно собирался уехать, но на сутки позже", trace: "в журнале массы фургона появилась лишняя нагрузка, не отражённая в накладной" },
        { method: "заманивание на закрытый объект через подменённую встречу", apparent: "срыв обязательств и побег от долгов", staging: "из квартиры забраны документы и дорожная сумка, но вещи собраны человеком другого роста", incidental: "сосед видел похожую куртку и невольно подтвердил ошибочный маршрут", trace: "календарное приглашение создано из клонированного шаблона, а не из аккаунта указанного отправителя" },
        { method: "удержание в техническом помещении после незапланированной ссоры", apparent: "самостоятельное исчезновение ради защиты третьего лица", staging: "телефон продолжал перемещаться по городу с курьерской сумкой", incidental: "авария связи дала двадцатиминутный разрыв, не связанный с действиями причастного", trace: "датчик двери фиксирует повторное открытие изнутри без соответствующего пропуска" },
      ],
    },
    {
      id: "theft",
      label: "подмена и хищение",
      targetLabel: "Заявитель",
      outcomeLabel: "исчезновение оригинала",
      mechanismLabel: "способ подмены",
      objective: "отделить доступ к объекту от возможности заменить его незаметно и найти путь оригинала",
      patterns: [
        { method: "двухэтапная подмена через возврат с ложным серийным номером", apparent: "ошибка инвентаризации при ночной приёмке", staging: "в системе оставлена фотография оригинала с сохранённой датой", incidental: "в тот день база действительно задвоила три посторонние позиции", trace: "масса упаковки в транспортной ведомости соответствует копии, но не оригиналу" },
        { method: "изъятие из защищённого фонда через временный контейнер-двойник", apparent: "хищение во время отключения камер", staging: "камера отключена намеренно, хотя само изъятие произошло днём раньше", incidental: "реальная неисправность питания сделала позднее отключение убедительным", trace: "пыль под пломбой нарушена до времени, указанного в журнале доступа" },
        { method: "цифровая переадресация курьера на промежуточный адрес", apparent: "сговор водителя и получателя", staging: "поддельное сообщение отправлено из легитимной учётной записи диспетчера", incidental: "водитель скрыл отдельную подработку и поэтому лгал о маршруте", trace: "геозона подтверждает остановку, но подпись маршрута использует устаревший ключ терминала" },
      ],
    },
    {
      id: "sabotage",
      label: "саботаж инфраструктуры",
      targetLabel: "Ответственный сотрудник",
      outcomeLabel: "авария",
      mechanismLabel: "механизм саботажа",
      objective: "разделить техническую случайность, халатность и намеренное вмешательство с отложенным эффектом",
      patterns: [
        { method: "внесение скрытой задержки в контроллер резервного питания", apparent: "износ аккумуляторного блока", staging: "в журнале обслуживания заранее создана запись о деградации батарей", incidental: "два аккумулятора действительно были ниже нормы", trace: "прошивка подписана тестовым ключом подрядчика, не применяемым в рабочем контуре" },
        { method: "частичное перекрытие охлаждения с расчётом на пиковую нагрузку", apparent: "ошибка оператора ночной смены", staging: "предупреждение удалено из панели, но осталось в независимом датчике", incidental: "оператор нарушил инструкцию по другой причине и скрыл это", trace: "крепёж заслонки переставлен инструментом, которого нет в аварийном комплекте" },
        { method: "подмена калибровочного образца, вызывающая накопительную ошибку", apparent: "производственный брак партии", staging: "несколько нормальных измерений добавлены в отчёт задним числом", incidental: "часть партии действительно имела допустимое отклонение", trace: "контрольный образец содержит связующее из другого производственного цикла" },
      ],
    },
    {
      id: "blackmail",
      label: "системный шантаж",
      targetLabel: "Получатель требований",
      outcomeLabel: "серия требований",
      mechanismLabel: "канал получения и использования тайны",
      objective: "установить источник компромата, посредника и того, кто управлял требованиями, не смешивая их роли",
      patterns: [
        { method: "сбор фрагментов тайны через резервные копии нескольких устройств", apparent: "утечка из личного облачного аккаунта", staging: "одна запись входа оставлена намеренно как указание на бывшего партнёра", incidental: "бывший партнёр действительно пытался войти неделей раньше", trace: "фрагменты требований происходят из файлов с разными владельцами, объединённых только локальным архивом" },
        { method: "передача требований через невольного посредника и одноразовые задания", apparent: "личная месть самого курьера", staging: "оплата посреднику оформлена как старый долг", incidental: "между посредником и адресатом существует реальный конфликт", trace: "формулировки заданий повторяют внутренний словарь организации, недоступный посреднику" },
        { method: "физическое фотографирование документов с последующим цифровым монтажом", apparent: "взлом защищённого документооборота", staging: "в систему загружен повреждённый архив, имитирующий неудачную выгрузку", incidental: "в день съёмки проходил настоящий аудит доступа", trace: "перспективные искажения на изображениях соответствуют одной комнате, а не экранным копиям" },
      ],
    },
  ];

  const ARCHETYPE_TITLES = {
    death: ["Нулевая минута", "Тепло после отключения", "След под вторым слоем", "Человек вне кадра", "Ошибка допустимого риска"],
    disappearance: ["Маршрут без пассажира", "Последняя добровольная остановка", "Человек в чужом расписании", "Двадцать минут тишины", "Билет после исчезновения"],
    theft: ["Вес пустой упаковки", "Оригинал без серийного номера", "Возврат, которого не было", "Вторая пломба", "Путь через промежуточный адрес"],
    sabotage: ["Отложенный отказ", "Нормальная авария", "Тестовый ключ", "Точка накопления", "Ошибка, которая ждала нагрузки"],
    blackmail: ["Третья копия тайны", "Посредник без текста", "Требование из разных файлов", "Чужой словарь", "Фотография закрытого документа"],
  };

  const DISCOVERY_ROUTES = [
    { id: "routine", label: "распорядки и окна возможностей", primary: "сопоставление бытовых маршрутов", secondary: "служебные часы и чеки", puzzleIds: ["timeline-order", "routine-gap", "osint-profile"] },
    { id: "financial", label: "платежи и владение", primary: "цепочка малых транзакций", secondary: "адреса доставки и залоги", puzzleIds: ["weak-password", "ledger-chain", "document-provenance"] },
    { id: "physical", label: "материалы и доступ к средству", primary: "происхождение редкого следа", secondary: "инструменты и обслуживание", puzzleIds: ["causal-method", "document-provenance", "timeline-order"] },
    { id: "network", label: "локальная инфраструктура", primary: "связь устройства, места и пользователя", secondary: "резервные копии и ключи", puzzleIds: ["digital-stego", "osint-profile", "weak-password"] },
    { id: "social", label: "отношения и посредники", primary: "граф скрытых контактов", secondary: "несовпадение заявленных легенд", puzzleIds: ["contact-graph", "logic-dead-end", "cipher-caesar"] },
    { id: "provenance", label: "происхождение документов", primary: "версии, подписи и цепь хранения", secondary: "типографика и метаданные", puzzleIds: ["document-provenance", "cipher-caesar", "digital-stego"] },
  ];

  const ORGANIZATION_NAMES = [
    "кооператив «Третий берег»", "служба внутреннего контроля «Контур»", "реставрационная артель «Лён»",
    "транспортная компания «Норд-8»", "городская лаборатория материалов", "радиосеть «Октава»",
    "управляющее бюро «Свод»", "частный депозитарий «Фарватер»", "редакция «Вечерний канал»",
    "профсоюз ночной смены", "страховое общество «Предел»", "аудиторское партнёрство «Мера»",
    "фонд городской памяти", "ремонтный подрядчик «Такт»", "архив технической документации",
  ];

  const STREET_NAMES = ["улица Кассетная", "переулок Мостовой", "набережная Латунная", "улица Верхняя", "проезд Трамвайный", "улица Старых Мастерских", "площадь Депо", "переулок Глухой", "улица Маячная", "набережная Зерновая"];

  const COMMISSIONER_PROFILES = [
    { role: "руководитель внутренней проверки", interest: "не допустить публичного обвинения до проверки цепи хранения", bias: "считает главным риском сговор сотрудников", constraint: "не использовать официальную систему запросов первые два часа" },
    { role: "представитель профсоюза ночной смены", interest: "отделить нарушения режима от причастности к основному событию", bias: "ожидает, что руководство попытается списать всё на младший персонал", constraint: "не раскрывать личные медицинские сведения работников" },
    { role: "страховой следователь", interest: "установить момент, когда случайный риск превратился в намеренное вмешательство", bias: "переоценивает финансовую выгоду", constraint: "каждый вывод должен иметь источник, допустимый для независимой экспертизы" },
    { role: "доверенное лицо наблюдательного совета", interest: "найти путь утечки до утреннего заседания", bias: "подозревает внешнего подрядчика", constraint: "доступ к переписке ограничен локальной копией без исходных серверов" },
    { role: "адвокат сотрудника, давшего первоначальные показания", interest: "проверить, не скрывает ли клиент другое нарушение, ошибочно принятое за основное", bias: "защищает клиента, но не обещает его невиновность", constraint: "не публиковать промежуточные поведенческие оценки" },
    { role: "куратор муниципального гранта", interest: "сохранить проект, если событие не связано с системной коррупцией", bias: "считает конфликт личным", constraint: "до проверки не связываться с прессой и донорами" },
    { role: "дежурный прокурор района", interest: "получить проверяемую рабочую версию без преждевременного задержания", bias: "доверяет временным меткам больше человеческой памяти", constraint: "отдельно отметить сведения, полученные косвенным путём" },
    { role: "внешний специалист по непрерывности бизнеса", interest: "понять, повторится ли событие при следующем цикле работы", bias: "ищет техническое объяснение", constraint: "не останавливать объект и не нарушать текущие процессы" },
  ];

  const MAILBOX_TEMPLATES = [
    { from: "dispatch@city.local", subject: "Сдвиг окна доставки", body: "Маршрут 4-Б перенесён на двадцать минут. Подтвердите новый интервал до конца смены." },
    { from: "billing@utility.local", subject: "Корректировка показаний", body: "Автоматическая сверка выявила расхождение. Начисление будет пересчитано в следующем периоде." },
    { from: "club@records.local", subject: "Бронь стола", body: "Ваш стол удерживается до 21:40. После этого бронь снимается без звонка." },
    { from: "hr@work.local", subject: "Обмен дежурствами", body: "Заявка сохранена как черновик. Подтверждения второго сотрудника пока нет." },
    { from: "repair@building.local", subject: "Доступ в техническую зону", body: "Осмотр перенесён. Старый пропуск будет действовать до завершения заявки." },
    { from: "library@district.local", subject: "Срок возврата", body: "Напоминаем: издание можно продлить ещё один раз через личный кабинет." },
    { from: "no-reply@transit.local", subject: "Поездка завершена", body: "Списано по тарифу ночной пересадки. Время рассчитано по терминалу выхода." },
    { from: "events@quarter.local", subject: "Изменение входа", body: "Из-за ремонта встреча переносится во двор со стороны погрузочной зоны." },
    { from: "storage@cloud.local", subject: "Резервная копия неполна", body: "Два объекта пропущены из-за конфликта версий. Повторная попытка запланирована ночью." },
    { from: "union@shift.local", subject: "Закрытая консультация", body: "Приходите без служебного устройства. Запись ведётся только с согласия участников." },
    { from: "orders@market.local", subject: "Заказ собран частично", body: "Одна позиция заменена аналогом. Серийный номер указан в приложенной накладной." },
    { from: "newsletter@radio.local", subject: "Частоты недели", body: "В выпуске: помехи в северном секторе, восстановление ретранслятора и архивные записи." },
    { from: "accounting@work.local", subject: "Возврат залога", body: "Платёж разбит на две части из-за лимита кассы. Это не повторное списание." },
    { from: "neighbor@home.local", subject: "Ваш ключ у меня", body: "Забрал запасной после протечки, как договаривались. Верну завтра до работы." },
    { from: "lab@work.local", subject: "Контрольный образец принят", body: "Пломба цела, но журнал температуры требует второй подписи. Не открывайте контейнер до пересчёта серии." },
    { from: "security@facility.local", subject: "Дверь бокового коридора", body: "Датчик сработал дважды. Первый импульс пришёл от сервисной карты, второй — от механического открытия." },
    { from: "museum@records.local", subject: "Фондовая карточка исправлена", body: "Серийный номер в описи заменён на архивный. Оригинальная фотография приложена только к внутренней версии." },
    { from: "port@dispatch.local", subject: "Вес по накладной не совпал", body: "Паллету провели через весы дважды. Проверьте, не относится ли вторая отметка к возвратному контейнеру." },
    { from: "ethics@committee.local", subject: "Закрытое заседание", body: "Обсуждаются условия доступа к данным участников. Просим не пересылать выдержки вне защищённого канала." },
    { from: "calendar@shared.local", subject: "Ресурс переговорной освобождён", body: "Бронь снята автоматически после отсутствия отметки. Локальный журнал правок сохранён." },
    { from: "procurement@work.local", subject: "Замена поставщика", body: "Партия пришла с тем же названием, но другим циклом производства. Уточните, можно ли смешивать остатки." },
    { from: "archive@city.local", subject: "Запрос на выдачу копии", body: "Нужный фонд выдан в читальный зал. Оригинал не покидал хранилище по этой заявке." },
    { from: "radio@relay.local", subject: "Несущая частота дрейфует", body: "Отклонение появилось после тестового включения. Логи синхронизации доступны только локально." },
    { from: "hr@work.local", subject: "Исправление в табеле", body: "Одна смена отмечена вручную. Причина: терминал на входе был недоступен девять минут." },
    { from: "legal@advice.local", subject: "Не отправлять оригинал", body: "Для консультации достаточно копии без персональных данных. Цепочка хранения важнее скорости ответа." },
    { from: "billing@utility.local", subject: "Второй счётчик", body: "Показания с резервного контура приняты с задержкой. Время в выгрузке указано по внутренним часам устройства." },
  ];

  const LEGACY_SHA1 = {
    bluevelvet: "78cf0b0d6bea20fd2acf100d81164ce76c4f283c",
    mayak7042: "9e36c00585e47398af66ee2db624de1566d8da55",
    midnight17: "1c9b1392da92c153808a56bd0293a7f6d7e24530",
    oldrecord: "43e5f810d4ee802293f7f572b3be3dda5db75261",
    northwind: "90228e2433a1eca916f8012ce9b912ed4818c713",
  };

  const NOISE_TITLES = [
    "Квитанция кофейного автомата", "Сводка погодной станции", "Заявка на замену лампы",
    "Переписка о парковке", "Копия пропуска посетителя", "Список поставки воды",
    "Технический отчёт вентиляции", "График уборки", "Журнал ложных срабатываний",
    "Рекламная рассылка", "Старая служебная записка", "Ведомость инвентаризации",
    "Пропуск на утреннюю экскурсию", "Заявка на калибровку лифта", "Черновик меню столовой",
    "Карточка возврата библиотечного фонда", "Счёт за зарядку служебного фургона", "Журнал приёма посетителей",
    "Неподтверждённая заявка на ремонт рации", "Протокол уборки чистой зоны", "Каталог запасных пломб",
    "Памятка о смене пароля", "Список цветов для озеленения", "Архивный бюллетень района",
  ];

  const NOISE_LINES = [
    "Запись выглядит необычно, но относится к другой смене.",
    "Имя совпадает, контрольный номер документа — нет.",
    "Время указано по серверу соседнего здания с отставанием на девять минут.",
    "Документ приложен к делу автоматически по географическому признаку.",
    "Упомянутый номер давно выведен из эксплуатации.",
    "Проверка не обнаружила связи с участниками дела.",
    "В документе совпадает адрес организации, но указан другой корпус и предыдущий год.",
    "Выгрузка корректна, однако относится к учебной серии, а не к рабочему циклу.",
    "Поля заполнены сотрудником с тем же именем, но другим табельным номером.",
    "Системное время совпадает случайно: источник использует другой часовой пояс.",
    "Запись подтверждает бытовой маршрут устройства, но не присутствие человека.",
    "Материал подлинный, но поступил из несвязанного внутреннего расследования.",
  ];

  const dom = {
    workspace: document.querySelector("#workspace"),
    caseHeading: document.querySelector("#case-heading"),
    sidebarBrief: document.querySelector("#sidebar-brief"),
    boardCount: document.querySelector("#board-count"),
    archiveCount: document.querySelector("#archive-count"),
    puzzleCount: document.querySelector("#puzzle-count"),
    taskIndicator: document.querySelector("#task-indicator"),
    computerIndicator: document.querySelector("#computer-indicator"),
    timer: document.querySelector("#case-timer"),
    status: document.querySelector("#status-message"),
    newCaseDialog: document.querySelector("#new-case-dialog"),
    newCaseForm: document.querySelector("#new-case-form"),
    detailDialog: document.querySelector("#detail-dialog"),
    detailContent: document.querySelector("#detail-content"),
    helpDialog: document.querySelector("#help-dialog"),
    settingsDialog: document.querySelector("#settings-dialog"),
    settingsForm: document.querySelector("#settings-form"),
    settingsButton: document.querySelector("#settings-button"),
    notes: document.querySelector("#global-notes"),
    quotes: document.querySelector("#global-quotes"),
    noteSaveState: document.querySelector("#notes-save-state"),
    checklist: document.querySelector("#checklist"),
    checklistForm: document.querySelector("#checklist-form"),
    toastRegion: document.querySelector("#toast-region"),
  };

  let caseData = null;
  let state = null;
  let archiveQuery = "";
  let archiveFilter = "all";
  let noteSaveTimer = null;
  let ticker = null;
  let audioSystem = null;
  let appSettings = loadSettings();
  let suppressBoardClickUntil = 0;
  let boardLineFrame = 0;

  function hashSeed(value) {
    let hash = 1779033703 ^ value.length;
    for (let index = 0; index < value.length; index += 1) {
      hash = Math.imul(hash ^ value.charCodeAt(index), 3432918353);
      hash = (hash << 13) | (hash >>> 19);
    }
    return () => {
      hash = Math.imul(hash ^ (hash >>> 16), 2246822507);
      hash = Math.imul(hash ^ (hash >>> 13), 3266489909);
      return (hash ^= hash >>> 16) >>> 0;
    };
  }

  function randomGenerator(seed) {
    const seedHash = hashSeed(seed)();
    let value = seedHash;
    return () => {
      value += 0x6d2b79f5;
      let result = value;
      result = Math.imul(result ^ (result >>> 15), result | 1);
      result ^= result + Math.imul(result ^ (result >>> 7), result | 61);
      return ((result ^ (result >>> 14)) >>> 0) / 4294967296;
    };
  }

  function choose(rng, list) {
    return list[Math.floor(rng() * list.length)];
  }

  function shuffled(rng, list) {
    const copy = [...list];
    for (let index = copy.length - 1; index > 0; index -= 1) {
      const target = Math.floor(rng() * (index + 1));
      [copy[index], copy[target]] = [copy[target], copy[index]];
    }
    return copy;
  }

  function uniquePeople(rng, count) {
    const people = [];
    const used = new Set();
    while (people.length < count) {
      const gender = rng() < 0.5 ? "male" : "female";
      const bank = PERSON_NAME_BANK[gender];
      const firstName = choose(rng, bank.first);
      const lastName = choose(rng, bank.last);
      const name = `${firstName} ${lastName}`;
      if (!used.has(name)) {
        used.add(name);
        people.push({ name, firstName, lastName, gender });
      }
    }
    return people;
  }

  function gendered(person, masculine, feminine) {
    return person.gender === "female" ? feminine : masculine;
  }

  function roleForGender(role, gender) {
    if (gender === "female") return ROLE_FORMS[role] || role;
    const maleForm = Object.entries(ROLE_FORMS).find(([, femaleForm]) => femaleForm === role)?.[0];
    return maleForm || role;
  }

  function pastTense(person, masculine, feminine) {
    return gendered(person, masculine, feminine);
  }

  function escapeHtml(value) {
    return String(value)
      .replaceAll("&", "&amp;")
      .replaceAll("<", "&lt;")
      .replaceAll(">", "&gt;")
      .replaceAll('"', "&quot;")
      .replaceAll("'", "&#039;");
  }

  function normalize(value) {
    return String(value)
      .toLocaleLowerCase("ru-RU")
      .replaceAll("ё", "е")
      .replace(/[^a-zа-я0-9]/gi, "")
      .trim();
  }

  function sentenceStart(value) {
    const text = String(value).trim();
    return text ? text[0].toLocaleUpperCase("ru-RU") + text.slice(1) : text;
  }

  function initials(name) {
    return name
      .split(" ")
      .map((part) => part[0])
      .join("")
      .slice(0, 2)
      .toUpperCase();
  }

  function surname(name) {
    return name.split(" ").at(-1);
  }

  function formatClock(totalMinutes) {
    const normalizedMinutes = ((totalMinutes % 1440) + 1440) % 1440;
    const hours = Math.floor(normalizedMinutes / 60);
    const minutes = normalizedMinutes % 60;
    return `${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}`;
  }

  function formatDuration(milliseconds) {
    const totalSeconds = Math.max(0, Math.floor(milliseconds / 1000));
    const minutes = Math.floor(totalSeconds / 60);
    const seconds = totalSeconds % 60;
    return `${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}`;
  }

  function pluralRu(number, one, few, many) {
    const absolute = Math.abs(number) % 100;
    const last = absolute % 10;
    if (absolute > 10 && absolute < 20) return many;
    if (last === 1) return one;
    if (last >= 2 && last <= 4) return few;
    return many;
  }

  function encodeCaesar(text, shift) {
    const alphabet = "АБВГДЕЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ";
    return [...text]
      .map((letter) => {
        const index = alphabet.indexOf(letter);
        return index < 0 ? letter : alphabet[(index + shift) % alphabet.length];
      })
      .join("");
  }

  function textToHex(text) {
    return [...new TextEncoder().encode(text)]
      .map((value) => value.toString(16).padStart(2, "0"))
      .join(" ");
  }

  function hiddenBinary(text) {
    return [...text]
      .flatMap((letter) => letter.charCodeAt(0).toString(2).padStart(8, "0").split(""))
      .map((bit) => (bit === "0" ? "\u200b" : "\u200c"))
      .join("");
  }

  function randomCaseCode() {
    const words = ["BLUE", "BRASS", "NIGHT", "ECHO", "VELVET", "SMOKE", "MUTE", "NOIR"];
    const word = words[Math.floor(Math.random() * words.length)];
    const number = Math.floor(1000 + Math.random() * 9000);
    return `${word}-${number}`;
  }

  function createAcrostic(word) {
    const lines = {
      а: "Август оставляет на стёклах пыль.",
      б: "Блеклый свет задержался у двери.",
      в: "Вечер помнит шаги лучше людей.",
      г: "Город молчит, когда ему выгодно.",
      и: "Имена стираются, номера остаются.",
      л: "Лишняя пауза бывает признанием.",
      м: "Медная стрелка не врёт о времени.",
      н: "Никто не смотрит под собственные ноги.",
      о: "Окна напротив погасли слишком рано.",
      п: "Пепел легче бумаги, но помнит огонь.",
      р: "Ровно в полночь тишина становится громче.",
      с: "Старый ключ всё ещё знает замок.",
      т: "Третий звонок был короче первых двух.",
      ф: "Фонарь качнулся без ветра.",
      ц: "Цифры на полях написаны другой рукой.",
      х: "Холод пришёл со стороны лестницы.",
      ь: "Ь — знак невозможный; пропусти его.",
      я: "Ящик закрыт, но пыль вокруг нарушена.",
    };
    return [...word].map((letter) => lines[letter] || `${letter.toUpperCase()} — отметка на полях.`).join("\n");
  }

  function generateAddress(rng) {
    return `${choose(rng, STREET_NAMES)}, ${4 + Math.floor(rng() * 116)}, кв. ${1 + Math.floor(rng() * 84)}`;
  }

  function buildMailbox(rng, context) {
    const { victim, culprit, route, incidentMinute, organization, scenario, pattern, supportStaff = [] } = context;
    const ordinary = shuffled(rng, MAILBOX_TEMPLATES).slice(0, 7 + Math.floor(rng() * 4)).map((template, index) => ({
      id: `mail-${index + 1}`,
      from: template.from.replace("work.local", organization.domain),
      subject: template.subject,
      time: formatClock(9 * 60 + Math.floor(rng() * 690)),
      body: template.body,
      important: index === 2 || (index === 5 && rng() > 0.45),
      folder: choose(rng, ["Входящие", "Входящие", "Архив", "Уведомления"]),
    }));

    const routeClues = {
      routine: {
        from: `calendar@${organization.domain}`,
        subject: "Исправление времени встречи",
        body: `В исходном приглашении стояло ${formatClock(incidentMinute - 34)}. После синхронизации — ${formatClock(incidentMinute - 18)}. Изменение сделал общий ресурс переговорной, а не участник встречи.`,
        finding: "Календарная поправка сдвигает окно, но не устанавливает автора изменения.",
      },
      financial: {
        from: `accounting@${organization.domain}`,
        subject: "Платёж вернулся без назначения",
        body: `Сумма разбита на ${2 + Math.floor(rng() * 3)} части. Один токен связан со старым адресом доставки, которым пользовались несколько сотрудников. Нужна сверка конечного получателя.`,
        finding: "Письмо подтверждает структуру платежа, но связь с человеком требует адресной истории.",
      },
      physical: {
        from: `maintenance@${organization.domain}`,
        subject: "Материал выдан без номера заказа",
        body: `Остаток из ремонтного комплекта передан на объект «${scenario.location}». В бумажном журнале подпись есть, в электронной заявке — нет.`,
        finding: `Выдача объясняет доступ к материалу, связанному со следом: ${pattern.trace}. Получателя ещё нужно установить.`,
      },
      network: {
        from: `backup@${organization.domain}`,
        subject: "Конфликт двух локальных снимков",
        body: `Узел ${context.alias} появился в двух копиях с разными владельцами профиля. Автоматическое объединение отменено; сохранены оба идентификатора устройства.`,
        finding: "Псевдоним принадлежит профилю, но устройство в критический момент могло быть передано.",
      },
      social: {
        from: `room@${organization.domain}`,
        subject: "Кто оставил третий стул?",
        body: `Переговорная была заказана на двоих, но после встречи нашли три чашки. Камера коридора не покрывает боковую лестницу.`,
        finding: "Встреча имела незаявленного третьего участника; письмо не раскрывает его личность.",
      },
      provenance: {
        from: `docs@${organization.domain}`,
        subject: "Версия файла не совпала",
        body: `Вложение создано раньше письма, но подпись добавлена позже с общего терминала. Не используйте поле «Автор» как идентификатор сотрудника.`,
        finding: "Цепь версий опровергает заявленное время документа, но общий терминал оставляет несколько возможных авторов.",
      },
    };
    const clue = routeClues[route.id];
    const clueMessage = {
      id: `mail-${ordinary.length + 1}`,
      from: clue.from,
      subject: clue.subject,
      time: formatClock(incidentMinute - 67 - Math.floor(rng() * 90)),
      body: clue.body,
      important: rng() > 0.5,
      folder: choose(rng, ["Входящие", "Архив", "Помеченные"]),
      unlock: "computer-mail",
    };
    const sentMessage = {
      id: `mail-${ordinary.length + 2}`,
      from: victim.name,
      subject: choose(rng, ["Re: завтра", "без темы", "проверю после смены", "список на потом"]),
      time: formatClock(incidentMinute - 180 - Math.floor(rng() * 240)),
      body: choose(rng, [
        "Не пересылай пока. Там два разных вопроса, и один из них вообще не относится к работе.",
        "Я видел несоответствие, но сначала хочу проверить первичный источник. Иначе обвиню не того человека.",
        "В календаре снова неверное время. Оставлю бумажную копию у себя.",
        "Если я опоздаю, начинайте без меня. Телефон может быть выключен из-за резервного копирования.",
      ]),
      important: true,
      folder: "Отправленные",
    };
    const staffMessages = supportStaff.slice(0, 2).map((person, index) => ({
      id: `mail-staff-${index + 1}`,
      from: `${person.firstName.toLocaleLowerCase("ru-RU")}.${person.lastName.toLocaleLowerCase("ru-RU").replaceAll("ё", "е")}@${organization.domain}`,
      subject: index === 0 ? "Нужен второй контрольный замер" : "Уточнение по цепочке хранения",
      time: formatClock(incidentMinute - 130 + index * 19),
      body: index === 0
        ? `${victim.name}, я отмечаю только наблюдение: ${pattern.trace}. До сравнения с контрольной серией это не указывает на человека. ${person.name}, ${person.role}.`
        : `Пожалуйста, не объединяйте поле автора, учётную запись и владельца носителя. По материалу «${scenario.location}» это три разные роли. ${person.name}, ${person.role}.`,
      important: index === 0 && rng() > 0.55,
      folder: choose(rng, ["Входящие", "Архив", "Черновики"]),
    }));
    return {
      messages: shuffled(rng, [...ordinary, clueMessage, sentMessage, ...staffMessages]),
      clue,
      clueSender: clueMessage.from,
      clueSubject: clueMessage.subject,
    };
  }

  function generateCase(seed, difficultyKey) {
    const rng = randomGenerator(`${seed}:${difficultyKey}`);
    const difficulty = DIFFICULTIES[difficultyKey];
    const scenario = choose(rng, SCENARIOS);
    const archetype = choose(rng, CASE_ARCHETYPES);
    const discoveryRoute = choose(rng, DISCOVERY_ROUTES);
    const people = uniquePeople(rng, difficulty.suspects + 4);
    const victimPerson = people.shift();
    const staffIdentities = people.splice(-3);
    const victimName = victimPerson.name;
    const victimRole = roleForGender(choose(rng, scenario.victimRoles), victimPerson.gender);
    const roles = shuffled(rng, scenario.roles).slice(0, difficulty.suspects);
    const culpritIndex = Math.floor(rng() * difficulty.suspects);
    const incidentMinute = 21 * 60 + 35 + Math.floor(rng() * 105);
    const motive = choose(rng, MOTIVES);
    const crimePattern = choose(rng, archetype.patterns);
    const method = crimePattern.method;
    const apparentMotive = choose(rng, MOTIVES.filter((item) => item !== motive));
    const cacheWord = choose(rng, scenario.cacheWords);
    const alias = `${choose(rng, ["blue", "north", "mute", "grey", "last"])}_${Math.floor(10 + rng() * 89)}`;
    const accessGroup = choose(rng, ["ночной персонал", "техническое обслуживание", "архивный допуск", "внешний осмотр", "резервная смена"]);
    const passwordOptions = ["bluevelvet", "mayak7042", "midnight17", "oldrecord", "northwind"];
    const password = choose(rng, passwordOptions);
    const safeLocations = ["в круглосуточном кафе", "у себя дома", "на другом конце города", "на вокзале", "в мастерской"];
    const relationships = ["острый деловой конфликт", "давнее знакомство", "скрытая взаимная зависимость", "непрозрачная финансовая связь", "личная обида", "контакт через посредника"];
    const privateStakes = [
      { male: "скрывал подработку, нарушающую договор", female: "скрывала подработку, нарушающую договор" },
      { male: "защищал семейную тайну другого участника", female: "защищала семейную тайну другого участника" },
      { male: "лгал о знакомстве из страха потерять должность", female: "лгала о знакомстве из страха потерять должность" },
      { male: "помогал центральному участнику обходить внутренние правила", female: "помогала центральному участнику обходить внутренние правила" },
      { male: "имел неподтверждённый долг перед третьим лицом", female: "имела неподтверждённый долг перед третьим лицом" },
      { male: "боялся огласки старой профессиональной ошибки", female: "боялась огласки старой профессиональной ошибки" },
    ];
    const organizations = shuffled(rng, ORGANIZATION_NAMES).slice(0, 4).map((name, index) => ({
      id: `org-${index + 1}`,
      name,
      domain: `org${Math.floor(rng() * 90 + 10)}-${index + 1}.local`,
      address: generateAddress(rng),
    }));
    const primaryOrganization = organizations[0];
    const commissionerProfile = choose(rng, COMMISSIONER_PROFILES);
    const commissionerIdentity = uniquePeople(rng, 1)[0];
    const commissioner = {
      name: commissionerIdentity.name,
      gender: commissionerIdentity.gender,
      role: commissionerProfile.role,
      organization: organizations[1].name,
      interest: commissionerProfile.interest,
      bias: commissionerProfile.bias,
      constraint: commissionerProfile.constraint,
    };
    const laboratoryRoles = [
      "эксперт по трасологии", "аналитик цифровых образов", "специалист по цепочке хранения",
      "химик-эксперт", "координатор полевых проверок", "архивист оперативного фонда",
    ];
    const supportStaff = staffIdentities.map((identity, index) => ({
      id: `staff-${index + 1}`,
      ...identity,
      role: roleForGender(laboratoryRoles[index], identity.gender),
      organization: organizations[(index + 1) % organizations.length].name,
      specialty: ["микроследы и контрольные образцы", "метаданные и локальные журналы", "маркировка, пломбы и версии документов"][index],
    }));

    const suspects = people.map((identity, index) => {
      const isCulprit = index === culpritIndex;
      const personality = PERSONALITIES[index % PERSONALITIES.length];
      const claimedLocation = choose(rng, safeLocations);
      const arrivalDelta = -18 - Math.floor(rng() * 38);
      const alibi = isCulprit
        ? `${pastTense(identity, "Находился", "Находилась")} ${claimedLocation} с ${formatClock(incidentMinute - 55)} до ${formatClock(incidentMinute + 20)}.`
        : `${pastTense(identity, "Был", "Была")} ${claimedLocation}; часть интервала подтверждается независимой записью, но остаётся разрыв в ${9 + index * 2} минут.`;
      return {
        id: `person-${index + 1}`,
        ...identity,
        role: roleForGender(roles[index], identity.gender),
        employer: organizations[index % organizations.length].name,
        workAddress: organizations[index % organizations.length].address,
        homeAddress: generateAddress(rng),
        routine: choose(rng, [
          `обычно приходит за ${12 + index * 3} минут до смены`,
          `по четвергам уходит через боковой выход в ${formatClock(incidentMinute - 84)}`,
          "не носит служебный телефон домой",
          "пользуется общим шкафчиком и регулярно меняется пропуском с коллегой",
          "после смены делает одну и ту же пересадку у площади Депо",
          "ведёт бумажный календарь и редко подтверждает цифровые приглашения",
        ]),
        habit: choose(rng, ["платит наличными за мелкие покупки", "сохраняет все транспортные чеки", "отвечает на письма только с рабочего терминала", "выключает геолокацию после смены", "часто одалживает рабочие инструменты", "использует два разных написания фамилии в старых базах"]),
        relationship: relationships[index % relationships.length],
        personalityId: personality.id,
        personality: gendered(identity, personality.male, personality.female),
        behavior: personality.signal,
        cadence: personality.cadence,
        tell: personality.tell,
        opening: personality.opening,
        privateStake: gendered(identity, privateStakes[index % privateStakes.length].male, privateStakes[index % privateStakes.length].female),
        alibi,
        isCulprit,
        arrivalDelta,
        detail: choose(rng, [
          "запах мокрой шерсти в коридоре",
          "дважды погасший индикатор камеры",
          "несовпадение цвета папки и точно названного номера кабинета",
          "песня, которой не было в программе вечера",
          "короткий металлический удар за служебной стеной",
          "фигура без верхней одежды у служебного входа",
        ]),
      };
    });

    const culprit = suspects[culpritIndex];
    const culpritSurname = surname(culprit.name);
    const innocentSuspects = shuffled(rng, suspects.filter((person) => !person.isCulprit));
    const cipherDirect = ["social", "provenance"].includes(discoveryRoute.id);
    const cipherContact = cipherDirect ? culprit : innocentSuspects[0];
    const cipherSurname = surname(cipherContact.name);
    const osintDirect = ["network", "routine"].includes(discoveryRoute.id);
    const osintContact = osintDirect ? culprit : innocentSuspects[1 % innocentSuspects.length];
    const calendarDirect = ["financial", "provenance"].includes(discoveryRoute.id);
    const calendarContact = calendarDirect ? culprit : innocentSuspects[2 % innocentSuspects.length];
    const digitalDirect = discoveryRoute.id === "network";
    const digitalContact = digitalDirect ? culprit : innocentSuspects[0];
    const routineDirect = discoveryRoute.id === "routine";
    const routineContact = routineDirect ? culprit : innocentSuspects[0];
    const financialDirect = discoveryRoute.id === "financial";
    const financialContact = financialDirect ? culprit : innocentSuspects[1 % innocentSuspects.length];
    const socialDirect = discoveryRoute.id === "social";
    const socialContact = socialDirect ? culprit : innocentSuspects[1 % innocentSuspects.length];
    const provenanceDirect = discoveryRoute.id === "provenance";
    const provenanceContact = provenanceDirect ? culprit : innocentSuspects[2 % innocentSuspects.length];
    const cellDirect = ["routine", "network"].includes(discoveryRoute.id);
    const cellContact = cellDirect ? culprit : innocentSuspects[2 % innocentSuspects.length];
    const conflictChains = Array.from({ length: difficulty.deadEnds }, (_, index) => {
      const pattern = CONFLICT_PATTERNS[index % CONFLICT_PATTERNS.length];
      const first = innocentSuspects[index % innocentSuspects.length];
      const second = innocentSuspects[(index + 1) % innocentSuspects.length];
      return {
        id: `conflict-${index + 1}`,
        title: pattern.title,
        actorIds: [first.id, second.id],
        actorNames: [first.name, second.name],
        hook: `${first.name} и ${second.name}: ${pattern.hook}.`,
        turn: pattern.turn,
        closure: pattern.closure,
        apparentMotive: choose(rng, MOTIVES.filter((item) => item !== motive)),
        excludedBy: formatClock(incidentMinute - 6 - index * 3),
      };
    });
    const incidentAddress = `${scenario.location}, ${generateAddress(rng)}`;
    const caseTitle = choose(rng, [...ARCHETYPE_TITLES[archetype.id], ...scenario.titles]);
    const incidentSummary = {
      death: `${victimName} ${pastTense(victimPerson, "найден", "найдена")} без сознания на объекте «${scenario.location}». Медицинская картина сначала выглядела естественной, но время, температура и один материальный след не образуют согласованной версии.`,
      disappearance: `${victimName} не ${pastTense(victimPerson, "появился", "появилась")} на обязательной встрече. Телефон продолжал двигаться, цифровой билет был куплен позже, а вещи из дома исчезли выборочно.`,
      theft: `${victimName} ${pastTense(victimPerson, "обнаружил", "обнаружила")}, что защищённый объект заменён точной копией. Все пломбы формально целы, а журнал показывает несколько правдоподобных окон подмены.`,
      sabotage: `На участке, за который ${pastTense(victimPerson, "отвечал", "отвечала")} ${victimName}, произошёл отложенный отказ. Часть нарушений реальна, но ни одно отдельно не объясняет момент аварии.`,
      blackmail: `${victimName} ${pastTense(victimPerson, "получил", "получила")} третье требование, составленное из фрагментов, которые не хранились вместе. Курьер, автор текста и источник компромата могут быть разными людьми.`,
    }[archetype.id];
    const mandateText = choose(rng, [
      `Нужно проверить, где версия «${crimePattern.apparent}» перестаёт объяснять факты. ${commissioner.constraint}.`,
      `Не ищите человека с самым громким конфликтом. Сначала установите путь возможности: ${discoveryRoute.primary}.`,
      `У нас есть несколько настоящих нарушений и только одно основное событие. ${commissioner.interest}.`,
      `Предварительная комиссия уже выбрала удобную причину. Ваша задача — показать, какой независимый источник её выдерживает или разрушает.`,
      `Срок — до следующей смены. Отдельно пометьте факты, ложь ради другого проступка и выводы, зависящие от допущений.`,
    ]);
    const sceneObservation = choose(rng, {
      death: [
        "Расположение посуды не соответствует показаниям о внезапном ухудшении: один предмет переставляли после основного события.",
        "Температура поверхности и служебный журнал расходятся на семнадцать минут; оба измерения исправны, но относятся к разным моментам.",
        "На манжете обнаружен промышленный порошок, отсутствующий в помещении, однако сам по себе он не мог вызвать исход.",
      ],
      disappearance: [
        "Из шкафа взяты сезонные вещи, но оставлены документы и ежедневное лекарство; сборы выглядят одновременно намеренными и незавершёнными.",
        "След обуви у служебного выхода старше заявленного времени исчезновения, хотя рядом есть свежий след тележки.",
        "Домашняя камера показывает выход, но отражение в витрине указывает на перенос сумки другим человеком.",
      ],
      theft: [
        "Пломбы подлинные, но нанесены на новый слой пыли: их снимали и вернули без видимого повреждения.",
        "Масса копии совпадает с оригиналом, а распределение веса — нет; подмена готовилась под поверхностную проверку.",
        "Внутри витрины найдено волокно упаковки, которую обычно используют не хранители, а страховые оценщики.",
      ],
      sabotage: [
        "Повреждённый узел действительно был изношен, но отказ начался только после штатной автоматической проверки.",
        "На панели есть след неправильной регулировки и независимый след замены датчика; эти вмешательства совершены в разные смены.",
        "Аварийный журнал содержит правдивую ошибку оператора, не объясняющую отложенный запуск второй неисправности.",
      ],
      blackmail: [
        "Конверт собран из обычных материалов трёх разных поставщиков; необычен не материал, а порядок их появления.",
        "Отпечаток курьера пригоден для идентификации, но машинописная лента и формулировки происходят из другого места.",
        "Фрагмент требования был приклеен повторно: первая версия текста отличалась одним местоимением и размером суммы.",
      ],
    }[archetype.id]);
    const victimProfiles = {
      routine: [
        `${pastTense(victimPerson, "стал", "стала")} выходить на одну остановку раньше и дважды опоздал${victimPerson.gender === "female" ? "а" : ""} на привычную пересадку`,
        `${pastTense(victimPerson, "обменял", "обменяла")} дежурство, но сохранил${victimPerson.gender === "female" ? "а" : ""} прежний заказ такси`,
      ],
      financial: [
        `${pastTense(victimPerson, "разделил", "разделила")} один возврат на три счёта и запросил${victimPerson.gender === "female" ? "а" : ""} копию старого залога`,
        `${pastTense(victimPerson, "отказался", "отказалась")} от наличного расчёта, а через день оплатил${victimPerson.gender === "female" ? "а" : ""} чужую камеру хранения`,
      ],
      physical: [
        `${pastTense(victimPerson, "запросил", "запросила")} образец редкого материала и вернул${victimPerson.gender === "female" ? "а" : ""} упаковку с другим серийным номером`,
        `${pastTense(victimPerson, "интересовался", "интересовалась")} графиком калибровки и забрал${victimPerson.gender === "female" ? "а" : ""} списанный инструмент`,
      ],
      network: [
        `${pastTense(victimPerson, "отключил", "отключила")} синхронизацию телефона, но оставил${victimPerson.gender === "female" ? "а" : ""} активным домашний планшет`,
        `${pastTense(victimPerson, "сменил", "сменила")} пароль локальной копии и передал${victimPerson.gender === "female" ? "а" : ""} резервный ключ не тому отделу`,
      ],
      social: [
        `${pastTense(victimPerson, "отменил", "отменила")} общий ужин, а затем встретил${victimPerson.gender === "female" ? "а" : ""} двух его участников по отдельности`,
        `${pastTense(victimPerson, "скрыл", "скрыла")} родство с посредником и публично поддержал${victimPerson.gender === "female" ? "а" : ""} его оппонента`,
      ],
      provenance: [
        `${pastTense(victimPerson, "попросил", "попросила")} бумажный оригинал и одновременно удалил${victimPerson.gender === "female" ? "а" : ""} собственную цифровую правку`,
        `${pastTense(victimPerson, "оспорил", "оспорила")} подпись на акте, хотя ранее ссылал${victimPerson.gender === "female" ? "ась" : "ся"} на его содержание`,
      ],
    };
    const victimRecentAction = choose(rng, victimProfiles[discoveryRoute.id]);
    const witness = choose(rng, [
      { title: "Показания диспетчера", excerpt: "Свидетель помнит порядок действий, но сверял время по отстающим часам.", quote: `«Сначала погас индикатор, затем появился человек с папкой. На часах было ${formatClock(incidentMinute - 16)}, но позже выяснилось, что они отстают»`, reliability: "высокая для последовательности; низкая для точного времени" },
      { title: "Показания ночной курьерки", excerpt: "Курьерка различила сумку и маршрут, но не лицо человека.", quote: `«У служебной двери стояла знакомая сумка с белой стяжкой. Человек ушёл в сторону ${choose(rng, STREET_NAMES)}, лица я не видела»`, reliability: "средняя; маршрут подтверждён заказом, личность не установлена" },
      { title: "Показания уборщика", excerpt: "Свидетель слышал разговор, но мог соединить две разные сцены.", quote: `«Голос сказал: “не здесь”. Через несколько минут хлопнула дверь. Я решил, что это те же люди, но коридор поворачивает»`, reliability: "средняя; акустика коридора создаёт ошибку источника" },
      { title: "Показания соседки объекта", excerpt: "Свидетельница узнала привычку, а не внешность.", quote: `«Кто-то дважды проверил ручку и вернулся за перчатками. Так делает один из работников, но сегодня куртка была другой»`, reliability: "средняя; узнавание поведения не равно идентификации" },
      { title: "Запись дежурного мастера", excerpt: "Мастер видел предмет до события, однако его журнал заполнен позже.", quote: `«Я уверен, что пломба уже была перекошена. Строку внёс утром по памяти, потому что терминал ночью не работал»`, reliability: "высокая для неисправности терминала; средняя для наблюдения" },
    ]);
    const strongEvidenceByRoute = {
      routine: ["ev-routine-gap", "ev-timeline", "ev-partner-cell"],
      financial: ["ev-ledger-chain", "ev-password", "ev-partner-money"],
      physical: ["ev-computer-meta", "ev-timeline", "ev-access"],
      network: ["ev-digital", "ev-osint", "ev-partner-cell"],
      social: ["ev-contact-graph", "ev-cipher", "ev-witness"],
      provenance: ["ev-document-provenance", "ev-cipher", "ev-computer-meta"],
    };
    const routeReconstruction = {
      routine: `Сопоставление регулярного маршрута, независимой отметки времени и отклонения в привычке оставляет проверяемое окно возможности. Профиль распорядка: ${culprit.name}; в заявленном алиби этого окна нет.`,
      financial: `Токен возврата, история владения адресом и разделённый платёж сходятся на одном учётном профиле. Имя профиля: ${culprit.name}; одинаковая сумма без этой цепи была бы лишь совпадением.`,
      physical: `Происхождение материала, доступ к инструменту и время обслуживания сходятся на одном рабочем контуре. Связанный профиль: ${culprit.name}. Один лабораторный след личности не устанавливал.`,
      network: `Узел ${alias}, локальный снимок и привычка использования устройства образуют непрерывную техническую цепь до личного профиля. Имя профиля: ${culprit.name}. Геопозиция сама по себе оставалась недостаточной.`,
      social: `Посредник, скрытый контакт и несовместимые легенды выводят на один профиль. Имя: ${culprit.name}. Самый громкий конфликт при этом оказался отдельной, хотя и реальной, историей.`,
      provenance: `Порядок версий, подпись служебного редактора и исходные метаданные показывают, что документ прошёл через личный контур до заявленного времени создания. Связанный профиль: ${culprit.name}.`,
    }[discoveryRoute.id];
    const eventSpecs = [
      { id: "event-arrival", minute: incidentMinute - 72, text: `${victimName} отмечается у главного входа.` },
      { id: "event-call", minute: incidentMinute - 46, text: "Служебный телефон принимает короткий звонок без номера." },
      { id: "event-badge", minute: incidentMinute - 24, text: `Резервный пропуск группы «${accessGroup}» открывает служебную дверь.` },
      { id: "event-network", minute: incidentMinute - 11, text: `Узел ${alias} подключается к локальной сети.` },
      { id: "event-incident", minute: incidentMinute, text: `Экспертное окно: ${archetype.outcomeLabel}.` },
    ];
    const timelineOrder = eventSpecs.map((event) => event.id);
    const shuffledTimeline = shuffled(rng, eventSpecs.map((event) => event.id));
    const signatureDelay = 17 + Math.floor(rng() * 40);

    const puzzlePool = [
      {
        id: "cipher-caesar",
        kind: "cipher",
        title: "Сдвиг на полях",
        summary: "Классический шифр в личной записной книжке центрального участника.",
        prompt: "Расшифруйте строку и укажите фамилию человека, назначившего встречу.",
        body: encodeCaesar(`ВСТРЕЧА ${cipherSurname.toUpperCase()} В ${formatClock(incidentMinute - 30)}`, 3),
        answer: normalize(cipherSurname),
        accepted: [normalize(cipherSurname)],
        hint: "Это шифр Цезаря: каждая буква сдвинута на три позиции вперёд по русскому алфавиту без «Ё».",
        reveal: `Запись подтверждает встречу. Второй участник: ${cipherContact.name}. ${cipherDirect ? "Это звено основного маршрута, но содержание встречи ещё не установлено." : "Контакт реален, однако относится к отдельной конфликтной цепочке."}`,
        evidenceId: "ev-cipher",
      },
      {
        id: "stego-acrostic",
        kind: "stego",
        title: "Письмо без адресата",
        summary: "Физическая стеганография в безобидном тексте.",
        prompt: "Где спрятан носитель? Введите одно слово.",
        body: createAcrostic(cacheWord),
        answer: normalize(cacheWord),
        accepted: [normalize(cacheWord)],
        hint: "Посмотрите на первые буквы каждой строки, а не на смысл текста.",
        reveal: `Акростих указывает на место «${cacheWord}». Там найден резервный носитель.`,
        evidenceId: "ev-stego",
      },
      {
        id: "timeline-order",
        kind: "timeline",
        title: "Одиннадцать минут",
        summary: "Сведение независимых часов в единую хронологию.",
        prompt: "Расставьте события от самого раннего к самому позднему.",
        body: "Сервер входа спешит на 4 минуты; телефонная станция отстаёт на 3. Показанное ниже время уже нормализовано экспертом.",
        answer: timelineOrder.join("|"),
        initialOrder: shuffledTimeline,
        hint: "Опирайтесь на нормализованные отметки времени, а не на порядок документов в архиве.",
        reveal: `Между входом резервного пропуска и подключением узла ${alias} проходит 13 минут.`,
        evidenceId: "ev-timeline",
      },
      {
        id: "osint-profile",
        kind: "choice",
        title: "Легенда в открытой сети",
        summary: "OSINT-проверка цифровой легенды одного из участников.",
        prompt: "Какая деталь действительно опровергает заявленное алиби?",
        body: "Профиль изучен через открытые источники. Совпадение имени или манеры письма само по себе недостаточно.",
        choices: [
          "Два аккаунта используют одинаковые эмодзи и пунктуацию.",
          `Старый профиль подписан на страницу места «${scenario.location}».`,
          `Фото опубликовано в ${formatClock(incidentMinute - 16)}; исходный EXIF содержит координаты соседнего здания.`,
          "Никнейм состоит из английского слова и двух цифр.",
        ],
        answer: "2",
        accepted: ["2"],
        hint: "Ищите независимую временную и географическую метку, а не сходство стиля.",
        reveal: `Метаданные публикации помещают устройство рядом с местом события. Имя связанного профиля: ${osintContact.name}. ${osintDirect ? "Личность ещё требует подтверждения владения устройством." : "Полный маршрут показывает, что это отдельное бытовое пересечение."}`,
        evidenceId: "ev-osint",
      },
      {
        id: "weak-password",
        kind: "password",
        title: "Словарь вместо ключа",
        summary: "Атака на слабую защиту старого контейнера.",
        prompt: "Какой пароль открывает контейнер? Можно вывести его из цифровой легенды или запустить короткую словарную проверку.",
        body: `SHA-1 (устаревший): ${LEGACY_SHA1[password]}\nПодсказки владельца: любимая пластинка, старая частота, памятная полночь.`,
        answer: normalize(password),
        accepted: [normalize(password)],
        hint: `Кандидаты из утечки: ${shuffled(rng, passwordOptions).join(", ")}.`,
        reveal: `Контейнер открыт. В календаре есть закрытая встреча; второй участник — ${calendarContact.name}. ${calendarDirect ? "Запись относится к основному маршруту проверки." : "Встреча относится к параллельному нарушению."}`,
        evidenceId: "ev-password",
      },
      {
        id: "digital-stego",
        kind: "digital",
        title: "Пустая строка",
        summary: "Нулевые символы скрывают второй слой сообщения.",
        prompt: "Извлеките скрытые биты, декодируйте Base64 и введите английское слово.",
        body: `Проверено. Ничего важного.${hiddenBinary("QVJDSElWRQ==")}`,
        visibleBody: "Проверено. Ничего важного.",
        answer: "archive",
        accepted: ["archive", "архив"],
        hint: "Невидимые символы кодируют нули и единицы. Инструмент извлечения покажет промежуточную Base64-строку.",
        reveal: `Скрытый слой указывает на ARCHIVE. В архивной копии найден сетевой псевдоним ${alias}. ${digitalDirect ? "Его нужно связать с сеансом пользователя." : "Псевдоним принадлежит общему терминалу и создаёт тупиковую атрибуцию."}`,
        evidenceId: "ev-digital",
      },
      {
        id: "routine-gap",
        kind: "logic",
        title: "Окно в распорядке",
        summary: "Четыре бытовых отметки выглядят как алиби, но относятся к человеку и устройствам по-разному.",
        prompt: "Какая последовательность действительно оставляет проверяемое окно возможности?",
        body: `${routineContact.name}: ${routineContact.routine}; ${routineContact.habit}.\n${formatClock(incidentMinute - 41)} — транспортный терминал; ${formatClock(incidentMinute - 24)} — групповой пропуск; ${formatClock(incidentMinute - 11)} — локальное устройство; ${formatClock(incidentMinute + 14)} — личная покупка.`,
        choices: [
          "Считать все четыре отметки непосредственным наблюдением одного человека.",
          "Отбросить бытовые данные как заведомо не относящиеся к делу.",
          "Разделить носителя пропуска, владельца устройства и подтверждённое личное действие; проверить промежуток между ними.",
          "Выбрать самую близкую ко времени события отметку независимо от типа источника.",
        ],
        answer: "2", accepted: ["2"],
        hint: "Терминал, пропуск и устройство фиксируют разные сущности. Только часть событий можно надёжно привязать к человеку.",
        reveal: `Проверяемое окно возникает между групповым доступом и личной отметкой. Профиль отметки: ${routineContact.name}. Оно не видно при склейке всех устройств в одну линию.`,
        evidenceId: "ev-routine-gap",
      },
      {
        id: "ledger-chain",
        kind: "logic",
        title: "Сумма без получателя",
        summary: "Раздробленный платёж проходит через залог, возврат и общий адрес доставки.",
        prompt: "Какое звено нужно установить, прежде чем связывать платёж с мотивом?",
        body: `Платёж A → одноразовый токен → возврат залога → адрес ${financialContact.homeAddress}.\nТот же адрес встречается у ${1 + Math.floor(rng() * 3)} старых заказов организации.`,
        choices: ["Самую крупную сумму", "Конечного распорядителя токена и основание возврата", "Любого человека, когда-либо использовавшего адрес", "Авторство первого письма в цепочке"],
        answer: "1", accepted: ["1"],
        hint: "Адрес — связь, но не владение. Нужен контроль над токеном в момент операции.",
        reveal: `История токена связывает операцию с рабочим профилем. Имя профиля: ${financialContact.name}. Финансовый контакт всё равно не становится самостоятельным доказательством основного события.`,
        evidenceId: "ev-ledger-chain",
      },
      {
        id: "contact-graph",
        kind: "logic",
        title: "Третий участник",
        summary: "Два человека отрицают знакомство, но их связывает посредник и общее помещение.",
        prompt: "Какая проверка отличит прямую координацию от совпадения через посредника?",
        body: `${innocentSuspects[0].name} ↔ общий посредник ↔ ${socialContact.name}.\nОбщая геозона: ${scenario.district}. Прямых сообщений между крайними узлами нет.`,
        choices: ["Сходство словаря в любых сообщениях", "Наличие одного общего знакомого", "Независимая отметка одновременного присутствия и общий предмет обмена", "Сам факт отрицания знакомства"],
        answer: "2", accepted: ["2"],
        hint: "Социальный граф показывает возможность передачи, но не доказывает, что передача состоялась.",
        reveal: "Граф контактов сузился только после совмещения помещения, времени и предмета обмена; один общий знакомый давал ложноположительную связь.",
        evidenceId: "ev-contact-graph",
      },
      {
        id: "document-provenance",
        kind: "logic",
        title: "Автор, терминал, редактор",
        summary: "Документ имеет три разных признака авторства и две временные версии.",
        prompt: "Какой вывод допустим из метаданных общего терминала?",
        body: `Поле Author: ${provenanceContact.name}\nПрофиль редактора: ${alias}\nТерминал: общий / ${primaryOrganization.name}\nПодпись: добавлена после создания на ${signatureDelay} ${pluralRu(signatureDelay, "минуту", "минуты", "минут")}.`,
        choices: ["Поле Author окончательно устанавливает личность", "Псевдоним всегда равен владельцу устройства", "Документ прошёл через общий терминал; личность требует журнала сеанса или независимого действия", "Поздняя подпись делает весь документ поддельным"],
        answer: "2", accepted: ["2"],
        hint: "Метаданные описывают путь файла, а не обязательно человека за клавиатурой.",
        reveal: `Цепь хранения установлена до общего терминала ${primaryOrganization.name}; авторство подтверждается только пересечением с отдельным журналом сеанса. ${provenanceDirect ? "Это часть основного маршрута проверки." : "В этом деле документ относится к отдельной линии и не устанавливает основного исполнителя."}`,
        evidenceId: "ev-document-provenance",
      },
      {
        id: "spectral-control",
        kind: "lab",
        title: "Контрольный спектр",
        summary: "Три образца похожи по составу, но только один был взят из независимой контрольной серии.",
        prompt: "Какой вывод допустим после сравнения спектров?",
        body: `S-01: след с объекта / пик ${Math.floor(17 + rng() * 6)}.4\nS-02: контрольная серия / пик ${Math.floor(17 + rng() * 6)}.4\nS-03: образец из личного инструмента / пик ${Math.floor(17 + rng() * 6)}.1\n\nСовпадение пика может объяснять происхождение материала, но не владельца предмета.`,
        choices: [
          "Совпадение спектра окончательно устанавливает человека, державшего образец.",
          "Нужно сопоставить образец с контрольной серией и журналом выдачи, а личность устанавливать отдельным источником.",
          "Любое различие между сериями означает, что все материалы поддельны.",
          "Достаточно выбрать образец с самым редким пиком без проверки происхождения.",
        ],
        answer: "1",
        accepted: ["1"],
        hint: "Спектр отвечает на вопрос о материале и партии, а не о личности человека.",
        reveal: `Контрольная серия подтверждает путь материала, связанный с ${supportStaff[0].name}. Его заключение ограничено происхождением образца и не атрибутирует действие конкретному человеку.`,
        evidenceId: "ev-lab-spectrum",
      },
      {
        id: "custody-seal",
        kind: "lab",
        title: "Три смены хранения",
        summary: "Контейнер менял руки трижды; одна подпись внесена после смены, но пломба не повреждена.",
        prompt: "Как правильно описать разрыв в цепочке хранения?",
        body: `19:42 — приём: ${supportStaff[2].name}\n20:06 — передача в холодильный шкаф: сервисный сканер\n21:18 — подпись в журнале: внесена вручную\nПломба: цела; номер соответствует исходному фото.`,
        choices: [
          "Поздняя подпись автоматически делает образец недопустимым и бесполезным.",
          "Целая пломба делает ручную запись неважной; журнал можно игнорировать.",
          "Нужно сохранить оба факта: физическая целостность подтверждена, а время и автор ручной записи требуют отдельной проверки.",
          "Если у контейнера есть номер, никакие журналы больше не нужны.",
        ],
        answer: "2",
        accepted: ["2"],
        hint: "Цепь хранения состоит из нескольких независимых полей. Не заменяйте один источник другим.",
        reveal: "Контейнер не исключён: пломба и фотография подтверждают его целостность, но ручная отметка остаётся ограниченным источником времени.",
        evidenceId: "ev-lab-custody",
      },
      {
        id: "mail-header",
        kind: "lab",
        title: "Письмо, маршрут и автор",
        summary: "В заголовке сохранены отправляющий сервер, локальная переадресация и поле имени автора.",
        prompt: "Что можно установить из такого заголовка без дополнительной проверки?",
        body: `From: archive@${primaryOrganization.domain}\nReceived: shared-terminal / ${formatClock(incidentMinute - 33)}\nX-Forwarded-For: 10.4.7.${20 + culpritIndex}\nDisplay-Name: ${provenanceContact.name}\n\nПрофиль общего терминала меняется между сменами.`,
        choices: [
          "Отображаемое имя доказывает, кто отправил письмо.",
          "Письмо прошло через общий терминал в указанное время; автора нужно проверять по сеансу и независимому действию.",
          "IP-адрес сам по себе устанавливает физическое присутствие человека.",
          "Локальная переадресация делает весь текст выдуманным.",
        ],
        answer: "1",
        accepted: ["1"],
        hint: "Заголовок фиксирует путь сообщения, но общий терминал не равен одному пользователю.",
        reveal: `Маршрут письма подтверждён, а имя ${provenanceContact.name} остаётся версией до сопоставления с журналом сеансов.`,
        evidenceId: "ev-mail-header",
      },
      {
        id: "signal-triangulation",
        kind: "lab",
        title: "Шум на трёх антеннах",
        summary: "Три приёмника зафиксировали один сигнал с разной задержкой и разной точностью часов.",
        prompt: "Какой способ позволяет корректно сузить источник передачи?",
        body: `A-1: ${formatClock(incidentMinute - 28)}:14.220 / часы +4 с\nA-2: ${formatClock(incidentMinute - 28)}:08.220 / часы −2 с\nA-3: ${formatClock(incidentMinute - 28)}:11.220 / часы синхронизированы\n\nКаждая антенна даёт сектор, а не точку.`,
        choices: [
          "Выбрать самую громкую запись и считать её координатами источника.",
          "Нормализовать часы, пересечь направления трёх антенн и проверить, не отражён ли сигнал от металлоконструкций.",
          "Использовать только антенну с синхронизированными часами.",
          "Приписать сигнал человеку, чей позывной похож на строку лога.",
        ],
        answer: "1",
        accepted: ["1"],
        hint: "Одна антенна показывает направление. Место получается только после коррекции часов и пересечения независимых линий.",
        reveal: `Три сектора сходятся у ${scenario.district}, но отражение от инфраструктуры сохраняет полосу неопределённости. Это место проверки, не идентификация человека.`,
        evidenceId: "ev-lab-signal",
      },
    ];

    const firstConflict = conflictChains[0];
    const logicTrapPuzzle = firstConflict ? {
      id: "logic-dead-end",
      kind: "logic",
      title: "Доказательство или объяснение лжи",
      summary: `Цепочка «${firstConflict.title}» даёт мотив, ложь и скрытый контакт — но может быть замкнутым тупиком.`,
      prompt: "Какой вывод логически допустим после проверки всей цепочки?",
      body: `${firstConflict.hook}\n\nПоворот: ${firstConflict.turn}.\n\nКонтрольная граница: ${firstConflict.closure}.`,
      choices: [
        `Ложь автоматически доказывает причастность к событию «${archetype.outcomeLabel}».`,
        `Совпадение мотива и скрытого контакта достаточно, даже без связи со способом основного события.`,
        "Цепочка объясняет самостоятельный конфликт и ложь, но замыкается вне механизма и критического окна основного события.",
        "Любая цепочка с финансовым или личным конфликтом должна считаться основной.",
      ],
      answer: "2",
      accepted: ["2"],
      hint: "Проверьте четыре звена: возможность, средство, критическое время и независимое подтверждение личности.",
      reveal: `Конфликт реален. Участники: ${firstConflict.actorNames.join(" / ")}. Однако его цепочка не продолжается до события «${archetype.outcomeLabel}».`,
      evidenceId: "ev-dead-end-resolution",
    } : null;
    const causalPuzzle = {
      id: "causal-method",
      kind: "logic",
      title: "Причина, маскировка и случайность",
      summary: `В материалах одновременно присутствуют признаки версии «${crimePattern.apparent}», сознательная инсценировка и случайное обстоятельство.`,
      prompt: `Какое наблюдение отделяет реальный механизм «${archetype.mechanismLabel}» от маскировки и фонового совпадения?`,
      body: `Предварительная версия: ${crimePattern.apparent}.\nИнсценировка: ${crimePattern.staging}.\nСлучайное условие: ${crimePattern.incidental}.`,
      choices: [
        "Сам факт последующего сокрытия — он всегда точно указывает способ преступления.",
        `Совпадение с внешней версией «${crimePattern.apparent}» без проверки достаточности условий.`,
        crimePattern.trace,
        "Наиболее драматичный конфликт между участниками независимо от материальных следов.",
      ],
      answer: "2",
      accepted: ["2"],
      hint: "Нужен след, который физически несовместим с кажущейся причиной, а не просто подозрительное поведение после события.",
      reveal: `Отделены три слоя: реальный механизм — ${method}; маскировка — ${crimePattern.staging}; случайность — ${crimePattern.incidental}.`,
      evidenceId: "ev-causal-method",
    };

    const routePuzzles = discoveryRoute.puzzleIds.map((id) => puzzlePool.find((puzzle) => puzzle.id === id)).filter(Boolean);
    const scenarioPuzzles = (scenario.labPuzzleIds || []).map((id) => puzzlePool.find((puzzle) => puzzle.id === id)).filter(Boolean);
    const supplementary = shuffled(rng, puzzlePool.filter((puzzle) => !routePuzzles.includes(puzzle) && !scenarioPuzzles.includes(puzzle)));
    const uniquePuzzles = (items, limit) => [...new Map(items.filter(Boolean).map((puzzle) => [puzzle.id, puzzle])).values()].slice(0, limit);
    let selectedPuzzles = uniquePuzzles([...scenarioPuzzles, ...routePuzzles, ...supplementary], 3);
    if (difficultyKey === "tutorial") {
      const timelinePuzzle = puzzlePool.find((puzzle) => puzzle.kind === "timeline");
      const digitalPuzzle = puzzlePool.find((puzzle) => puzzle.kind === "digital");
      selectedPuzzles = uniquePuzzles([timelinePuzzle, digitalPuzzle, ...scenarioPuzzles, ...routePuzzles, ...supplementary], 3);
    }
    if (difficultyKey === "detective") selectedPuzzles = uniquePuzzles([...scenarioPuzzles, ...routePuzzles, logicTrapPuzzle, ...supplementary], 5);
    if (difficultyKey === "inspector") selectedPuzzles = uniquePuzzles([...scenarioPuzzles, ...routePuzzles, logicTrapPuzzle, causalPuzzle, ...supplementary], 6);
    const mailbox = buildMailbox(rng, {
      victim: { ...victimPerson, role: victimRole }, culprit, route: discoveryRoute, incidentMinute,
      organization: primaryOrganization, scenario, pattern: crimePattern, alias, supportStaff,
    });
    const evidence = [
      {
        id: "ev-scene",
        code: "M-01",
        title: "Первичный осмотр места",
        type: "forensics",
        excerpt: `Контур доступа не показывает грубого вторжения. Картина допускает версию «${crimePattern.apparent}», но не доказывает её.`,
        content: `Место: ${incidentAddress}.\nОкно события: ${formatClock(incidentMinute - 8)}–${formatClock(incidentMinute + 9)}.\n\nЗафиксированное наблюдение: ${sceneObservation}\nДополнительный след: ${crimePattern.trace}.\nПредварительная версия: ${crimePattern.apparent}.\nУсловие, способное исказить время и картину: ${crimePattern.incidental}.\n\nВывод ограничен: осмотр не позволяет сам по себе разделить механизм, маскировку и случайное обстоятельство.`,
        tags: ["осмотр", "форензика", "условный вывод"],
        reliability: "высокая для наблюдений; средняя для интерпретации",
        status: "версия не подтверждена",
        relevant: true,
      },
      {
        id: "ev-victim",
        code: "D-02",
        title: `Досье: ${victimName}`,
        type: "document",
        excerpt: `Профессия: ${victimRole}. За последние две недели ${victimRecentAction}.`,
        content: `${victimName}, ${38 + Math.floor(rng() * 24)} лет. Профессия: ${victimRole}. Место работы: ${primaryOrganization.name}.\n\nЗа двенадцать дней до события ${victimRecentAction}. ${pastTense(victimPerson, "Объяснил", "Объяснила")} это бытовой необходимостью, однако коллеги приводят две несовместимые причины. В тот же период ${pastTense(victimPerson, "запросил", "запросила")} доступ к источнику «${discoveryRoute.secondary}», не связанному напрямую с обычными обязанностями.\n\nОграничение вывода: изменение поведения устанавливает контекст и направление проверки, но не мотив и не причастность другого человека.`,
        tags: ["личность", "фон"],
        reliability: "смешанная: банковские операции точны, объяснения со слов коллег",
        status: "фон, требует связи",
        relevant: true,
      },
      {
        id: "ev-access",
        code: "L-07",
        title: "Журнал служебного входа",
        type: "log",
        excerpt: `Групповой пропуск использован в ${formatClock(incidentMinute - 24)}. В группе доступа — ${culprit.name} и ${innocentSuspects[0].name}.`,
        content: `${formatClock(incidentMinute - 72)}  MAIN-01  персональный пропуск центрального участника\n${formatClock(incidentMinute - 24)}  SRV-02   групповой пропуск / ${accessGroup}\n${formatClock(incidentMinute - 11)}  NET-04   новое устройство / ${alias}\n${formatClock(incidentMinute + 8)}  SRV-02   механическое открытие изнутри\n\nГруппа SRV-02: ${culprit.name}; ${innocentSuspects[0].name}; дежурный подрядчик. Журнал фиксирует право доступа, но не личность носителя карты.`,
        tags: ["время", "доступ"],
        reliability: "высокая для события доступа; личность носителя не установлена",
        status: "возможность доступа без атрибуции",
        relevant: true,
      },
      {
        id: "ev-witness",
        code: "W-03",
        title: witness.title,
        type: "interview",
        excerpt: witness.excerpt,
        content: `${witness.quote}.\n\nНадёжность: ${witness.reliability}. Показание нельзя использовать как самостоятельную идентификацию.`,
        tags: ["свидетель", "граница наблюдения"],
        reliability: witness.reliability,
        status: "наблюдение без надёжной атрибуции",
        relevant: true,
      },
      {
        id: "ev-cipher",
        code: "C-11",
        title: "Расшифрованная запись о встрече",
        type: "cipher",
        excerpt: `Фамилия ${cipherSurname} и время ${formatClock(incidentMinute - 30)}. Контакт не равен причастности.`,
        content: `После сдвига на три позиции получено:\n«ВСТРЕЧА ${cipherSurname.toUpperCase()} В ${formatClock(incidentMinute - 30)}».\n\nЗапись сделана рукой центрального участника; экспертиза чернил подтверждает дату. ${cipherDirect ? "Время пересекается с основным маршрутом проверки." : "Встреча объясняется отдельным конфликтом и не связана с установленным способом."}`,
        tags: ["крипто", "встреча"],
        relevant: cipherDirect,
        lockedBy: "cipher-caesar",
      },
      {
        id: "ev-stego",
        code: "S-14",
        title: "Носитель из тайника",
        type: "stego",
        excerpt: `Акростих указал на «${cacheWord}». Носитель был скрыт физически.`,
        content: `В месте «${cacheWord}» найден тонкий накопитель без маркировки. Корпус очищен, но в углублении осталась частица лака того же типа, что на месте события.\n\nСодержимое зашифровано отдельным слабым паролем.`,
        tags: ["стего", "носитель"],
        relevant: true,
        lockedBy: "stego-acrostic",
      },
      {
        id: "ev-timeline",
        code: "T-18",
        title: "Нормализованная хронология",
        type: "timeline",
        excerpt: "Пять независимых источников сведены к единому времени.",
        content: eventSpecs.map((event) => `${formatClock(event.minute)} — ${event.text}`).join("\n"),
        tags: ["время", "связь"],
        relevant: true,
        lockedBy: "timeline-order",
      },
      {
        id: "ev-osint",
        code: "O-21",
        title: "EXIF открытой публикации",
        type: "osint",
        excerpt: `Устройство было рядом с местом события; имя связанного профиля — ${osintContact.name}. Нахождение человека устанавливается отдельно.`,
        content: `Исходный файл получен из CDN до удаления публикации.\nCreated: ${formatClock(incidentMinute - 16)}\nGPS: соседнее здание, погрешность 19 м\nDevice fingerprint: совпадает с двумя ранними публикациями. Связанный профиль: ${osintContact.name}.\n\n${osintDirect ? "Устройство пересекается с другим независимым действием владельца." : "Поздняя транспортная отметка закрывает маршрут этого человека вне основного события."}`,
        tags: ["OSINT", "метаданные"],
        relevant: osintDirect,
        lockedBy: "osint-profile",
      },
      {
        id: "ev-password",
        code: "K-24",
        title: "Содержимое слабого контейнера",
        type: "crypto",
        excerpt: `Календарь показывает двух участников; второй указан как ${calendarContact.name}. Цель встречи не указана.`,
        content: `Контейнер защищён паролем «${password}», найденным словарным перебором.\nЗакрытая запись: «${formatClock(incidentMinute - 30)} — ${surname(calendarContact.name)}, документы и оригинал записи».\n\nСтойкость защиты признана недостаточной; целостность образа подтверждена контрольной суммой. ${calendarDirect ? "Время совпадает с другим независимым звеном." : "Переписка показывает отдельный предмет встречи."}`,
        tags: ["крипто", "календарь"],
        relevant: calendarDirect,
        lockedBy: "weak-password",
      },
      {
        id: "ev-digital",
        code: "S-29",
        title: "Скрытый слой сообщения",
        type: "stego",
        excerpt: `Нулевые символы привели к архивной копии с псевдонимом ${alias}.`,
        content: `Из последовательности U+200B / U+200C извлечено двоичное сообщение. После Base64-декодирования: ARCHIVE.\nАрхивная копия содержит строку авторизации узла ${alias}. Поле подписи повреждено; общий терминал использовали ${digitalContact.name} и ${innocentSuspects[digitalDirect ? 0 : 1 % innocentSuspects.length].name}.`,
        tags: ["цифровое стего", "сеть"],
        relevant: digitalDirect,
        lockedBy: "digital-stego",
      },
      {
        id: "ev-partner-cell",
        code: "A-31",
        title: "Сверка базовых станций",
        type: "forensics",
        excerpt: `Устройство из личного профиля было в секторе; имя профиля — ${cellContact.name}. Передача устройства не исключена.`,
        content: `За интервал ${formatClock(incidentMinute - 40)}–${formatClock(incidentMinute + 10)} устройство прошло handover между двумя станциями, покрывающими ${scenario.district}.\nВероятность нахождения устройства в заявленном месте алиби — менее 4%. Личная операция владельца возникает только после критического окна.`,
        tags: ["сотовая сеть", "алиби"],
        relevant: cellDirect,
        lockedBy: "task-cell",
      },
      {
        id: "ev-partner-money",
        code: "A-34",
        title: "Цепочка микроплатежей",
        type: "finance",
        excerpt: `Серия переводов связывает общий токен с личным адресом; имя адресного профиля — ${financialContact.name}. Распорядитель устанавливается отдельно.`,
        content: `Три платежа были раздроблены и проведены через одноразовые карты. Конечный получатель связан с адресом, который ${financialContact.name} ${pastTense(financialContact, "указывал", "указывала")} при старой доставке.\nНазначение не доказывает мотив автоматически, но опровергает заявление об отсутствии финансовых контактов.`,
        tags: ["финансы", "скрытая связь"],
        relevant: financialDirect,
        lockedBy: "task-money",
      },
      {
        id: "ev-partner-lab",
        code: "A-37",
        title: "Сверка контрольной серии",
        type: "lab",
        excerpt: `Контрольный образец подтверждает происхождение материала; ведущий специалист — ${supportStaff[0].name}. Личность исполнителя не устанавливается.`,
        content: `Серия: ${scenario.location}.\nСравнены след с объекта, контрольная партия и материал из общего комплекта. ${crimePattern.trace}.\n\nЗаключение: совпадение описывает путь материала и время его появления, но не доказывает, кто физически выполнил действие. Для атрибуции нужна отдельная связка доступа и времени.`,
        tags: ["лаборатория", "контрольная серия", "граница вывода"],
        reliability: "высокая для происхождения материала; нулевая для личности без второго источника",
        status: "материальная линия подготовлена",
        relevant: ["physical", "provenance"].includes(discoveryRoute.id),
        lockedBy: "task-lab",
      },
      {
        id: "ev-lab-spectrum",
        code: "LAB-12",
        title: "Спектральное сравнение контрольной серии",
        type: "lab",
        excerpt: "Прибор подтверждает происхождение материала, но не владельца предмета.",
        content: `Эксперт: ${supportStaff[0].name}.\nСлед на объекте сопоставлен с контрольной серией и журналом выдачи. Результат совместим с одним производственным контуром; предмет мог пройти через общий комплект.\n\nЭкспертный предел: совпадение состава не атрибутирует действие человеку.`,
        tags: ["спектр", "материал", "экспертиза"],
        reliability: "высокая для состава; не устанавливает человека",
        status: "происхождение материала уточнено",
        relevant: true,
        lockedBy: "spectral-control",
      },
      {
        id: "ev-lab-custody",
        code: "LAB-15",
        title: "Проверка пломбы и ручной отметки",
        type: "lab",
        excerpt: "Целостность контейнера подтверждена; ручная отметка времени остаётся ограниченным источником.",
        content: `Специалист: ${supportStaff[2].name}.\nНомер пломбы совпал с исходной фотографией. Ручная подпись появилась позже автоматической передачи, поэтому она не может без проверки заменить журнал сканера.\n\nВывод: физическая цепь сохранена; временная цепь требует дополнительного источника.`,
        tags: ["цепь хранения", "пломба", "время"],
        reliability: "высокая для целостности; средняя для времени ручной записи",
        status: "частичный разрыв описан",
        relevant: true,
        lockedBy: "custody-seal",
      },
      {
        id: "ev-mail-header",
        code: "LAB-18",
        title: "Разбор маршрута письма",
        type: "lab",
        excerpt: "Заголовок фиксирует общий терминал и время, но не автора сообщения.",
        content: `Аналитик: ${supportStaff[1].name}.\nПисьмо прошло через ${primaryOrganization.name} в критический период. Display-Name и сетевой адрес не идентифицируют человека без журнала сеанса и отдельного действия.\n\nВывод: маршрут подтверждён, авторство открыто для проверки.`,
        tags: ["почта", "метаданные", "атрибуция"],
        reliability: "высокая для маршрута; низкая для личности",
        status: "нужна корреляция с сеансом",
        relevant: true,
        lockedBy: "mail-header",
      },
      {
        id: "ev-lab-signal",
        code: "LAB-23",
        title: "Триангуляция архивного сигнала",
        type: "lab",
        excerpt: "Пересечение направлений сужает зону поиска, а не указывает на оператора передатчика.",
        content: `Коррекция часов трёх антенн оставляет сектор в районе «${scenario.district}». Отражения от металлоконструкций расширяют допустимую полосу.\n\nВывод: есть место для проверки, но не доказательство присутствия или личности.`,
        tags: ["сигнал", "триангуляция", "место"],
        reliability: "средняя; зависит от отражений",
        status: "сектор поиска установлен",
        relevant: true,
        lockedBy: "signal-triangulation",
      },
      {
        id: "ev-computer-mail",
        code: "PC-04",
        title: `Контекст письма: ${mailbox.clueSubject}`,
        type: "digital",
        excerpt: mailbox.clue.finding,
        content: `Отправитель: ${mailbox.clueSender}\nТема: ${mailbox.clueSubject}\n\n${mailbox.clue.finding}\n\nПредел вывода: одно письмо фиксирует административный или бытовой контекст. Оно не устанавливает личность причастного без второго источника из канала «${discoveryRoute.secondary}».`,
        tags: ["почта", "контекст", "косвенный источник"],
        reliability: "высокая для текста локальной копии; авторство и смысл требуют проверки",
        status: "косвенное звено, не обвинение",
        relevant: true,
        lockedBy: "computer-mail",
      },
      {
        id: "ev-computer-meta",
        code: "PC-09",
        title: "Метаданные резервной фотографии",
        type: "digital",
        excerpt: "Время редактирования не совпадает с легендой о выключенном устройстве.",
        content: `Артефакт канала ${discoveryRoute.id}\nCreated: ${formatClock(incidentMinute - 19)}\nModified: ${formatClock(incidentMinute - 14)}\nEditor account: ${alias}\nLocation cache: ${scenario.district}\n\nФайл был переименован, но блок метаданных сохранился. Профиль редактора использовался на общем терминале.`,
        tags: ["компьютер", "метаданные"],
        relevant: ["network", "provenance", "physical"].includes(discoveryRoute.id),
        lockedBy: "computer-meta",
      },
      {
        id: "ev-dead-end-resolution",
        code: "R-36",
        title: "Развилка: конфликт без причинного продолжения",
        type: "logic",
        excerpt: firstConflict ? `Цепочка «${firstConflict.title}» объясняет ложь двух участников, но замыкается вне преступления.` : "Логическая развилка не требуется на этом уровне.",
        content: firstConflict ? `Участники: ${firstConflict.actorNames.join(" / ")}.\n\nИсходная зацепка: ${firstConflict.hook}\nПоворот: ${firstConflict.turn}.\nЗамыкание: ${firstConflict.closure}.\n\nПредел вывода: конфликт и взаимная ложь подтверждены, однако нет звена, связывающего их со способом «${method}» и критическим окном. Исключить цепочку позволяет контрольная отметка ${firstConflict.excludedBy}.` : "Нет данных.",
        tags: ["логический тупик", "конфликт", "исключено"],
        reliability: "высокая после замыкания всей цепочки",
        status: `реальный конфликт; не причина события «${archetype.outcomeLabel}»`,
        relevant: false,
        lockedBy: "logic-dead-end",
      },
      {
        id: "ev-causal-method",
        code: "F-38",
        title: "Разделение причины, маскировки и случайности",
        type: "forensics",
        excerpt: `Физический след отделяет реальный механизм от версии «${crimePattern.apparent}».`,
        content: `Установленный механизм: ${method}.\nНаблюдение, которое его подтверждает: ${crimePattern.trace}.\n\nСознательная маскировка: ${crimePattern.staging}.\nСлучайное обстоятельство: ${crimePattern.incidental}.\nКажущаяся причина: ${crimePattern.apparent}.\n\nЭкспертный предел: вывод о механизме не устанавливает личность исполнителя; для атрибуции нужны доступ, время и независимый цифровой либо материальный след.`,
        tags: ["причинность", "маскировка", "экспертиза"],
        reliability: "высокая для механизма; личность не установлена",
        status: "причина установлена условно",
        relevant: true,
        lockedBy: "causal-method",
      },
      {
        id: "ev-routine-gap", code: "R-41", title: "Разделённая линия распорядка", type: "timeline",
        excerpt: `Пропуск, устройство и личная покупка больше не считаются одним непрерывным алиби. Профиль линии: ${routineContact.name}.`,
        content: `Дом: ${routineContact.homeAddress}.\nРабота: ${routineContact.workAddress}.\nОбычный распорядок: ${routineContact.routine}.\nБытовая привычка: ${routineContact.habit}.\n\nПосле разделения источников осталось окно, в котором групповая отметка не подтверждает личность пользователя. Контрольная личная операция произошла только в ${formatClock(incidentMinute + 14)}.`,
        tags: ["распорядок", "окно возможности", "условный вывод"], reliability: "высокая для отметок, средняя для атрибуции", status: "окно установлено; личность требует второго источника", relevant: routineDirect, lockedBy: "routine-gap",
      },
      {
        id: "ev-ledger-chain", code: "F-43", title: "Владение платёжным токеном", type: "finance",
        excerpt: "Общий адрес отделён от контроля над одноразовым токеном.",
        content: `Токен активирован на терминале ${primaryOrganization.name}.\nАдрес назначения: ${financialContact.homeAddress}.\nОснование: возврат залога по закрытому заказу.\n\nАдрес ранее использовался организацией, поэтому сам по себе не устанавливает получателя. Журнал сеанса связывает операцию с отдельным учётным окном. Имя профиля: ${financialContact.name}. Назначение платежа всё ещё допускает законное объяснение.`,
        tags: ["финансы", "владение", "граница вывода"], reliability: "высокая для токена; мотив не установлен", status: "финансовая связь подтверждена условно", relevant: financialDirect, lockedBy: "ledger-chain",
      },
      {
        id: "ev-contact-graph", code: "G-46", title: "Граф контактов после очистки", type: "social",
        excerpt: "Удалены случайные общие знакомые; осталась одна совместная точка во времени и месте.",
        content: `Крайние узлы: ${innocentSuspects[0].name} / ${socialContact.name}.\nОрганизация-посредник: ${primaryOrganization.name}.\nСовместная точка: ${scenario.district}, ${formatClock(incidentMinute - 31)}.\nПредмет обмена: контейнер с инвентарным номером, упомянутый в двух независимых источниках.\n\nОграничение: граф доказывает возможность прямого контакта, но содержание контакта восстанавливается отдельно.`,
        tags: ["социальный граф", "посредник", "контакт"], reliability: "средняя до материального подтверждения", status: "прямой контакт вероятен", relevant: socialDirect, lockedBy: "contact-graph",
      },
      {
        id: "ev-document-provenance", code: "P-49", title: "Цепь происхождения документа", type: "document",
        excerpt: "Поля автора, редактора и владельца сеанса разделены.",
        content: `Организация: ${primaryOrganization.name}.\nТерминал: общий, ${primaryOrganization.address}.\nПоле Author: ${provenanceContact.name}.\nПрофиль редактора: ${alias}.\nПодпись добавлена позже исходного сохранения.\n\nВывод: файл проходил через общий терминал. Личность редактора устанавливается не полем Author, а пересечением времени сеанса, физического доступа и независимого действия.`,
        tags: ["происхождение", "документ", "метаданные"], reliability: "высокая для цепи файла", status: "авторство требует корреляции", relevant: provenanceDirect, lockedBy: "document-provenance",
      },
    ];

    const conflictEvidence = conflictChains.flatMap((chain, index) => [
      {
        id: `${chain.id}-hook`, code: `CF-${index + 1}A`, title: `${chain.title}: исходная зацепка`, type: "conflict",
        excerpt: chain.hook,
        content: `${chain.hook}\n\nПредполагаемый мотив: ${chain.apparentMotive}. Оба участника сначала отрицали прямой контакт. Совпадают дата, место вторичного события и общий посредник. Это сильная связь между людьми, но пока не связь с механизмом «${archetype.mechanismLabel}».`,
        tags: ["конфликт", "скрытая связь", "неполная цепь"], reliability: "средняя", status: "подозрительное совпадение", relevant: false,
      },
      {
        id: `${chain.id}-turn`, code: `CF-${index + 1}B`, title: `${chain.title}: сложный поворот`, type: "conflict",
        excerpt: chain.turn,
        content: `${chain.turn}.\n\nОба участника продолжают лгать о причине встречи, потому что правдивое объяснение создаёт для них отдельные профессиональные и личные последствия. Ложь поэтому реальна, согласована и не обязательно относится к событию «${archetype.outcomeLabel}».`,
        tags: ["поворот", "ложь", "альтернативное объяснение"], reliability: "средняя; мотив лжи подтверждён частично", status: "конфликт переоценён", relevant: false,
      },
      {
        id: `${chain.id}-close`, code: `CF-${index + 1}C`, title: `${chain.title}: контрольная граница`, type: "conflict",
        excerpt: chain.closure,
        content: `${chain.closure}.\n\nКонтрольная отметка: ${chain.excludedBy}. Она получена из независимого источника и помещает ключевое действие цепочки вне критического окна. Цепочка объясняет социальные недоговорки, но не образует непрерывного пути «мотив → возможность → средство → результат».`,
        tags: ["проверка", "граница вывода", "тупик"], reliability: "высокая", status: "цепочка замкнута", relevant: false,
      },
    ]);

    const worldEvidence = suspects.flatMap((person, index) => [
      {
        id: `world-${person.id}-profile`, code: `CITY-${String(index + 1).padStart(2, "0")}A`, title: `Городская карточка: ${person.name}`, type: "document",
        excerpt: `${person.role}; ${person.employer}. Адреса и официальный распорядок без следственной интерпретации.`,
        content: `${person.name}\nПрофессия: ${person.role}\nОрганизация: ${person.employer}\nРабочий адрес: ${person.workAddress}\nДомашний адрес: ${person.homeAddress}\nОбычный распорядок: ${person.routine}\nБытовая особенность: ${person.habit}\n\nИсточники: кадровая карточка, городская адресная база, четыре недели обезличенных отметок. Данные описывают привычку, но не гарантируют поведение в день события.`,
        tags: ["городская база", "адрес", "распорядок"], reliability: "высокая для официальных полей, вероятностная для привычек", status: "контекст мира", relevant: false,
      },
      {
        id: `world-${person.id}-links`, code: `CITY-${String(index + 1).padStart(2, "0")}B`, title: `Связи и общие места: ${person.name}`, type: "social",
        excerpt: `${person.relationship}; известны общие адреса с другими участниками, но часть объясняется работой.`,
        content: `Рабочая связь: ${person.employer}.\nОбщее место: ${scenario.district}.\nСвязь с центральным участником: ${person.relationship}.\nЛичная причина скрытности: ${person.privateStake}.\n\nПримечание: совместное место или знакомый создают ребро графа, но не указывают направление влияния и содержание контакта.`,
        tags: ["социальный граф", "общее место", "легенда"], reliability: "средняя; собрана из разных периодов", status: "неинтерпретированная связь", relevant: false,
      },
    ]);

    const staffEvidence = supportStaff.map((person, index) => ({
      id: `staff-${person.id}-memo`,
      code: `LAB-M${index + 1}`,
      title: `Рабочая карточка: ${person.name}`,
      type: "lab",
      excerpt: `${person.role} · ${person.organization}. Профиль: ${person.specialty}.`,
      content: `${person.name}\nРоль: ${person.role}\nОрганизация: ${person.organization}\nСпециализация: ${person.specialty}.\n\nСлужебная запись: сотрудник подключён к проверке как независимый эксперт. Его вывод описывает только границы собственного метода и не подменяет решение по делу.`,
      tags: ["рабочая группа", "эксперт", "контекст"],
      reliability: "официальная кадровая карточка; не является доказательством по существу дела",
      status: "контакт рабочей группы",
      relevant: false,
    }));

    evidence.forEach((item) => {
      item.reliability ||= "требует сопоставления с независимым источником";
      item.status ||= item.relevant ? "проверяемое звено" : "контекст, не доказательство";
    });

    const noiseEvidence = Array.from({ length: difficulty.noise }, (_, index) => {
      const title = `${choose(rng, NOISE_TITLES)} №${String(index + 1).padStart(2, "0")}`;
      const line = choose(rng, NOISE_LINES);
      return {
        id: `noise-${index + 1}`,
        code: `X-${String(40 + index).padStart(2, "0")}`,
        title,
        type: choose(rng, ["document", "log", "finance", "interview"]),
        excerpt: line,
        content: `${title}\n\n${line}\nДата регистрации: ${String(2 + (index % 25)).padStart(2, "0")}.0${1 + (index % 8)}.2026\nОтветственный отдел: ${choose(rng, ["хозяйственный", "транспортный", "архивный", "технический"])}.`,
        tags: [choose(rng, ["проверено", "фон", "совпадение", "иной период"])],
        reliability: "формально достоверно, причинная связь не установлена",
        status: "совпадение не интерпретировано",
        relevant: false,
      };
    });

    const routeTask = {
      routine: ["Свести транспортные терминалы и личные покупки", "Отделить человека от перемещения его устройств"],
      financial: ["Развернуть историю одноразовых токенов", "Сверить возвраты, залоги и общие адреса"],
      physical: ["Сопоставить партии материала и журналы выдачи", "Проверить инструменты подрядчиков по микроследам"],
      network: ["Развести владельца профиля и носителя устройства", "Сверить локальные снимки и ключи авторизации"],
      social: ["Очистить граф контактов от случайных пересечений", "Найти одновременное присутствие крайних узлов"],
      provenance: ["Построить цепь версий документов", "Сопоставить сеансы общего терминала и подписи"],
    }[discoveryRoute.id];
    const tasks = [
      {
        id: "task-cell",
        title: routeTask[0],
        description: `${18 + Math.floor(rng() * 34)} тысяч строк и событий. Напарник нормализует источники, но не будет интерпретировать мотив.`,
        duration: difficulty.taskSeconds,
        evidenceId: "ev-partner-cell",
      },
      {
        id: "task-money",
        title: routeTask[1],
        description: `Рутинная обработка канала «${discoveryRoute.secondary}». Результат покажет границы вывода и альтернативные объяснения.`,
        duration: difficulty.taskSeconds + 12,
        evidenceId: "ev-partner-money",
      },
      {
        id: "task-lab",
        title: choose(rng, ["Проверить контрольную серию и микрослед", "Сверить пломбы с исходными фотографиями", "Нормализовать журналы приборов и ручные отметки"]),
        description: `${supportStaff[0].name}, ${supportStaff[0].role}, отделяет путь материала от личности исполнителя. Результат дополняет, но не заменяет вашу версию.`,
        duration: difficulty.taskSeconds + 24,
        evidenceId: "ev-partner-lab",
      },
    ];

    const routeFile = {
      routine: { name: "calendar_merge.ics", kind: "ICS", body: `Два часовых пояса, общий ресурс и личное приглашение. Последняя правка: ${formatClock(incidentMinute - 18)}.` },
      financial: { name: "token_returns.csv", kind: "CSV", body: `token,status,address_ref\nA17,returned,ADDR-${Math.floor(rng() * 80 + 10)}\nB04,closed,WORK-${primaryOrganization.id}\n\nПолный адрес вынесен в отдельный журнал владения.` },
      physical: { name: "material_issue.pdf", kind: "PDF", body: `Партия: ${Math.floor(rng() * 8000 + 1000)}-Q\nПолучатель: общий комплект\nСлед: ${crimePattern.trace}` },
      network: { name: "local_snapshot.db", kind: "DB", body: `profile=${alias}\ndevice_owner=conflict\nlast_sync=${formatClock(incidentMinute - 11)}\nsource=local-only` },
      social: { name: "room_booking.ics", kind: "ICS", body: `participants=2\ncups=3\nside_entrance=true\ntime=${formatClock(incidentMinute - 31)}` },
      provenance: { name: "version_manifest.json", kind: "JSON", body: `{ "author": "service_account", "editor": "${alias}", "terminal": "shared", "signedLater": true }` },
    }[discoveryRoute.id];
    const computer = {
      mail: mailbox.messages,
      files: [
        { id: "file-1", name: "intake_context.txt", size: "6 KB", kind: "TXT", body: `${archetype.label}\n${incidentAddress}\nОкно: ${formatClock(incidentMinute - 8)}–${formatClock(incidentMinute + 9)}\nМаршрут проверки: ${discoveryRoute.label}` },
        { id: "file-2", name: choose(rng, ["access_export.csv", "door_events.tsv", "shift_badges.log"]), size: `${12 + Math.floor(rng() * 40)} KB`, kind: "CSV", body: evidence.find((item) => item.id === "ev-access").content },
        { id: "file-3", name: routeFile.name, size: `${2 + Math.floor(rng() * 12)}.${Math.floor(rng() * 9)} MB`, kind: routeFile.kind, body: `${routeFile.body}\n\nВизуальный просмотр неполон. Метаданные сохранены.`, unlock: "computer-meta", metadata: `Created=${formatClock(incidentMinute - 19)}\nModified=${formatClock(incidentMinute - 14)}\nEditor=${alias}\nSource=${primaryOrganization.name}\nLocation-cache=${scenario.district}` },
        { id: "file-4", name: choose(rng, ["voice_fragment.wav", "corridor_audio.flac", "meeting_note.ogg"]), size: `${400 + Math.floor(rng() * 900)} KB`, kind: "AUDIO", body: `[00:03] фоновый шум\n[00:07] сигнал оборудования\n[00:11] неразборчивая фраза о боковом входе\n[00:14] запись обрывается. Голос не атрибутирован.` },
        { id: "file-5", name: choose(rng, [".cache_note", ".session_tail", "recover.part"]), size: "1 KB", kind: "SYS", body: `UTF-8 HEX:\n${textToHex(`node=${alias}; terminal=shared; role_group=multi_user`)}` },
        { id: "file-6", name: choose(rng, ["canteen_receipts.pdf", "weekly_roster.ods", "parking_notes.txt"]), size: `${8 + Math.floor(rng() * 80)} KB`, kind: "MISC", body: "Фоновый бытовой массив. Несколько временных отметок относятся к устройствам, а не к людям." },
        { id: "file-7", name: choose(rng, ["control_series.lab", "seal_photo_index.pdf", "receiver_alignment.csv"]), size: `${3 + Math.floor(rng() * 9)} MB`, kind: "LAB", body: `Рабочая группа: ${supportStaff.map((person) => person.name).join(" / ")}\nМетод: ${supportStaff[0].specialty}.\n\nМатериал готов к ручной проверке в лаборатории. Совпадение образцов, времени или маршрута не атрибутирует действие личности без независимого звена.` },
        { id: "file-8", name: choose(rng, ["archive_transfer_note.md", "field_call_sheet.txt", "review_questions.rtf"]), size: `${4 + Math.floor(rng() * 16)} KB`, kind: "NOTE", body: `Вопросы к следующей проверке:\n1. Кто контролировал носитель в критическое окно?\n2. Что подтверждает время независимо от ручной записи?\n3. Какая версия объясняет след «${crimePattern.trace}» без подмены личности совпадением?` },
      ],
      network: [
        `${formatClock(incidentMinute - 38)} AUTH  backup-service  OK`,
        `${formatClock(incidentMinute - 17)} DHCP  ${alias}  10.4.7.${20 + culpritIndex}`,
        `${formatClock(incidentMinute - 11)} ${choose(rng, ["SMB", "CALDAV", "SFTP", "SYNC"])}   ${alias}  /shared/${discoveryRoute.id}`,
        `${formatClock(incidentMinute - 9)}  AUTH  guest-tablet  FAIL`,
        `${formatClock(incidentMinute + 3)}  NET   camera-02  LINK_DOWN`,
      ],
    };

    return {
      seed,
      difficultyKey,
      difficulty,
      scenario,
      archetype,
      discoveryRoute,
      title: caseTitle,
      number: `ТО-${String(Math.floor(rng() * 999)).padStart(3, "0")}/${String(26 + Math.floor(rng() * 3))}`,
      victim: { ...victimPerson, role: victimRole },
      client: `${commissioner.name}, ${commissioner.role}`,
      commissioner,
      organizations,
      primaryOrganization,
      incidentAddress,
      mandateText,
      routeReconstruction,
      strongEvidenceIds: strongEvidenceByRoute[discoveryRoute.id],
      sector: choose(rng, scenario.sectors),
      circumstance: incidentSummary,
      incidentMinute,
      culpritId: culprit.id,
      motive,
      apparentMotive,
      method,
      apparentMethod: crimePattern.apparent,
      staging: crimePattern.staging,
      incidental: crimePattern.incidental,
      trace: crimePattern.trace,
      alias,
      suspects,
      supportStaff,
      conflictChains,
      events: eventSpecs,
      puzzles: selectedPuzzles,
      evidence: [...evidence, ...staffEvidence, ...worldEvidence, ...conflictEvidence, ...noiseEvidence],
      tasks,
      computer,
      motiveOptions: shuffled(rng, MOTIVES),
      methodOptions: shuffled(rng, [method, ...shuffled(rng, [...new Set(CASE_ARCHETYPES.flatMap((item) => item.patterns.map((pattern) => pattern.method)))].filter((item) => item !== method)).slice(0, 7)]),
    };
  }

  function createInitialState(data) {
    const firstEvidence = data.difficultyKey === "tutorial" ? [] : ["ev-scene", "ev-victim", "ev-access", "ev-witness"];
    const boardIds = [...data.suspects.map((person) => person.id), ...firstEvidence];
    const boardPositions = {};
    boardIds.forEach((id, index) => {
      const column = index % 4;
      const row = Math.floor(index / 4);
      boardPositions[id] = { x: 35 + column * 190, y: 38 + row * 185 };
    });
    const timelinePuzzle = data.puzzles.find((puzzle) => puzzle.kind === "timeline");
    return {
      view: "overview",
      startedAt: Date.now(),
      solvedPuzzles: [],
      hintsUsed: [],
      importantIds: [],
      boardIds,
      boardPositions,
      boardLinks: [],
      boardSelected: [],
      boardNotes: [],
      transcripts: {},
      askedQuestions: {},
      activeSuspect: data.suspects[0].id,
      tasks: {},
      timelineOrder: timelinePuzzle ? [...timelinePuzzle.initialOrder] : [],
      computer: {
        app: "mail",
        opened: "mail-1",
        finds: [],
        query: "",
        searchResults: [],
        toolOutput: "",
      },
      toolRuns: {},
      report: null,
      tutorial: data.difficultyKey === "tutorial" ? { step: 0, hidden: false, completed: false } : null,
      lastSavedAt: Date.now(),
    };
  }

  function getEvidence(id) {
    return caseData?.evidence.find((item) => item.id === id) || null;
  }

  function getSuspect(id) {
    return caseData?.suspects.find((person) => person.id === id) || null;
  }

  function isEvidenceUnlocked(item) {
    if (!item.lockedBy) return true;
    if (item.lockedBy.startsWith("task-")) return state.tasks[item.lockedBy]?.status === "done";
    if (item.lockedBy.startsWith("computer-")) return state.computer.finds.includes(item.lockedBy);
    return state.solvedPuzzles.includes(item.lockedBy);
  }

  function availableEvidence() {
    return caseData ? caseData.evidence.filter(isEvidenceUnlocked) : [];
  }

  function saveCase() {
    if (!caseData || !state) return;
    state.lastSavedAt = Date.now();
    localStorage.setItem(
      STORAGE.activeCase,
      JSON.stringify({ seed: caseData.seed, difficultyKey: caseData.difficultyKey, state }),
    );
  }

  function loadCase() {
    try {
      const saved = JSON.parse(localStorage.getItem(STORAGE.activeCase));
      if (!saved?.seed || !DIFFICULTIES[saved.difficultyKey] || !saved.state) return false;
      caseData = generateCase(saved.seed, saved.difficultyKey);
      state = { ...createInitialState(caseData), ...saved.state };
      state.computer = { ...createInitialState(caseData).computer, ...(saved.state.computer || {}) };
      state.boardLinks = (state.boardLinks || []).map(boardLink).filter((link) => link.from && link.to);
      state.boardNotes ||= [];
      if (saved.difficultyKey === "tutorial") state.tutorial ||= { step: 0, hidden: false, completed: false };
      updateCompletedTasks(false);
      return true;
    } catch {
      return false;
    }
  }

  function setStatus(message) {
    dom.status.textContent = message;
  }

  function toast(message, isError = false) {
    const element = document.createElement("div");
    element.className = `toast${isError ? " is-error" : ""}`;
    element.textContent = message;
    dom.toastRegion.append(element);
    window.setTimeout(() => element.remove(), 3600);
  }

  function showDialog(dialog) {
    if (!dialog.open) dialog.showModal();
  }

  function closeDialog(dialog) {
    if (dialog.open) dialog.close();
  }

  function updateChrome() {
    if (!caseData || !state) return;
    const solved = state.solvedPuzzles.length;
    const relevantUnlocked = availableEvidence().filter((item) => item.relevant).length;
    const asked = Object.values(state.askedQuestions).flat().length;
    const activeTask = Object.values(state.tasks).some((task) => task.status === "running");
    const unreadComputer = ["computer-mail", "computer-meta"].some((find) => !state.computer.finds.includes(find));
    const progress = Math.min(100, Math.round((solved / caseData.puzzles.length) * 65 + Math.min(15, asked * 2) + (relevantUnlocked / 14) * 20));

    dom.caseHeading.innerHTML = `
      <span class="eyebrow">${escapeHtml(caseData.number)} · ${escapeHtml(caseData.difficulty.label)}</span>
      <h1>${escapeHtml(caseData.title)}</h1>
    `;
    dom.sidebarBrief.innerHTML = `
      <span class="eyebrow">Готовность версии</span>
      <p><strong>${progress}%</strong> · решено ${solved} из ${caseData.puzzles.length} аналитических задач.</p>
      <div class="sidebar-progress"><i style="width:${progress}%"></i></div>
    `;
    dom.boardCount.textContent = String(state.boardIds.length);
    dom.archiveCount.textContent = String(availableEvidence().length);
    dom.puzzleCount.textContent = `${solved}/${caseData.puzzles.length}`;
    dom.taskIndicator.hidden = !activeTask;
    dom.computerIndicator.hidden = !unreadComputer;
    document.querySelectorAll(".nav-item").forEach((button) => {
      button.classList.toggle("is-active", button.dataset.view === state.view);
    });
  }

  function renderCurrentView() {
    if (!caseData || !state) return;
    const renderers = {
      overview: renderOverview,
      board: renderBoard,
      archive: renderArchive,
      computer: renderComputer,
      interviews: renderInterviews,
      lab: renderLab,
      analysis: renderAnalysis,
      report: renderReport,
    };
    updateChrome();
    renderers[state.view]?.();
    saveCase();
    renderTutorialUi();
  }

  function currentTutorialStep() {
    if (!state?.tutorial || state.tutorial.completed) return null;
    return TUTORIAL_STEPS[state.tutorial.step] || null;
  }

  function clearTutorialUi() {
    document.querySelectorAll("[data-tutorial-ui]").forEach((element) => element.remove());
    document.querySelectorAll(".tutorial-highlight").forEach((element) => element.classList.remove("tutorial-highlight"));
  }

  function renderTutorialUi() {
    clearTutorialUi();
    if (!state?.tutorial || state.tutorial.completed) return;

    const step = currentTutorialStep();
    if (!step) return;
    if (state.tutorial.hidden) {
      const resume = document.createElement("button");
      resume.type = "button";
      resume.className = "tutorial-resume";
      resume.dataset.action = "tutorial-resume";
      resume.dataset.tutorialUi = "true";
      resume.textContent = `Показать обучение · ${Math.min(state.tutorial.step + 1, TUTORIAL_STEPS.length)}/${TUTORIAL_STEPS.length}`;
      document.body.append(resume);
      return;
    }

    const target = document.querySelector(step.target);
    target?.classList.add("tutorial-highlight");
    const sticker = document.createElement("aside");
    sticker.className = "tutorial-sticker";
    sticker.dataset.tutorialUi = "true";
    sticker.setAttribute("aria-live", "polite");
    sticker.innerHTML = `
      <div class="tutorial-sticker-head"><span>ОБУЧЕНИЕ · ${state.tutorial.step + 1}/${TUTORIAL_STEPS.length}</span><button type="button" class="tutorial-close" data-action="tutorial-skip" aria-label="Закрыть этот шаг">×</button></div>
      <h2>${escapeHtml(step.title)}</h2>
      ${step.body.map((paragraph) => `<p>${escapeHtml(paragraph)}</p>`).join("")}
      <div class="tutorial-sticker-actions">
        <button type="button" class="button button-ghost button-small" data-action="tutorial-hide">Скрыть</button>
        <button type="button" class="button button-primary button-small" data-action="tutorial-continue">${escapeHtml(step.action || "Пропустить шаг")}</button>
      </div>
    `;
    if (dom.detailDialog.open) {
      sticker.classList.add("is-in-dialog");
      dom.detailContent.append(sticker);
    } else {
      document.body.append(sticker);
    }
  }

  function advanceTutorial() {
    const tutorial = state?.tutorial;
    const step = currentTutorialStep();
    if (!tutorial || !step) return;
    if (step.final) {
      tutorial.completed = true;
      saveCase();
      clearTutorialUi();
      toast("Обучение завершено. Новый режим можно выбрать при создании дела.");
      return;
    }

    tutorial.step += 1;
    const next = currentTutorialStep();
    saveCase();
    if (!next) return;
    if (dom.detailDialog.open && !dom.detailContent.querySelector(next.target)) closeDialog(dom.detailDialog);
    if (next.view !== state.view) {
      navigate(next.view);
      return;
    }
    renderTutorialUi();
  }

  function handleTutorialEvent(name) {
    if (currentTutorialStep()?.trigger === name) advanceTutorial();
  }

  function shouldAdvanceTutorialForNavigation(view) {
    const step = currentTutorialStep();
    const next = step ? TUTORIAL_STEPS[state.tutorial.step + 1] : null;
    return Boolean(
      step?.action
      && next
      && step.view === state.view
      && next.view === view
      && view !== state.view,
    );
  }

  function navigate(view) {
    if (!caseData || !VIEW_ORDER.includes(view)) return;
    if (shouldAdvanceTutorialForNavigation(view)) {
      advanceTutorial();
      return;
    }
    state.view = view;
    state.boardSelected = [];
    renderCurrentView();
    dom.workspace.scrollTop = 0;
    setStatus(`Открыт раздел: ${document.querySelector(`[data-view="${view}"] span:nth-child(2)`)?.textContent || view}`);
  }

  function renderOverview() {
    const solved = state.solvedPuzzles.length;
    const asked = Object.values(state.askedQuestions).flat().length;
    const tasksDone = Object.values(state.tasks).filter((task) => task.status === "done").length;
    const objectives = [
      { done: solved >= 1, text: "Получить первую проверяемую связь" },
      { done: asked >= 3, text: "Собрать показания для независимой проверки" },
      { done: tasksDone >= 1, text: "Делегировать массив рутинных данных" },
    ];
    dom.workspace.innerHTML = `
      <div class="view">
        <header class="view-header">
          <div>
            <span class="eyebrow">Оперативная сводка</span>
            <h2>${escapeHtml(caseData.title)}</h2>
            <p>${escapeHtml(caseData.circumstance)}</p>
          </div>
          <div class="tag-row">
            <span class="tag tag-accent">${escapeHtml(caseData.sector)}</span>
            <span class="tag">код ${escapeHtml(caseData.seed)}</span>
          </div>
        </header>

        <div class="case-lead">
          <article class="briefing-card">
            <span class="briefing-number">${escapeHtml(caseData.archetype.label)} / ${escapeHtml(caseData.number)}</span>
            <blockquote>«${escapeHtml(caseData.mandateText)}»</blockquote>
            <span class="briefing-sign">Поручение ${caseData.commissioner.gender === "female" ? "передала" : "передал"}: ${escapeHtml(caseData.commissioner.name)}, ${escapeHtml(caseData.commissioner.role)} · ${escapeHtml(caseData.commissioner.organization)}</span>
          </article>
          <div class="briefing-facts">
            <div class="fact-row"><span>${escapeHtml(caseData.archetype.targetLabel)}</span><strong>${escapeHtml(caseData.victim.name)}<br>${escapeHtml(caseData.victim.role)}</strong></div>
            <div class="fact-row"><span>Адрес</span><strong>${escapeHtml(caseData.incidentAddress)}</strong></div>
            <div class="fact-row"><span>Окно</span><strong>${formatClock(caseData.incidentMinute - 8)}–${formatClock(caseData.incidentMinute + 9)}</strong></div>
            <div class="fact-row"><span>Видимость</span><strong>${escapeHtml(caseData.apparentMethod)}</strong></div>
            <div class="fact-row"><span>Маршрут</span><strong>${escapeHtml(caseData.discoveryRoute.label)}</strong></div>
          </div>
        </div>

        <div class="stats-grid">
          <article class="stat-card"><span>Подозреваемые</span><strong>${caseData.suspects.length}</strong><small>легенды сформированы</small></article>
          <article class="stat-card"><span>Архив</span><strong>${availableEvidence().length}</strong><small>из ${caseData.evidence.length} записей доступно</small></article>
          <article class="stat-card"><span>Аналитика</span><strong>${solved}/${caseData.puzzles.length}</strong><small>задач решено</small></article>
          <article class="stat-card"><span>Допросы</span><strong>${asked}</strong><small>вопросов задано</small></article>
        </div>

        <section class="panel objective-panel">
          <span class="micro-label">Ближайшие ориентиры</span>
          <div class="objective-list">
            ${objectives.map((item, index) => `
              <div class="objective${item.done ? " is-done" : ""}">
                <i>${item.done ? "✓" : index + 1}</i><span>${escapeHtml(item.text)}</span>
              </div>`).join("")}
          </div>
        </section>
        <section class="panel world-panel">
          <span class="micro-label">Контекст поручения</span>
          <div class="world-grid">
            <div><span>Практический интерес</span><b>${escapeHtml(caseData.commissioner.interest)}</b></div>
            <div><span>Вероятное предубеждение</span><b>${escapeHtml(caseData.commissioner.bias)}</b></div>
            <div><span>Ограничение</span><b>${escapeHtml(caseData.commissioner.constraint)}</b></div>
            <div><span>Основной вопрос</span><b>${escapeHtml(caseData.archetype.objective)}</b></div>
          </div>
        </section>
        <section class="panel world-panel">
          <span class="micro-label">Рабочая группа и лаборатории</span>
          <div class="world-grid">
            ${caseData.supportStaff.map((person) => `<div><span>${escapeHtml(person.role)}</span><b>${escapeHtml(person.name)}</b><small>${escapeHtml(person.specialty)}</small></div>`).join("")}
          </div>
          <p class="fine-print">Эксперты расширяют архив и выполняют отдельные проверки. Их заключения описывают метод и границы данных, но не назначают виновного.</p>
        </section>
        ${caseData.conflictChains.length ? `<section class="panel condition-panel">
          <span class="micro-label">Особые развилки этого дела</span>
          <div class="condition-grid">
            <div><b>${caseData.conflictChains.length}</b><span>${caseData.conflictChains.length === 1 ? "реальная конфликтная цепочка может" : "реальные конфликтные цепочки могут"} объяснить ложь, не объясняя основное событие</span></div>
            <div><b>3</b><span>слоя нужно разделить: причина, сознательная маскировка и случайное обстоятельство</span></div>
            <div><b>1</b><span>кажущийся мотив «${escapeHtml(caseData.apparentMotive)}» конкурирует с фактическим</span></div>
          </div>
        </section>` : ""}
      </div>
    `;
  }

  function boardEntity(id) {
    const person = getSuspect(id);
    if (person) {
      return {
        id: person.id,
        title: person.name,
        subtitle: `${person.role} · ${person.relationship}`,
        readingText: `${person.role} · ${person.relationship}`,
        type: "подозреваемый",
        person: true,
      };
    }
    const note = state.boardNotes.find((item) => item.id === id);
    if (note) {
      return {
        id: note.id,
        title: note.title,
        subtitle: note.body,
        readingText: note.body,
        type: "рабочая версия",
        person: false,
        note: true,
      };
    }
    const evidence = getEvidence(id);
    if (!evidence) return null;
    return {
      id: evidence.id,
      title: evidence.title,
      subtitle: evidence.excerpt,
      readingText: evidence.content,
      type: evidence.tags[0] || evidence.type,
      important: state.importantIds.includes(evidence.id),
      person: false,
    };
  }

  function isBoardNote(id) {
    return state.boardNotes.some((item) => item.id === id);
  }

  function boardLink(link) {
    if (Array.isArray(link)) return { from: link[0], to: link[1], kind: "связь", note: "" };
    return {
      from: link?.from || "",
      to: link?.to || "",
      kind: link?.kind || "связь",
      note: link?.note || "",
    };
  }

  function renderBoard() {
    const entities = state.boardIds.map(boardEntity).filter(Boolean);
    const canDeleteSelectedNote = state.boardSelected.length === 1 && isBoardNote(state.boardSelected[0]);
    const selectionText = state.boardSelected.length === 0
      ? "Выберите две карточки, чтобы провести связь"
      : state.boardSelected.length === 1
        ? "Выберите вторую карточку"
        : "Связь готова к фиксации";
    dom.workspace.innerHTML = `
      <div class="view view-wide">
        <header class="view-header">
          <div><span class="eyebrow">Интерактивная борда</span><h2>Карта связей</h2><p>Кликните две карточки и сами определите характер связи. Удерживайте карточку ${appSettings.holdDuration / 1000} ${appSettings.holdDuration === 1000 ? "секунду" : "секунды"}, чтобы прочитать её целиком.</p></div>
          <div class="view-actions">
            <button class="button button-ghost button-small" type="button" data-action="open-board-note">Добавить мысль</button>
            <button class="button button-ghost button-small" type="button" data-action="delete-board-note" ${canDeleteSelectedNote ? "" : "disabled"}>Убрать мысль</button>
            <button class="button button-ghost button-small" type="button" data-action="clear-board-links" ${state.boardLinks.length ? "" : "disabled"}>Очистить связи</button>
            <button class="button button-primary button-small" type="button" data-action="connect-board" ${state.boardSelected.length === 2 ? "" : "disabled"}>Связать</button>
          </div>
        </header>
        <div class="board-toolbar">
          <p class="board-selection">${escapeHtml(selectionText)}</p>
          <div class="board-logic-controls">
            <label for="board-link-kind">Связь<select id="board-link-kind">${BOARD_LINK_TYPES.map((kind) => `<option value="${escapeHtml(kind)}">${escapeHtml(kind)}</option>`).join("")}</select></label>
            <label for="board-link-note">Проверка<input id="board-link-note" maxlength="80" placeholder="Почему вы так считаете?" autocomplete="off" /></label>
          </div>
          <p>${entities.length} ${pluralRu(entities.length, "карточка", "карточки", "карточек")} · ${state.boardLinks.length} ${pluralRu(state.boardLinks.length, "связь", "связи", "связей")}</p>
        </div>
        <div class="board" id="case-board">
          <svg class="board-lines" id="board-lines" aria-hidden="true"></svg>
          ${entities.map((entity) => {
            const position = state.boardPositions[entity.id] || { x: 30, y: 30 };
            const classes = ["board-node", entity.person ? "is-person" : "", entity.important ? "is-important" : "", entity.note ? "is-note" : "", state.boardSelected.includes(entity.id) ? "is-selected" : ""].filter(Boolean).join(" ");
            return `<button class="${classes}" type="button" data-board-id="${escapeHtml(entity.id)}" style="left:${position.x}px;top:${position.y}px">
              <span class="node-type">${escapeHtml(entity.type)}</span>
              <strong>${escapeHtml(entity.title)}</strong>
              <small>${escapeHtml(entity.subtitle)}</small>
              <span class="board-reading-body">${escapeHtml(entity.readingText)}</span>
            </button>`;
          }).join("")}
          ${entities.length ? "" : '<div class="empty-board-hint">Добавляйте материалы из архива</div>'}
        </div>
      </div>
    `;
    scheduleBoardLines();
    enableBoardDragging();
  }

  function scheduleBoardLines() {
    if (boardLineFrame) return;
    boardLineFrame = window.requestAnimationFrame(() => {
      boardLineFrame = 0;
      drawBoardLines();
    });
  }

  function drawBoardLines() {
    const board = document.querySelector("#case-board");
    const svg = document.querySelector("#board-lines");
    if (!board || !svg) return;
    const boardRect = board.getBoundingClientRect();
    svg.setAttribute("viewBox", `0 0 ${board.clientWidth} ${board.clientHeight}`);
    svg.innerHTML = state.boardLinks.map((savedLink) => {
      const link = boardLink(savedLink);
      const fromNode = board.querySelector(`[data-board-id="${CSS.escape(link.from)}"]`);
      const toNode = board.querySelector(`[data-board-id="${CSS.escape(link.to)}"]`);
      if (!fromNode || !toNode) return "";
      const first = fromNode.getBoundingClientRect();
      const second = toNode.getBoundingClientRect();
      const x1 = first.left - boardRect.left + first.width / 2;
      const y1 = first.top - boardRect.top + first.height / 2;
      const x2 = second.left - boardRect.left + second.width / 2;
      const y2 = second.top - boardRect.top + second.height / 2;
      const labelX = (x1 + x2) / 2;
      const labelY = (y1 + y2) / 2 - 5;
      const title = link.note ? `${link.kind}: ${link.note}` : link.kind;
      return `<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}"></line><circle cx="${x1}" cy="${y1}" r="2.2"></circle><circle cx="${x2}" cy="${y2}" r="2.2"></circle><text x="${labelX}" y="${labelY}"><title>${escapeHtml(title)}</title>${escapeHtml(link.kind)}</text>`;
    }).join("");
  }

  function enableBoardDragging() {
    const board = document.querySelector("#case-board");
    if (!board) return;
    board.querySelectorAll(".board-node").forEach((node) => {
      node.addEventListener("pointerdown", (event) => {
        if (event.button !== 0) return;
        const boardRect = board.getBoundingClientRect();
        const nodeRect = node.getBoundingClientRect();
        const offsetX = event.clientX - nodeRect.left;
        const offsetY = event.clientY - nodeRect.top;
        const startX = event.clientX;
        const startY = event.clientY;
        let moved = false;
        let dragging = false;
        let reading = false;
        node.setPointerCapture(event.pointerId);
        node.classList.add("is-reading-pending");

        const openForReading = () => {
          if (dragging || moved) return;
          const startRect = node.getBoundingClientRect();
          reading = true;
          node.classList.remove("is-reading-pending");
          node.classList.add("is-reading");
          board.classList.add("is-reading");
          if (!window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
            window.requestAnimationFrame(() => {
              const endRect = node.getBoundingClientRect();
              const shiftX = startRect.left - endRect.left;
              const shiftY = startRect.top - endRect.top;
              const scaleX = startRect.width / endRect.width;
              const scaleY = startRect.height / endRect.height;
              node.animate(
                [
                  { transform: `translate(calc(-50% + ${shiftX}px), calc(-50% + ${shiftY}px)) scale(${scaleX}, ${scaleY})`, opacity: 0.72 },
                  { transform: "translate(-50%, -50%) scale(1)", opacity: 1 },
                ],
                { duration: 460, easing: "cubic-bezier(0.22, 0.85, 0.26, 1)", fill: "none" },
              );
            });
          }
          suppressBoardClickUntil = performance.now() + 260;
        };
        const holdTimer = window.setTimeout(openForReading, appSettings.holdDuration);

        const cancelReadingHold = () => {
          window.clearTimeout(holdTimer);
          node.classList.remove("is-reading-pending");
        };

        const move = (moveEvent) => {
          if (reading) return;
          const movedFromStart = Math.abs(moveEvent.clientX - startX) > 6 || Math.abs(moveEvent.clientY - startY) > 6;
          if (!dragging && !movedFromStart) return;
          if (!dragging) {
            dragging = true;
            cancelReadingHold();
            node.classList.add("is-dragging");
          }
          const nextX = Math.max(5, Math.min(board.clientWidth - node.offsetWidth - 5, moveEvent.clientX - boardRect.left - offsetX));
          const nextY = Math.max(5, Math.min(board.clientHeight - node.offsetHeight - 5, moveEvent.clientY - boardRect.top - offsetY));
          moved ||= Math.abs(nextX - parseFloat(node.style.left)) > 2 || Math.abs(nextY - parseFloat(node.style.top)) > 2;
          node.style.left = `${nextX}px`;
          node.style.top = `${nextY}px`;
          state.boardPositions[node.dataset.boardId] = { x: Math.round(nextX), y: Math.round(nextY) };
          scheduleBoardLines();
        };

        const finish = () => {
          cancelReadingHold();
          if (reading) {
            node.classList.remove("is-reading");
            board.classList.remove("is-reading");
            suppressBoardClickUntil = performance.now() + 260;
          }
          node.classList.remove("is-dragging");
          node.removeEventListener("pointermove", move);
          node.removeEventListener("pointerup", finish);
          node.removeEventListener("pointercancel", finish);
          if (node.hasPointerCapture(event.pointerId)) node.releasePointerCapture(event.pointerId);
          if (moved) suppressBoardClickUntil = performance.now() + 180;
          if (moved) {
            scheduleBoardLines();
            saveCase();
          }
        };

        node.addEventListener("pointermove", move);
        node.addEventListener("pointerup", finish);
        node.addEventListener("pointercancel", finish);
      });
    });
  }

  function updateBoardSelectionUi() {
    const board = document.querySelector("#case-board");
    if (!board) return;
    board.querySelectorAll("[data-board-id]").forEach((node) => {
      node.classList.toggle("is-selected", state.boardSelected.includes(node.dataset.boardId));
    });
    const message = document.querySelector(".board-selection");
    if (message) {
      message.textContent = state.boardSelected.length === 0
        ? "Выберите две карточки, чтобы провести связь"
        : state.boardSelected.length === 1
          ? "Выберите вторую карточку"
          : "Связь готова к фиксации";
    }
    const connect = document.querySelector('[data-action="connect-board"]');
    if (connect) connect.disabled = state.boardSelected.length !== 2;
    const deleteNote = document.querySelector('[data-action="delete-board-note"]');
    if (deleteNote) deleteNote.disabled = state.boardSelected.length !== 1 || !isBoardNote(state.boardSelected[0]);
    saveCase();
  }

  function renderArchive() {
    const unlocked = availableEvidence();
    const query = normalize(archiveQuery);
    const filtered = unlocked.filter((item) => {
      const matchesQuery = !query || normalize(`${item.title} ${item.excerpt} ${item.content} ${item.tags.join(" ")} ${item.code}`).includes(query);
      const matchesFilter = archiveFilter === "all"
        || (archiveFilter === "important" && state.importantIds.includes(item.id))
        || (archiveFilter === "relevant" && item.relevant)
        || item.type === archiveFilter;
      return matchesQuery && matchesFilter;
    });
    dom.workspace.innerHTML = `
      <div class="view">
        <header class="view-header">
          <div><span class="eyebrow">Материалы дела</span><h2>Архив</h2><p>Поиск работает по содержимому, меткам и фрагментам. Большая часть совпадений может не иметь отношения к делу.</p></div>
        </header>
        <div class="archive-tools">
          <label class="search-field"><span class="sr-only">Поиск по архиву</span><input id="archive-search" type="search" autocomplete="off" placeholder="Имя, время, номер, фраза…" value="${escapeHtml(archiveQuery)}"></label>
          <div class="filter-group" role="group" aria-label="Фильтр архива">
            ${[
              ["all", "Все"], ["important", "Важное"], ["conflict", "Конфликты"], ["document", "Документы"], ["log", "Логи"], ["forensics", "Форензика"], ["lab", "Лаборатория"],
            ].map(([value, label]) => `<button class="filter-button${archiveFilter === value ? " is-active" : ""}" type="button" data-archive-filter="${value}">${label}</button>`).join("")}
          </div>
        </div>
        <div class="archive-meta"><span>Найдено: ${filtered.length}</span><span>Шум не помечается системой</span></div>
        <div class="archive-grid">
          ${filtered.length ? filtered.map((item) => `
            <article class="archive-card${state.importantIds.includes(item.id) ? " is-important" : ""}" tabindex="0" data-evidence-id="${item.id}">
              <div class="archive-card-head"><span class="file-code">${escapeHtml(item.code)} / ${escapeHtml(item.type)}</span>${state.importantIds.includes(item.id) ? '<span class="important-star" title="Отмечено важным">★</span>' : ""}</div>
              <h3>${escapeHtml(item.title)}</h3>
              <p>${escapeHtml(item.excerpt)}</p>
              ${item.status ? `<small class="evidence-status">Статус: ${escapeHtml(item.status)}</small>` : ""}
              <div class="tag-row">${item.tags.map((tag) => `<span class="tag">${escapeHtml(tag)}</span>`).join("")}</div>
            </article>`).join("") : '<div class="empty-results">Ничего не найдено. Попробуйте менее точный запрос.</div>'}
        </div>
      </div>
    `;
    const search = document.querySelector("#archive-search");
    search?.addEventListener("input", (event) => {
      archiveQuery = event.target.value;
      const selection = event.target.selectionStart;
      renderArchive();
      const next = document.querySelector("#archive-search");
      next?.focus();
      next?.setSelectionRange(selection, selection);
    });
  }

  function renderComputer() {
    const app = state.computer.app;
    const apps = [
      ["mail", "Почта", "@"],
      ["files", "Файлы", "▤"],
      ["search", "Поиск", "⌕"],
      ["network", "Сеть", "≋"],
      ["tools", "Инструменты", "◇"],
    ];
    dom.workspace.innerHTML = `
      <div class="view view-wide computer-view">
        <header class="view-header">
          <div><span class="eyebrow">АРМ следователя / изолированный контур</span><h2>Рабочий компьютер</h2><p>Локальные копии, образы и инструменты анализа. Система не подсказывает, какая находка важна.</p></div>
          <div class="computer-clock"><span id="computer-clock">${new Date().toLocaleTimeString("ru-RU", { hour: "2-digit", minute: "2-digit" })}</span><small>ТО-OS / READ ONLY</small></div>
        </header>
        <section class="computer-shell">
          <div class="computer-menubar"><span>ТО-OS</span><span>ДЕЛО ${escapeHtml(caseData.number)} · ОБРАЗ ПОДКЛЮЧЁН ТОЛЬКО ДЛЯ ЧТЕНИЯ</span><span>● ЗАЩИЩЕНО</span></div>
          <div class="computer-body">
            <nav class="computer-dock" aria-label="Программы компьютера">
              ${apps.map(([value, label, glyph]) => `<button type="button" class="computer-app-button${app === value ? " is-active" : ""}" data-computer-app="${value}" title="${label}"><i>${glyph}</i><span>${label}</span></button>`).join("")}
            </nav>
            <div class="computer-window">
              ${renderComputerApp(app)}
            </div>
          </div>
          <div class="computer-status"><span>CASE://LOCAL/${escapeHtml(caseData.seed)}</span><span>${state.computer.finds.length} ${pluralRu(state.computer.finds.length, "цифровая находка зафиксирована", "цифровые находки зафиксированы", "цифровых находок зафиксировано")}</span></div>
        </section>
      </div>
    `;
  }

  function renderComputerApp(app) {
    if (app === "mail") return renderMailApp();
    if (app === "files") return renderFilesApp();
    if (app === "search") return renderSearchApp();
    if (app === "network") return renderNetworkApp();
    return renderToolsApp();
  }

  function renderMailApp() {
    const selected = caseData.computer.mail.find((mail) => mail.id === state.computer.opened) || caseData.computer.mail[0];
    return `
      <div class="computer-app-head"><div><span class="micro-label">Локальная копия · несколько папок</span><h3>Почтовый ящик: ${escapeHtml(caseData.victim.name)}</h3></div><span>${caseData.computer.mail.length} сообщений</span></div>
      <div class="mail-layout">
        <div class="mail-list">
          ${caseData.computer.mail.map((mail) => `<button class="mail-item${selected.id === mail.id ? " is-active" : ""}${mail.important ? " is-suspicious" : ""}" type="button" data-mail-id="${mail.id}">
            <span><strong>${escapeHtml(mail.from)}</strong><time>${escapeHtml(mail.time)} · ${escapeHtml(mail.folder || "Входящие")}</time></span>
            <b>${escapeHtml(mail.subject)}</b>
            <small>${escapeHtml(mail.body.slice(0, 66))}</small>
          </button>`).join("")}
        </div>
        <article class="mail-reader">
          <div class="mail-reader-head"><span>От: <b>${escapeHtml(selected.from)}</b> · ${escapeHtml(selected.folder || "Входящие")}</span><time>${escapeHtml(selected.time)}</time></div>
          <h3>${escapeHtml(selected.subject)}</h3>
          <div class="mail-body">${escapeHtml(selected.body).replaceAll("\n", "<br>")}</div>
          ${selected.unlock ? `<button class="button button-primary button-small" type="button" data-action="fix-computer-find" data-find="${selected.unlock}">${state.computer.finds.includes(selected.unlock) ? "Добавлено в дело" : "Зафиксировать как улику"}</button>` : ""}
        </article>
      </div>
    `;
  }

  function renderFilesApp() {
    const selected = caseData.computer.files.find((file) => file.id === state.computer.opened);
    return `
      <div class="computer-app-head"><div><span class="micro-label">Образ QD-${escapeHtml(caseData.seed)}</span><h3>Файловая система</h3></div><span>${caseData.computer.files.length} объектов · скрытые показаны</span></div>
      <div class="files-layout">
        <div class="file-grid">
          ${caseData.computer.files.map((file) => `<button class="file-item${selected?.id === file.id ? " is-active" : ""}" type="button" data-file-id="${file.id}">
            <i>${escapeHtml(file.kind)}</i><span><b>${escapeHtml(file.name)}</b><small>${escapeHtml(file.size)}</small></span>
          </button>`).join("")}
        </div>
        <article class="file-preview">
          ${selected ? `
            <div class="file-preview-head"><span>${escapeHtml(selected.kind)} / ${escapeHtml(selected.size)}</span><b>${escapeHtml(selected.name)}</b></div>
            <pre>${escapeHtml(selected.body)}</pre>
            ${selected.metadata ? `<div class="view-actions"><button class="button button-ghost button-small" type="button" data-action="inspect-file-meta" data-file="${selected.id}">Прочитать метаданные</button>${selected.unlock ? `<button class="button button-primary button-small" type="button" data-action="fix-computer-find" data-find="${selected.unlock}">${state.computer.finds.includes(selected.unlock) ? "Добавлено в дело" : "Зафиксировать"}</button>` : ""}</div>` : ""}
            ${state.computer.toolOutput && state.computer.opened === "file-3" ? `<div class="terminal-output">${escapeHtml(state.computer.toolOutput).replaceAll("\n", "<br>")}</div>` : ""}
          ` : '<div class="computer-empty">Выберите файл для безопасного просмотра.</div>'}
        </article>
      </div>
    `;
  }

  function renderSearchApp() {
    const results = state.computer.searchResults || [];
    return `
      <div class="computer-app-head"><div><span class="micro-label">Локальный индекс</span><h3>Поиск по массиву</h3></div><span>${caseData.evidence.length + 18642} записей индексировано</span></div>
      <div class="database-search">
        <form class="computer-search-form" id="computer-search-form">
          <label class="sr-only" for="computer-query">Запрос</label>
          <input id="computer-query" type="search" autocomplete="off" placeholder="Имя, псевдоним, время, роль…" value="${escapeHtml(state.computer.query)}">
          <button class="button button-primary" type="submit">Искать</button>
        </form>
        <p class="query-help">Поиск буквальный. Попробуйте данные из писем, допросов или сетевого журнала.</p>
        <div class="database-results">
          ${results.length ? results.map((result) => `<button type="button" data-evidence-id="${result.id}" class="database-row"><span>${escapeHtml(result.code)}</span><b>${escapeHtml(result.title)}</b><small>${escapeHtml(result.excerpt)}</small></button>`).join("") : `<div class="computer-empty">${state.computer.query ? "Совпадений нет. Уточните форму запроса." : "Введите запрос. Автоматическая выдача приоритетов отключена."}</div>`}
        </div>
      </div>
    `;
  }

  function renderNetworkApp() {
    return `
      <div class="computer-app-head"><div><span class="micro-label">Восстановленный фрагмент</span><h3>Сетевой журнал</h3></div><span>UTC+3 · время нормализовано</span></div>
      <div class="network-terminal">
        <div class="terminal-title"><span>qd-netview --case ${escapeHtml(caseData.number)}</span><span>LIVE IMAGE / NO WRITE</span></div>
        <pre>${caseData.computer.network.map((line, index) => `<span class="${index === 1 || index === 2 ? "log-hit" : ""}">${escapeHtml(line)}</span>`).join("\n")}</pre>
        <div class="terminal-command">$ correlate --window ${formatClock(caseData.incidentMinute - 30)}..${formatClock(caseData.incidentMinute + 10)} <i>█</i></div>
      </div>
      <div class="computer-callout">Устройство может быть связано с человеком только через независимый идентификатор. Сам по себе псевдоним <b>${escapeHtml(caseData.alias)}</b> — ещё не личность.</div>
    `;
  }

  function renderToolsApp() {
    return `
      <div class="computer-app-head"><div><span class="micro-label">Безопасная лаборатория</span><h3>Инструменты</h3></div><span>локальное выполнение</span></div>
      <div class="tool-grid">
        <button type="button" class="computer-tool" data-computer-tool="hex"><i>HEX</i><span><b>Декодер HEX</b><small>Преобразовать байты UTF-8 из .cache_note</small></span></button>
        <button type="button" class="computer-tool" data-computer-tool="hash"><i>#</i><span><b>Сверка хешей</b><small>Проверить целостность образов без изменения</small></span></button>
        <button type="button" class="computer-tool" data-computer-tool="meta"><i>EXIF</i><span><b>Метаданные</b><small>Найти временные и географические хвосты</small></span></button>
        <button type="button" class="computer-tool" data-computer-tool="strings"><i>STR</i><span><b>Строки файла</b><small>Извлечь печатные фрагменты и нулевые символы</small></span></button>
      </div>
      ${state.computer.toolOutput ? `<div class="network-terminal compact"><div class="terminal-title"><span>Результат последней операции</span></div><pre>${escapeHtml(state.computer.toolOutput)}</pre></div>` : '<div class="computer-callout">Инструмент выполняет только техническую операцию. Интерпретировать результат должен следователь.</div>'}
    `;
  }

  function styleReply(person, topic, fact) {
    const variants = {
      controller: {
        where: `Уточню: вы спрашиваете о моём местонахождении или о местонахождении телефона? ${fact}`,
        relation: `«Отношения» — слишком широкое слово. Зафиксируйте: ${fact}`,
        detail: `Не называйте это мелочью, пока не проверили. ${fact}`,
        conflict: `Разделяйте конфликт и преступление. ${fact}`,
        method: `Я не эксперт и не стану подтверждать вашу формулировку. ${fact}`,
        pressure: `Минуты нельзя восстановить по памяти с точностью до протокола. ${fact}`,
      },
      performer: {
        where: `Ночь была такая, что каждый теперь сочинит себе идеальное алиби. Моё хотя бы можно проверить: ${fact}`,
        relation: `Вы хотите короткую версию, а короткой здесь нет. ${fact}`,
        detail: `Вот это я помню почти как кадр: ${fact}`,
        conflict: `Конечно, со стороны всё выглядит отвратительно. Но драма была совсем о другом: ${fact}`,
        method: `Вы говорите так, будто кто-то репетировал эту сцену заранее. ${fact}`,
        pressure: `Я помню вечер цельной сценой, а не таблицей. В этом и разница. ${fact}`,
      },
      anxious: {
        where: `Я уже называл${person.gender === "female" ? "а" : ""} время, но мог${person.gender === "female" ? "ла" : ""} округлить. Это ведь не считается сменой показаний? ${fact}`,
        relation: `Пожалуйста, запишите целиком, не только последнюю фразу. ${fact}`,
        detail: `Не уверен${person.gender === "female" ? "а" : ""}, что это важно… хотя теперь всё кажется важным. ${fact}`,
        conflict: `Я скрывал${person.gender === "female" ? "а" : ""} это, да. Но не из-за основного события. ${fact}`,
        method: `Я не знал${person.gender === "female" ? "а" : ""}, что именно вы нашли. ${fact}`,
        pressure: `Подождите, я хочу назвать это правильно, чтобы потом не исправлять. ${fact}`,
      },
      analytic: {
        where: `По пунктам. Первое — что помню лично. Второе — что подтверждается системой. ${fact}`,
        relation: `Факт контакта подтверждаю. Вашу трактовку причины — нет. ${fact}`,
        detail: `Степень уверенности около семидесяти процентов: ${fact}`,
        conflict: `Это объясняет ложь, но не причинность. ${fact}`,
        method: `Я могу говорить только о доступе и процедуре. ${fact}`,
        pressure: `Вот граница моей уверенности: маршрут помню, отдельные минуты — нет. ${fact}`,
      },
      adaptive: {
        where: `Понимаю, вам нужна точка на карте, а не впечатление. ${fact}`,
        relation: `Если говорить вашим языком — связь была, но её значение вы сейчас завышаете. ${fact}`,
        detail: `Вы правы, такие вещи иногда держат всю картину. ${fact}`,
        conflict: `Да, я бы на вашем месте тоже зацепил${person.gender === "female" ? "ась" : "ся"} за это. ${fact}`,
        method: `Давайте без догадок: я скажу, к чему имел${person.gender === "female" ? "а" : ""} доступ. ${fact}`,
        pressure: `Если вам нужен ориентир, дам ориентир. Если точная минута — её у меня нет. ${fact}`,
      },
      reserved: {
        where: `Я не буду заполнять пробелы догадками. ${fact}`,
        relation: `Связывал конфликт. Не дружба. ${fact}`,
        detail: `Запомнил${person.gender === "female" ? "а" : ""} одно. ${fact}`,
        conflict: `Солгал${person.gender === "female" ? "а" : ""}. Причина была другой. ${fact}`,
        method: `Не знаю. Доступ — был. Намерения — не было. ${fact}`,
        pressure: `Про время скажу только то, в чём не ошибусь. ${fact}`,
      },
    };
    return variants[person.personalityId]?.[topic] || fact;
  }

  function interviewObservation(person, topic) {
    const name = person.firstName;
    const observations = {
      controller: {
        where: `${name} на секунду задерживается, прежде чем отделить то, что помнит, от того, что может подтвердить.`,
        relation: `${name} поправляет одно слово в собственном ответе, но не меняет его смысл.`,
        detail: `${name} перечисляет детали ровным голосом, словно сверяет их с внутренним списком.`,
        method: `${name} сразу уводит разговор от предположений к процедуре.`,
        timeline: `${name} не спорит с временем, но очень точно выбирает, за что готов отвечать.`,
      },
      performer: {
        where: `${name} рисует картину вечера выразительнее, чем сам маршрут.`,
        relation: `${name} пытается придать разговору с ${caseData.victim.firstName} понятную драматургию.`,
        detail: `${name} охотно называет звуки и цвета, но делает паузу перед временем.`,
        method: `${name} отвечает с лёгкой усмешкой, будто примеряет вашу версию на себя.`,
        timeline: `${name} снова говорит «примерно», хотя раньше звучал${person.gender === "female" ? "а" : ""} увереннее.`,
      },
      anxious: {
        where: `${name} следит за вашей реакцией, будто ждёт, что вы поправите его${person.gender === "female" ? "" : ""}.`,
        relation: `${name} сжимает пальцы на краю стола, когда разговор заходит о ${caseData.victim.firstName}.`,
        detail: `${name} сначала отвергает деталь как неважную, затем возвращается к ней сам${person.gender === "female" ? "а" : ""}.`,
        method: `${name} отвечает тише и просит не делать выводов раньше времени.`,
        timeline: `${name} долго считает про себя, прежде чем назвать следующий ориентир.`,
      },
      analytic: {
        where: `${name} строит ответ как последовательность отдельных фактов, не связывая их причинно.`,
        relation: `${name} подтверждает факт контакта, но оставляет оценку его причины вам.`,
        detail: `${name} отмечает степень уверенности так же тщательно, как саму деталь.`,
        method: `${name} различает право доступа и фактическое действие.`,
        timeline: `${name} уточняет границы того, что можно восстановить по памяти.`,
      },
      adaptive: {
        where: `${name} подхватывает ваши слова о маршруте и возвращает их в собственной формулировке.`,
        relation: `${name} подбирает тон ответа под ваш вопрос, не становясь от этого конкретнее.`,
        detail: `${name} будто проверяет, какая деталь вызовет у вас больший интерес.`,
        method: `${name} быстро соглашается с формулировкой, но оставляет себе путь отступления.`,
        timeline: `${name} меняет темп речи, когда разговор снова касается часов.`,
      },
      reserved: {
        where: `${name} отвечает после долгой паузы и не пытается заполнить пробелы догадками.`,
        relation: `${name} смотрит в сторону, прежде чем произнести имя ${caseData.victim.firstName}.`,
        detail: `${name} повторяет одну деталь дословно, как будто боится исказить её.`,
        method: `${name} признаёт пределы своих знаний и не добавляет лишнего.`,
        timeline: `${name} молчит дольше обычного, прежде чем вернуться к времени.`,
      },
    };
    return observations[person.personalityId]?.[topic] || "";
  }

  function interviewQuestions(person) {
    const culprit = person.isCulprit;
    const asked = state.askedQuestions[person.id] || [];
    const relationFact = culprit
      ? "Нас связывали рабочие вопросы. Личная встреча в тот вечер заранее не обсуждалась."
      : `Между нами был отдельный контекст: ${person.relationship}. Последний разговор закончился резко, потому что я ${person.privateStake}.`;
    const accessFact = culprit
      ? "К служебной зоне доступ у меня был по должности. Но резервный пропуск не персональный, а устройство с псевдонимом ничего не говорит о том, кто держал его в руках."
      : `По работе я знал${person.gender === "female" ? "а" : ""} только обычную процедуру. О следе «${caseData.trace}» слышу сейчас впервые.`;
    const timelineFact = culprit
      ? `Точное время не назову: телефон мог остаться в машине, а часы я помню приблизительно. Одно могу сказать уверенно: в ${formatClock(caseData.incidentMinute - 24)} я не прикладывал${person.gender === "female" ? "а" : ""} резервный пропуск.`
      : `Я ${pastTense(person, "ошибся", "ошиблась")} на несколько минут. В тот вечер я ${person.privateStake}, поэтому сначала не хотел${person.gender === "female" ? "а" : ""} говорить обо всём. Независимая отметка закрывает мой разрыв до критического окна.`;
    const detailFact = `${sentenceStart(person.detail)}; я связываю это примерно с ${formatClock(caseData.incidentMinute - 20)}.`;
    return [
      {
        id: "where",
        question: "Расскажите о вашем вечере после смены.",
        answer: styleReply(person, "where", person.alibi),
        observation: interviewObservation(person, "where"),
      },
      {
        id: "relation",
        question: `Каким был ваш последний разговор с пропавшим — ${caseData.victim.name}?`,
        answer: styleReply(person, "relation", relationFact),
        observation: interviewObservation(person, "relation"),
      },
      {
        id: "detail",
        question: "Что в тот вечер показалось вам не на месте?",
        answer: styleReply(person, "detail", detailFact),
        observation: interviewObservation(person, "detail"),
      },
      {
        id: "method",
        question: "Как в ту смену был устроен доступ в служебную зону?",
        answer: styleReply(person, "method", accessFact),
        observation: interviewObservation(person, "method"),
      },
      {
        id: "timeline",
        question: "Вернёмся к времени: чем можно подтвердить ваш маршрут?",
        locked: !asked.includes("where"),
        answer: styleReply(person, "pressure", timelineFact),
        observation: interviewObservation(person, "timeline"),
      },
    ];
  }

  function renderInterviews() {
    const active = getSuspect(state.activeSuspect) || caseData.suspects[0];
    const transcript = state.transcripts[active.id] || [];
    const questions = interviewQuestions(active);
    const asked = state.askedQuestions[active.id] || [];
    dom.workspace.innerHTML = `
      <div class="view">
        <header class="view-header">
          <div><span class="eyebrow">Комната бесед</span><h2>Беседы</h2><p>Собеседники сообщают только то, что помнят или готовы сообщить. Сопоставляйте слова с независимыми фактами.</p></div>
        </header>
        <div class="interview-layout">
          <div class="suspect-list">
            ${caseData.suspects.map((person) => `<button class="suspect-button${person.id === active.id ? " is-active" : ""}" type="button" data-suspect-id="${person.id}">
              <i class="person-avatar">${initials(person.name)}</i><span><strong>${escapeHtml(person.name)}</strong><small>${escapeHtml(person.role)}</small></span>${(state.askedQuestions[person.id] || []).length ? '<span class="interview-seen">✓</span>' : ""}
            </button>`).join("")}
          </div>
          <section class="interview-room">
            <div class="interview-subject"><i class="person-avatar">${initials(active.name)}</i><div><h3>${escapeHtml(active.name)}</h3><p>${escapeHtml(active.role)} · ${escapeHtml(active.employer)}</p></div></div>
            <div class="transcript" id="transcript">
              ${transcript.length ? transcript.map((line) => `<div class="dialogue-line${line.speaker === "Детектив" ? " is-detective" : ""}${line.kind === "observation" ? " is-observation" : ""}"><b>${escapeHtml(line.speaker)}</b><p>${line.kind === "observation" ? `<em>${escapeHtml(line.text)}</em>` : escapeHtml(line.text)}</p></div>`).join("") : `<div class="dialogue-line"><b>${escapeHtml(active.name)}</b><p>${escapeHtml(active.opening)}</p></div>`}
            </div>
            <div class="question-panel"><span>Выберите тему. Вернуться к времени можно после рассказа о вечере.</span><div class="question-grid">
              ${questions.map((item) => `<button type="button" class="question-button${asked.includes(item.id) ? " is-asked" : ""}${item.locked ? " is-locked" : ""}" data-question-id="${item.id}" ${item.locked || asked.includes(item.id) ? "disabled" : ""}>${escapeHtml(item.question)}</button>`).join("")}
            </div></div>
          </section>
        </div>
      </div>
    `;
    const transcriptElement = document.querySelector("#transcript");
    if (transcriptElement) transcriptElement.scrollTop = transcriptElement.scrollHeight;
  }

  function renderLab() {
    const solved = state.solvedPuzzles.length;
    dom.workspace.innerHTML = `
      <div class="view">
        <header class="view-header">
          <div><span class="eyebrow">Ручная аналитика</span><h2>Лаборатория</h2><p>Разные слои дела требуют разных способов чтения. Ответы не чувствительны к регистру и пробелам.</p></div>
          <div class="lab-progress"><div class="progress-ring">${solved}/${caseData.puzzles.length}</div><span class="micro-label">решено<br>${caseData.difficulty.hint}</span></div>
        </header>
        <div class="puzzle-grid">
          ${caseData.puzzles.map((puzzle, index) => {
            const isSolved = state.solvedPuzzles.includes(puzzle.id);
            return `<article class="puzzle-card${isSolved ? " is-solved" : ""}">
              <span class="puzzle-index">${String(index + 1).padStart(2, "0")}</span>
              <span class="tag ${isSolved ? "tag-success" : "tag-accent"}">${escapeHtml(puzzle.kind)}</span>
              <h3>${escapeHtml(puzzle.title)}</h3>
              <p>${escapeHtml(isSolved ? puzzle.reveal : puzzle.summary)}</p>
              <div class="puzzle-footer">
                ${isSolved ? '<span class="solved-mark"><i>✓</i> вывод зафиксирован</span>' : `<span class="micro-label">улика закрыта</span><button class="button button-ghost button-small" type="button" data-puzzle-id="${puzzle.id}" data-puzzle-kind="${puzzle.kind}">Открыть</button>`}
              </div>
            </article>`;
          }).join("")}
        </div>
        <p class="lab-note">Подсказка отмечается в журнале и немного снижает итоговый рейтинг, но не блокирует идеальное логическое решение.</p>
      </div>
    `;
  }

  function renderPuzzleDetail(puzzle) {
    const hintUsed = state.hintsUsed.includes(puzzle.id);
    const toolOutput = state.toolRuns[puzzle.id] || "";
    let interaction = "";
    if (puzzle.kind === "timeline") {
      interaction = `
        <div class="timeline-sort">
          ${state.timelineOrder.map((eventId, index) => {
            const event = caseData.events.find((item) => item.id === eventId);
            return `<div class="timeline-item"><span>${formatClock(event.minute)}</span><span>${escapeHtml(event.text)}</span><div class="sort-controls"><button type="button" data-timeline-move="up" data-event-id="${eventId}" ${index === 0 ? "disabled" : ""} aria-label="Выше">↑</button><button type="button" data-timeline-move="down" data-event-id="${eventId}" ${index === state.timelineOrder.length - 1 ? "disabled" : ""} aria-label="Ниже">↓</button></div></div>`;
          }).join("")}
        </div>
        <button class="button button-primary" type="button" data-action="check-timeline" data-puzzle="${puzzle.id}">Проверить порядок</button>`;
    } else if (puzzle.choices) {
      interaction = `
        <form class="puzzle-choice-form" data-puzzle-form="${puzzle.id}">
          <div class="choice-list">${puzzle.choices.map((choice, index) => `<label><input type="radio" name="puzzle-answer" value="${index}"><span>${escapeHtml(choice)}</span></label>`).join("")}</div>
          <button class="button button-primary" type="submit">Зафиксировать вывод</button>
        </form>`;
    } else {
      const toolButton = puzzle.kind === "password"
        ? `<button class="button button-ghost button-small" type="button" data-action="run-dictionary" data-puzzle="${puzzle.id}">Словарная проверка</button>`
        : puzzle.kind === "digital"
          ? `<button class="button button-ghost button-small" type="button" data-action="extract-zero" data-puzzle="${puzzle.id}">Извлечь нулевые символы</button>`
          : "";
      interaction = `
        ${toolButton}
        ${toolOutput ? `<div class="tool-output">${escapeHtml(toolOutput).replaceAll("\n", "<br>")}</div>` : ""}
        <form class="answer-form" data-puzzle-form="${puzzle.id}">
          <label class="sr-only" for="answer-${puzzle.id}">Ответ</label>
          <input class="answer-input" id="answer-${puzzle.id}" name="puzzle-answer" autocomplete="off" placeholder="Ваш ответ">
          <button class="button button-primary" type="submit">Проверить</button>
        </form>`;
    }
    const displayBody = puzzle.visibleBody || puzzle.body;
    dom.detailContent.innerHTML = `
      <button class="modal-close" type="button" data-action="close-detail" aria-label="Закрыть">×</button>
      <div class="modal-kicker">${escapeHtml(puzzle.kind)} / аналитическая задача</div>
      <h2>${escapeHtml(puzzle.title)}</h2>
      <div class="document-sheet${["cipher", "password", "digital"].includes(puzzle.kind) ? " is-code" : ""}">${escapeHtml(displayBody)}</div>
      <div class="puzzle-prompt">${escapeHtml(puzzle.prompt)}</div>
      ${interaction}
      <div class="modal-actions">
        ${hintUsed ? "" : `<button class="button button-ghost" type="button" data-action="show-hint" data-puzzle="${puzzle.id}">Взять подсказку</button>`}
        <button class="button button-ghost" type="button" data-action="close-detail">Закрыть</button>
      </div>
      ${hintUsed ? `<div class="hint-box"><b>Подсказка:</b> ${escapeHtml(puzzle.hint)}</div>` : ""}
    `;
    showDialog(dom.detailDialog);
  }

  function taskStatus(task) {
    const saved = state.tasks[task.id];
    if (!saved) return { status: "idle", remaining: task.duration * 1000, percent: 0 };
    if (saved.status === "done" || saved.finishAt <= Date.now()) return { status: "done", remaining: 0, percent: 100 };
    const duration = task.duration * 1000;
    const remaining = saved.finishAt - Date.now();
    return { status: "running", remaining, percent: Math.max(0, Math.min(100, 100 - (remaining / duration) * 100)) };
  }

  function renderAnalysis() {
    const anyRunning = caseData.tasks.some((task) => taskStatus(task).status === "running");
    const doneCount = caseData.tasks.filter((task) => taskStatus(task).status === "done").length;
    dom.workspace.innerHTML = `
      <div class="view">
        <header class="view-header"><div><span class="eyebrow">Делегирование</span><h2>Напарник</h2><p>Передавайте массивы, где человеческий просмотр не добавляет смысла. Один поток обработки за раз.</p></div></header>
        <section class="panel partner-intro"><div class="partner-avatar">КР</div><div><span class="micro-label">Кира Руднева · аналитик</span><h3>«Рутину беру на себя. Вы ищите, где цифры не сходятся с человеком».</h3><p>Обработка идёт по реальному времени и сохраняется, даже если закрыть страницу. Пока задача выполняется, остальные разделы полностью доступны.</p></div></section>
        <div class="task-list">
          ${caseData.tasks.map((task, index) => {
            const info = taskStatus(task);
            let statusContent = `<button class="button button-primary button-small" type="button" data-task-id="${task.id}" ${anyRunning ? "disabled" : ""}>Передать · ${task.duration} с</button>`;
            if (info.status === "running") statusContent = `<span class="task-countdown" data-task-countdown="${task.id}">${formatDuration(info.remaining)}</span><small class="micro-label">обработка</small><div class="task-progress"><i data-task-progress="${task.id}" style="width:${info.percent}%"></i></div>`;
            if (info.status === "done") statusContent = `<span class="solved-mark"><i>✓</i> готово</span><br><button class="button button-ghost button-small" type="button" data-evidence-id="${task.evidenceId}">Открыть вывод</button>`;
            return `<article class="task-card"><i class="task-icon">${String(index + 1).padStart(2, "0")}</i><div><h3>${escapeHtml(task.title)}</h3><p>${escapeHtml(task.description)}</p></div><div class="task-status">${statusContent}</div></article>`;
          }).join("")}
        </div>
        ${doneCount ? `<blockquote class="partner-quote">«Готовые выборки уже в архиве. Помните: геопозиция устройства подтверждает маршрут устройства, не человека. Нужна вторая независимая связь».</blockquote>` : ""}
      </div>
    `;
  }

  function updateAnalysisProgress() {
    caseData.tasks.forEach((task) => {
      const info = taskStatus(task);
      if (info.status !== "running") return;
      const countdown = document.querySelector(`[data-task-countdown="${task.id}"]`);
      const progress = document.querySelector(`[data-task-progress="${task.id}"]`);
      if (countdown) countdown.textContent = formatDuration(info.remaining);
      if (progress) progress.style.width = `${info.percent}%`;
    });
  }

  function readinessScore() {
    const puzzlePart = (state.solvedPuzzles.length / caseData.puzzles.length) * 55;
    const asked = Object.values(state.askedQuestions).flat().length;
    const interviewPart = Math.min(20, asked * 2);
    const evidencePart = Math.min(25, state.importantIds.length * 4 + Object.values(state.tasks).filter((task) => task.status === "done").length * 5);
    return Math.round(Math.min(100, puzzlePart + interviewPart + evidencePart));
  }

  function renderReport() {
    if (state.report) {
      renderReportResult();
      return;
    }
    const relevantEvidence = availableEvidence().filter((item) => item.relevant || state.importantIds.includes(item.id));
    const readiness = readinessScore();
    const asked = Object.values(state.askedQuestions).flat().length;
    dom.workspace.innerHTML = `
      <div class="view">
        <header class="view-header"><div><span class="eyebrow">Итоговый протокол</span><h2>Версия</h2><p>Версия оценивается по ответственному лицу, мотиву, механизму события и качеству выбранной доказательной цепочки.</p></div></header>
        <div class="report-layout">
          <form class="report-form" id="report-form">
            <section class="report-section"><h3>01 · Основная версия</h3>
              <label for="report-suspect">Ответственное лицо</label><select id="report-suspect" name="suspect" required><option value="">Выберите…</option>${caseData.suspects.map((person) => `<option value="${person.id}">${escapeHtml(person.name)} · ${escapeHtml(person.role)}</option>`).join("")}</select>
              <label for="report-motive">Мотив</label><select id="report-motive" name="motive" required><option value="">Выберите…</option>${caseData.motiveOptions.map((motive) => `<option value="${escapeHtml(motive)}">${escapeHtml(motive)}</option>`).join("")}</select>
              <label for="report-method">${escapeHtml(caseData.archetype.mechanismLabel)}</label><select id="report-method" name="method" required><option value="">Выберите…</option>${caseData.methodOptions.map((method) => `<option value="${escapeHtml(method)}">${escapeHtml(method)}</option>`).join("")}</select>
            </section>
            <section class="report-section"><h3>02 · Доказательная цепочка</h3><p class="micro-label">Выберите от 3 до 5 материалов. Отмеченный вами конфликт тоже появится здесь — если вы считаете его частью причины.</p><div class="evidence-checks">
              ${relevantEvidence.map((item) => `<label class="evidence-check"><input type="checkbox" name="evidence" value="${item.id}"><span>${escapeHtml(item.code)} · ${escapeHtml(item.title)}</span></label>`).join("")}
            </div></section>
            <section class="report-section"><h3>03 · Логика</h3><label for="report-reasoning">Объясните причинную цепь и границы вывода</label><textarea id="report-reasoning" name="reasoning" minlength="60" required placeholder="Что опровергает алиби? Как отделены причина, маскировка и случайное обстоятельство? Почему социальный конфликт не является достаточным доказательством?"></textarea></section>
            <button class="button button-primary" type="submit">Подписать и передать версию</button>
          </form>
          <aside class="panel case-readiness"><span class="eyebrow">Оценка готовности</span><h3>${readiness}% собрано</h3><div class="readiness-meter"><i style="width:${readiness}%"></i></div><ul class="readiness-list">
            <li><span>Задачи</span><strong>${state.solvedPuzzles.length}/${caseData.puzzles.length}</strong></li>
            <li><span>Беседы</span><strong>${asked}</strong></li>
            <li><span>Важные материалы</span><strong>${state.importantIds.length}</strong></li>
            <li><span>Подсказки</span><strong>${state.hintsUsed.length}</strong></li>
          </ul><p class="fine-print">Высокая готовность не гарантирует верную версию. Это мера полноты работы, а не истинности вывода.</p></aside>
        </div>
      </div>
    `;
  }

  function evaluateReport(formData) {
    const suspect = formData.get("suspect");
    const motive = formData.get("motive");
    const method = formData.get("method");
    const evidenceIds = formData.getAll("evidence");
    const reasoning = String(formData.get("reasoning") || "");
    const correctSuspect = suspect === caseData.culpritId;
    const correctMotive = motive === caseData.motive;
    const correctMethod = method === caseData.method;
    const strongIds = new Set(caseData.strongEvidenceIds);
    const strongEvidence = evidenceIds.filter((id) => strongIds.has(id)).length;
    const falseEvidence = evidenceIds.filter((id) => getEvidence(id)?.relevant === false).length;
    const chainBase = Math.max(0, Math.min(18, strongEvidence * 6) - Math.max(0, evidenceIds.length - 5) * 2);
    const falseEvidencePenalty = Math.min(chainBase, falseEvidence * 5);
    const chainScore = chainBase - falseEvidencePenalty;
    const workScore = Math.round((state.solvedPuzzles.length / caseData.puzzles.length) * 9);
    const reasoningScore = reasoning.trim().length >= 80 ? 8 : reasoning.trim().length >= 30 ? 5 : 0;
    const hintPenalty = Math.max(0, state.hintsUsed.length - 1) * 2;
    const score = Math.max(0, Math.min(100,
      (correctSuspect ? 37 : 0)
      + (correctMotive ? 14 : 0)
      + (correctMethod ? 14 : 0)
      + chainScore
      + workScore
      + reasoningScore
      - hintPenalty,
    ));
    return { suspect, motive, method, evidenceIds, reasoning, correctSuspect, correctMotive, correctMethod, falseEvidence, falseEvidencePenalty, chainBase, chainScore, workScore, reasoningScore, hintPenalty, score };
  }

  function renderReportResult() {
    const result = state.report;
    const culprit = getSuspect(caseData.culpritId);
    const verdict = result.score >= 80 ? "Версия выдерживает проверку" : result.score >= 58 ? "Версия требует уточнения" : "Цепочка не доказана";
    const description = result.score >= 80
      ? "Вы связали человека, время и цифровой след независимыми источниками. Материалы можно передавать дальше."
      : "В версии остаются разрывы. Дело можно вернуть на стол, проверить алиби и заменить слабые материалы.";
    dom.workspace.innerHTML = `
      <div class="view">
        <header class="view-header"><div><span class="eyebrow">Результат расследования</span><h2>${escapeHtml(caseData.title)}</h2></div></header>
        <article class="result-card">
          <span class="eyebrow">Оценка протокола</span><div class="result-score">${result.score}<small>/100</small></div><h3>${verdict}</h3><p>${description}</p>
          <div class="result-breakdown">
            <span>Ответственное лицо</span><strong>${result.correctSuspect ? "+37" : "+0"}</strong>
            <span>Мотив</span><strong>${result.correctMotive ? "+14" : "+0"}</strong>
            <span>${escapeHtml(caseData.archetype.mechanismLabel)}</span><strong>${result.correctMethod ? "+14" : "+0"}</strong>
            <span>Основа доказательной цепочки</span><strong>+${result.chainBase ?? result.chainScore}</strong>
            <span>Тупиковые материалы</span><strong>−${result.falseEvidencePenalty || 0}</strong>
            <span>Аналитическая работа</span><strong>+${result.workScore + result.reasoningScore}</strong>
            <span>Цена подсказок</span><strong>−${result.hintPenalty}</strong>
          </div>
          <div class="document-sheet"><b>Фактическая реконструкция</b>\n\nОтветственное лицо: ${escapeHtml(culprit.name)} (${escapeHtml(culprit.role)}).\nФактический мотив: ${escapeHtml(caseData.motive)}.\nМотив, созданный внешней картиной: ${escapeHtml(caseData.apparentMotive)}.\nРеальный механизм: ${escapeHtml(caseData.method)}.\nКажущаяся причина: ${escapeHtml(caseData.apparentMethod)}.\nИнсценировка: ${escapeHtml(caseData.staging)}.\nСлучайное условие: ${escapeHtml(caseData.incidental)}.\n\nКлючевой разрыв легенды: ${escapeHtml(caseData.routeReconstruction)} Социальные конфликты объясняют часть лжи, но не подменяют причинную цепь. Ни один поведенческий сигнал не использован как самостоятельное доказательство.</div>
          <div class="view-actions"><button class="button button-ghost" type="button" data-action="reopen-case">Вернуть дело на стол</button><button class="button button-primary" type="button" data-action="open-new-case">Новое дело</button></div>
        </article>
      </div>
    `;
  }

  function openEvidence(id) {
    const item = getEvidence(id);
    if (!item || !isEvidenceUnlocked(item)) return;
    const isImportant = state.importantIds.includes(item.id);
    const onBoard = state.boardIds.includes(item.id);
    dom.detailContent.innerHTML = `
      <button class="modal-close" type="button" data-action="close-detail" aria-label="Закрыть">×</button>
      <div class="modal-kicker">${escapeHtml(item.code)} / ${escapeHtml(item.type)}</div>
      <h2>${escapeHtml(item.title)}</h2>
      <div class="detail-meta">${item.tags.map((tag) => `<span class="tag">${escapeHtml(tag)}</span>`).join("")}</div>
      ${(item.status || item.reliability) ? `<div class="evidence-provenance"><div><span>Статус вывода</span><b>${escapeHtml(item.status || "не классифицирован")}</b></div><div><span>Надёжность</span><b>${escapeHtml(item.reliability || "зависит от источника")}</b></div></div>` : ""}
      <div class="document-sheet${["log", "digital", "crypto"].includes(item.type) ? " is-code" : ""}">${escapeHtml(item.content)}</div>
      <div class="modal-actions">
        <button class="button button-ghost" type="button" data-action="toggle-important" data-evidence="${item.id}">${isImportant ? "Снять отметку" : "Отметить важным"}</button>
        <button class="button button-primary" type="button" data-action="add-to-board" data-evidence="${item.id}" ${onBoard ? "disabled" : ""}>${onBoard ? "На доске" : "На доску"}</button>
      </div>
    `;
    showDialog(dom.detailDialog);
  }

  function openBoardNoteDialog() {
    dom.detailContent.innerHTML = `
      <button class="modal-close" type="button" data-action="close-detail" aria-label="Закрыть">×</button>
      <div class="modal-kicker">Ручная заметка</div>
      <h2>Рабочая версия</h2>
      <p>Это не доказательство и не готовый вывод. Сформулируйте проверяемую мысль, а затем свяжите её с материалами на доске.</p>
      <form id="board-note-form" class="board-note-form">
        <label class="field-label" for="board-note-title">Короткий заголовок</label>
        <input class="field-input" id="board-note-title" name="title" maxlength="70" required placeholder="Например, доступ не равен присутствию" autocomplete="off" />
        <label class="field-label" for="board-note-body">Основание для проверки</label>
        <textarea id="board-note-body" name="body" maxlength="600" required placeholder="Какие факты поддерживают версию, а что ещё нужно проверить?"></textarea>
        <div class="modal-actions"><button class="button button-primary" type="submit">Поместить на доску</button></div>
      </form>
    `;
    showDialog(dom.detailDialog);
  }

  function solvePuzzle(puzzleId, answer) {
    const puzzle = caseData.puzzles.find((item) => item.id === puzzleId);
    if (!puzzle || state.solvedPuzzles.includes(puzzleId)) return;
    const normalizedAnswer = normalize(answer);
    const isCorrect = (puzzle.accepted || [puzzle.answer]).some((value) => normalize(value) === normalizedAnswer);
    if (!isCorrect) {
      toast("Ответ не сходится с данными. Проверьте метод и попробуйте снова.", true);
      setStatus("Аналитическая гипотеза отклонена");
      return;
    }
    state.solvedPuzzles.push(puzzleId);
    if (!state.importantIds.includes(puzzle.evidenceId)) state.importantIds.push(puzzle.evidenceId);
    saveCase();
    closeDialog(dom.detailDialog);
    toast(`Вывод зафиксирован: ${puzzle.reveal}`);
    setStatus("Новая улика разблокирована в архиве");
    renderCurrentView();
  }

  function updateCompletedTasks(notify = true) {
    if (!caseData || !state) return false;
    let changed = false;
    caseData.tasks.forEach((task) => {
      const saved = state.tasks[task.id];
      if (saved?.status === "running" && saved.finishAt <= Date.now()) {
        saved.status = "done";
        changed = true;
        if (!state.importantIds.includes(task.evidenceId)) state.importantIds.push(task.evidenceId);
        if (notify) toast(`Кира закончила: «${task.title}». Вывод добавлен в архив.`);
      }
    });
    if (changed) saveCase();
    return changed;
  }

  function askQuestion(questionId) {
    const person = getSuspect(state.activeSuspect);
    if (!person) return;
    const question = interviewQuestions(person).find((item) => item.id === questionId);
    if (!question || question.locked) return;
    state.askedQuestions[person.id] ||= [];
    state.transcripts[person.id] ||= [];
    if (state.askedQuestions[person.id].includes(questionId)) return;
    state.askedQuestions[person.id].push(questionId);
    state.transcripts[person.id].push({ speaker: "Детектив", text: question.question });
    if (question.observation) state.transcripts[person.id].push({ speaker: "Наблюдение", text: `Вы замечаете, как ${question.observation}`, kind: "observation" });
    state.transcripts[person.id].push({ speaker: person.name, text: question.answer });
    saveCase();
    renderInterviews();
  }

  function fixComputerFind(findId) {
    if (!state.computer.finds.includes(findId)) {
      state.computer.finds.push(findId);
      const evidence = caseData.evidence.find((item) => item.lockedBy === findId);
      if (evidence && !state.importantIds.includes(evidence.id)) state.importantIds.push(evidence.id);
      toast("Цифровая находка зафиксирована и добавлена в архив.");
      saveCase();
      updateChrome();
    }
    renderComputer();
  }

  function runComputerTool(tool) {
    const routeArtifact = caseData.computer.files.find((file) => file.id === "file-3");
    const outputs = {
      hex: `HEX → UTF-8\nnode=${caseData.alias}; terminal=shared; role_group=multi_user\nСтатус: строка восстановлена, личность пользователя не атрибутирована`,
      hash: "SHA-256 images: MATCH\nSHA-256 archive: MATCH\nЦелостность копий подтверждена.",
      meta: `Выберите ${routeArtifact?.name || "маршрутный артефакт"} в разделе «Файлы», затем прочитайте сохранённый блок метаданных.`,
      strings: "Найдено: 214 печатных строк\nНайдено: 96 символов U+200B/U+200C\nНеобычный блок передан в лабораторию.",
    };
    state.computer.toolOutput = outputs[tool] || "Операция не поддерживается.";
    saveCase();
    renderComputer();
  }

  function renderChecklist() {
    let items = [];
    try {
      items = JSON.parse(localStorage.getItem(STORAGE.checklist)) || [];
    } catch {
      items = [];
    }
    dom.checklist.innerHTML = items.length
      ? items.map((item) => `<li class="${item.done ? "is-done" : ""}" data-check-id="${item.id}"><input type="checkbox" ${item.done ? "checked" : ""} aria-label="Выполнено"><span>${escapeHtml(item.text)}</span><button type="button" class="checklist-delete" aria-label="Удалить">×</button></li>`).join("")
      : "<li><span></span><span>Список пуст. Добавьте то, что нельзя забыть.</span></li>";
  }

  function getChecklistItems() {
    try {
      return JSON.parse(localStorage.getItem(STORAGE.checklist)) || [];
    } catch {
      return [];
    }
  }

  function setChecklistItems(items) {
    localStorage.setItem(STORAGE.checklist, JSON.stringify(items));
    renderChecklist();
  }

  function saveNotesSoon() {
    dom.noteSaveState.textContent = "сохранение…";
    window.clearTimeout(noteSaveTimer);
    noteSaveTimer = window.setTimeout(() => {
      localStorage.setItem(STORAGE.notes, dom.notes.value);
      localStorage.setItem(STORAGE.quotes, dom.quotes.value);
      dom.noteSaveState.textContent = "сохранено";
    }, 280);
  }

  function loadSettings() {
    try {
      const saved = JSON.parse(localStorage.getItem(STORAGE.settings) || "{}");
      const theme = Object.hasOwn(THEME_AUDIO_PROFILES, saved.theme) ? saved.theme : DEFAULT_SETTINGS.theme;
      const holdDuration = [500, 1000, 1500, 2000].includes(Number(saved.holdDuration)) ? Number(saved.holdDuration) : DEFAULT_SETTINGS.holdDuration;
      const effectsVolume = Number.isFinite(Number(saved.effectsVolume)) ? Math.max(0, Math.min(100, Number(saved.effectsVolume))) : DEFAULT_SETTINGS.effectsVolume;
      return { theme, clickSounds: saved.clickSounds !== false, effectsVolume, holdDuration };
    } catch {
      return { ...DEFAULT_SETTINGS };
    }
  }

  function syncSettingsForm() {
    if (!dom.settingsForm) return;
    dom.settingsForm.querySelector("#settings-theme").value = appSettings.theme;
    dom.settingsForm.querySelector("#settings-click-sounds").checked = appSettings.clickSounds;
    dom.settingsForm.querySelector("#settings-effects-volume").value = String(appSettings.effectsVolume);
    dom.settingsForm.querySelector("#settings-effects-volume-value").textContent = `${appSettings.effectsVolume}%`;
    dom.settingsForm.querySelector("#settings-hold-duration").value = String(appSettings.holdDuration);
  }

  function applyEffectsVolume() {
    if (!audioSystem) return;
    const effectsLevel = (appSettings.effectsVolume / 100) ** 1.4 * 0.58;
    if (audioSystem.context?.state === "closed") return;
    const now = audioSystem.context.currentTime;
    audioSystem.effectsMaster.gain.setTargetAtTime(effectsLevel, now, 0.025);
  }

  function applySettings() {
    const lightTheme = appSettings.theme === "high-contrast-white";
    document.documentElement.dataset.theme = appSettings.theme;
    document.documentElement.style.colorScheme = lightTheme ? "light" : "dark";
    document.documentElement.style.setProperty("--board-read-hold-duration", `${appSettings.holdDuration}ms`);
    document.querySelector('meta[name="theme-color"]')?.setAttribute("content", lightTheme ? "#ffffff" : appSettings.theme === "cyberpunk" ? "#100a24" : "#171512");
    syncSettingsForm();
    applyEffectsVolume();
  }

  function saveSettings(nextSettings) {
    appSettings = { ...appSettings, ...nextSettings };
    localStorage.setItem(STORAGE.settings, JSON.stringify(appSettings));
    applySettings();
  }

  function ensureAudioSystem() {
    if (audioSystem && audioSystem.context.state !== "closed") return audioSystem;
    const AudioContext = window.AudioContext || window.webkitAudioContext;
    if (!AudioContext) {
      toast("Аудио недоступно в этом браузере.", true);
      return null;
    }
    const context = new AudioContext();
    const effectsMaster = context.createGain();
    effectsMaster.gain.value = 0.32;
    effectsMaster.connect(context.destination);
    audioSystem = {
      context,
      effectsMaster,
      random: randomGenerator(`audio-${Date.now()}-${Math.random()}`),
      noiseBuffer: null,
    };
    applyEffectsVolume();
    return audioSystem;
  }

  function resumeAudio(audio) {
    if (audio.context.state === "suspended") void audio.context.resume();
  }

  function scheduleTone(context, destination, frequency, start, duration, volume, wave = "sine") {
    const oscillator = context.createOscillator();
    const gain = context.createGain();
    oscillator.type = wave;
    oscillator.frequency.setValueAtTime(frequency, start);
    gain.gain.setValueAtTime(0.0001, start);
    gain.gain.exponentialRampToValueAtTime(Math.max(0.0002, volume), start + Math.min(0.035, duration * 0.2));
    gain.gain.exponentialRampToValueAtTime(0.0001, start + duration);
    oscillator.connect(gain).connect(destination);
    oscillator.start(start);
    oscillator.stop(start + duration + 0.04);
  }

  function getNoiseBuffer(audio) {
    if (audio.noiseBuffer) return audio.noiseBuffer;
    const length = Math.floor(audio.context.sampleRate * 0.8);
    const buffer = audio.context.createBuffer(1, length, audio.context.sampleRate);
    const channel = buffer.getChannelData(0);
    for (let index = 0; index < length; index += 1) channel[index] = (audio.random() * 2 - 1) * 0.82;
    audio.noiseBuffer = buffer;
    return buffer;
  }

  function scheduleNoise(audio, destination, start, duration, volume, frequency) {
    const source = audio.context.createBufferSource();
    const filter = audio.context.createBiquadFilter();
    const gain = audio.context.createGain();
    source.buffer = getNoiseBuffer(audio);
    filter.type = "bandpass";
    filter.frequency.value = frequency;
    filter.Q.value = 0.7;
    gain.gain.setValueAtTime(0.0001, start);
    gain.gain.exponentialRampToValueAtTime(Math.max(0.0002, volume), start + 0.006);
    gain.gain.exponentialRampToValueAtTime(0.0001, start + duration);
    source.connect(filter).connect(gain).connect(destination);
    source.start(start);
    source.stop(start + duration + 0.02);
  }

  function playInterfaceSound(kind) {
    if (!appSettings.clickSounds) return;
    const audio = ensureAudioSystem();
    if (!audio) return;
    resumeAudio(audio);
    const profile = THEME_AUDIO_PROFILES[appSettings.theme];
    const now = audio.context.currentTime + 0.005;
    const style = kind === "computer" ? { first: profile.accent, second: profile.accent * 1.26, duration: 0.045 } : kind === "page" ? { first: profile.click * 0.72, second: profile.click * 0.91, duration: 0.06 } : { first: profile.click, second: profile.accent, duration: 0.04 };
    scheduleTone(audio.context, audio.effectsMaster, style.first, now, style.duration, 0.05, profile.wave);
    scheduleTone(audio.context, audio.effectsMaster, style.second, now + style.duration * 1.1, style.duration * 0.72, 0.024, profile.wave);
    if (kind === "page" || kind === "computer") scheduleNoise(audio, audio.effectsMaster, now, style.duration * 1.7, profile.noise, kind === "computer" ? 2800 : 1500);
  }

  function handleInterfaceSound(event) {
    const target = event.target instanceof Element ? event.target.closest("button, input, select, [role='tab']") : null;
    if (!target || target.disabled) return;
    const kind = target.closest(".computer-shell") ? "computer" : target.matches(".nav-item, .notebook-tab, .filter-button, select") ? "page" : "click";
    playInterfaceSound(kind);
  }

  function startNewCase(seed, difficultyKey) {
    caseData = generateCase(seed.toUpperCase(), difficultyKey);
    state = createInitialState(caseData);
    archiveQuery = "";
    archiveFilter = "all";
    saveCase();
    closeDialog(dom.newCaseDialog);
    renderCurrentView();
    toast(`Дело ${caseData.number} принято. Материалы загружены.`);
    setStatus(`Открыто дело ${caseData.number}`);
  }

  function openNewCaseDialog() {
    document.querySelector("#case-seed").value = randomCaseCode();
    showDialog(dom.newCaseDialog);
  }

  function handleClick(event) {
    const target = event.target.closest("button, [data-evidence-id]");
    if (!target) return;

    if (target.dataset.action === "tutorial-continue" || target.dataset.action === "tutorial-skip") {
      advanceTutorial();
      return;
    }
    if (target.dataset.action === "tutorial-hide") {
      state.tutorial.hidden = true;
      saveCase();
      renderTutorialUi();
      return;
    }
    if (target.dataset.action === "tutorial-resume") {
      state.tutorial.hidden = false;
      saveCase();
      renderTutorialUi();
      return;
    }

    if (target.dataset.view) navigate(target.dataset.view);
    if (target.dataset.action === "open-new-case") openNewCaseDialog();
    if (target.dataset.action === "close-new-case") closeDialog(dom.newCaseDialog);
    if (target.dataset.action === "close-detail") {
      closeDialog(dom.detailDialog);
      renderTutorialUi();
    }
    if (target.dataset.action === "close-help") closeDialog(dom.helpDialog);
    if (target.dataset.action === "close-settings") closeDialog(dom.settingsDialog);

    if (target.dataset.evidenceId) {
      openEvidence(target.dataset.evidenceId);
      handleTutorialEvent("evidence-open");
    }
    if (target.dataset.puzzleId) {
      const puzzle = caseData.puzzles.find((item) => item.id === target.dataset.puzzleId);
      if (puzzle) {
        renderPuzzleDetail(puzzle);
        if (puzzle.kind === "digital") handleTutorialEvent("digital-open");
      }
    }
    if (target.dataset.suspectId) {
      state.activeSuspect = target.dataset.suspectId;
      renderInterviews();
    }
    if (target.dataset.questionId) {
      askQuestion(target.dataset.questionId);
      handleTutorialEvent("interview-question");
    }
    if (target.dataset.archiveFilter) {
      archiveFilter = target.dataset.archiveFilter;
      renderArchive();
    }
    if (target.dataset.taskId) {
      const running = Object.values(state.tasks).some((task) => task.status === "running");
      const task = caseData.tasks.find((item) => item.id === target.dataset.taskId);
      if (task && !running) {
        state.tasks[task.id] = { status: "running", startedAt: Date.now(), finishAt: Date.now() + task.duration * 1000 };
        saveCase();
        renderAnalysis();
        toast(`Кира начала задачу. Результат будет через ${task.duration} секунд.`);
      }
    }

    if (target.dataset.computerApp) {
      state.computer.app = target.dataset.computerApp;
      state.computer.opened = target.dataset.computerApp === "mail" ? "mail-1" : null;
      state.computer.toolOutput = "";
      saveCase();
      renderComputer();
      if (target.dataset.computerApp === "tools") handleTutorialEvent("computer-tools");
    }
    if (target.dataset.mailId) {
      state.computer.opened = target.dataset.mailId;
      saveCase();
      renderComputer();
    }
    if (target.dataset.fileId) {
      state.computer.opened = target.dataset.fileId;
      state.computer.toolOutput = "";
      saveCase();
      renderComputer();
    }
    if (target.dataset.computerTool) {
      runComputerTool(target.dataset.computerTool);
      if (target.dataset.computerTool === "strings") handleTutorialEvent("computer-strings");
    }
    if (target.dataset.action === "fix-computer-find") fixComputerFind(target.dataset.find);
    if (target.dataset.action === "inspect-file-meta") {
      const file = caseData.computer.files.find((item) => item.id === target.dataset.file);
      state.computer.toolOutput = file?.metadata || "Метаданные не найдены.";
      saveCase();
      renderComputer();
    }

    if (target.dataset.action === "toggle-important") {
      const id = target.dataset.evidence;
      state.importantIds = state.importantIds.includes(id) ? state.importantIds.filter((item) => item !== id) : [...state.importantIds, id];
      saveCase();
      openEvidence(id);
      updateChrome();
    }
    if (target.dataset.action === "add-to-board") {
      const id = target.dataset.evidence;
      if (!state.boardIds.includes(id)) {
        state.boardIds.push(id);
        const index = state.boardIds.length - 1;
        state.boardPositions[id] = { x: 35 + (index % 4) * 190, y: 38 + Math.floor(index / 4) * 150 };
        saveCase();
        toast("Материал добавлен на доску.");
      }
      openEvidence(id);
      updateChrome();
      handleTutorialEvent("board-add");
    }
    if (target.dataset.action === "open-board-note") openBoardNoteDialog();
    if (target.dataset.action === "delete-board-note" && state.boardSelected.length === 1) {
      const id = state.boardSelected[0];
      if (isBoardNote(id)) {
        state.boardNotes = state.boardNotes.filter((item) => item.id !== id);
        state.boardIds = state.boardIds.filter((item) => item !== id);
        state.boardLinks = state.boardLinks.filter((savedLink) => {
          const link = boardLink(savedLink);
          return link.from !== id && link.to !== id;
        });
        delete state.boardPositions[id];
        state.boardSelected = [];
        saveCase();
        renderBoard();
        toast("Рабочая версия убрана с доски.");
      }
    }
    if (target.dataset.boardId && performance.now() >= suppressBoardClickUntil) {
      const id = target.dataset.boardId;
      if (state.boardSelected.includes(id)) state.boardSelected = state.boardSelected.filter((item) => item !== id);
      else if (state.boardSelected.length < 2) state.boardSelected.push(id);
      else state.boardSelected = [id];
      updateBoardSelectionUi();
    }
    if (target.dataset.action === "connect-board" && state.boardSelected.length === 2) {
      const [from, to] = state.boardSelected;
      const kind = document.querySelector("#board-link-kind")?.value || "связь";
      const note = String(document.querySelector("#board-link-note")?.value || "").trim();
      const index = state.boardLinks.findIndex((savedLink) => {
        const link = boardLink(savedLink);
        return (link.from === from && link.to === to) || (link.from === to && link.to === from);
      });
      if (index >= 0) state.boardLinks[index] = { from, to, kind, note };
      else state.boardLinks.push({ from, to, kind, note });
      state.boardSelected = [];
      saveCase();
      renderBoard();
      handleTutorialEvent("board-link");
    }
    if (target.dataset.action === "clear-board-links") {
      state.boardLinks = [];
      state.boardSelected = [];
      saveCase();
      renderBoard();
    }

    if (target.dataset.action === "show-hint") {
      if (!state.hintsUsed.includes(target.dataset.puzzle)) state.hintsUsed.push(target.dataset.puzzle);
      saveCase();
      const puzzle = caseData.puzzles.find((item) => item.id === target.dataset.puzzle);
      renderPuzzleDetail(puzzle);
    }
    if (target.dataset.timelineMove) {
      const index = state.timelineOrder.indexOf(target.dataset.eventId);
      const nextIndex = target.dataset.timelineMove === "up" ? index - 1 : index + 1;
      if (index >= 0 && nextIndex >= 0 && nextIndex < state.timelineOrder.length) {
        [state.timelineOrder[index], state.timelineOrder[nextIndex]] = [state.timelineOrder[nextIndex], state.timelineOrder[index]];
        saveCase();
        const puzzle = caseData.puzzles.find((item) => item.kind === "timeline");
        renderPuzzleDetail(puzzle);
      }
    }
    if (target.dataset.action === "check-timeline") solvePuzzle(target.dataset.puzzle, state.timelineOrder.join("|"));
    if (target.dataset.action === "run-dictionary") {
      const puzzle = caseData.puzzles.find((item) => item.id === target.dataset.puzzle);
      state.toolRuns[puzzle.id] = `dictionary.small … 5 кандидатов\nMATCH: ${puzzle.answer}\nВремя: 0.8 с · защита признана слабой`;
      saveCase();
      renderPuzzleDetail(puzzle);
    }
    if (target.dataset.action === "extract-zero") {
      const puzzle = caseData.puzzles.find((item) => item.id === target.dataset.puzzle);
      state.toolRuns[puzzle.id] = "U+200B/U+200C → binary → ASCII\nИзвлечено: QVJDSElWRQ==";
      saveCase();
      renderPuzzleDetail(puzzle);
      handleTutorialEvent("digital-decode");
    }
    if (target.dataset.action === "reopen-case") {
      state.report = null;
      saveCase();
      renderReport();
    }
  }

  function handleSubmit(event) {
    if (event.target === dom.newCaseForm) {
      event.preventDefault();
      const formData = new FormData(dom.newCaseForm);
      const seed = String(formData.get("seed") || "").trim() || randomCaseCode();
      const difficultyKey = String(formData.get("difficulty") || "detective");
      startNewCase(seed, difficultyKey);
      return;
    }

    if (event.target.matches("[data-puzzle-form]")) {
      event.preventDefault();
      const formData = new FormData(event.target);
      solvePuzzle(event.target.dataset.puzzleForm, String(formData.get("puzzle-answer") || ""));
      return;
    }

    if (event.target.id === "computer-search-form") {
      event.preventDefault();
      const query = String(new FormData(event.target).get("query") || document.querySelector("#computer-query")?.value || "").trim();
      state.computer.query = query;
      const normalizedQuery = normalize(query);
      state.computer.searchResults = normalizedQuery
        ? availableEvidence().filter((item) => normalize(`${item.code} ${item.title} ${item.excerpt} ${item.content} ${item.tags.join(" ")}`).includes(normalizedQuery)).slice(0, 14)
        : [];
      saveCase();
      renderComputer();
      return;
    }

    if (event.target.id === "board-note-form") {
      event.preventDefault();
      const formData = new FormData(event.target);
      const title = String(formData.get("title") || "").trim();
      const body = String(formData.get("body") || "").trim();
      if (!title || !body) return;
      const id = `note-${Date.now().toString(36)}`;
      state.boardNotes.push({ id, title, body });
      state.boardIds.push(id);
      const index = state.boardIds.length - 1;
      state.boardPositions[id] = { x: 35 + (index % 4) * 190, y: 38 + Math.floor(index / 4) * 150 };
      saveCase();
      closeDialog(dom.detailDialog);
      renderBoard();
      toast("Рабочая версия добавлена на доску.");
      return;
    }

    if (event.target.id === "report-form") {
      event.preventDefault();
      const formData = new FormData(event.target);
      const evidenceCount = formData.getAll("evidence").length;
      if (evidenceCount < 3 || evidenceCount > 5) {
        toast("Выберите от трёх до пяти материалов для доказательной цепочки.", true);
        return;
      }
      state.report = evaluateReport(formData);
      saveCase();
      renderReportResult();
      dom.workspace.scrollTop = 0;
      handleTutorialEvent("report-submit");
      return;
    }
  }

  function handleKeyboard(event) {
    const tag = event.target.tagName;
    const typing = ["INPUT", "TEXTAREA", "SELECT"].includes(tag) || event.target.isContentEditable;
    if (!typing && /^[1-8]$/.test(event.key) && caseData) {
      navigate(VIEW_ORDER[Number(event.key) - 1]);
    }
    if (!typing && event.key.toLowerCase() === "n") {
      document.querySelector(".notebook")?.classList.toggle("is-open");
      if (document.querySelector(".notebook")?.classList.contains("is-open")) dom.notes.focus();
    }
    if (!typing && event.key === "?") showDialog(dom.helpDialog);
    if (event.key === "Escape") document.querySelector(".notebook")?.classList.remove("is-open");
  }

  function tick() {
    if (!caseData || !state) return;
    dom.timer.textContent = formatDuration(Date.now() - state.startedAt);
    const completed = updateCompletedTasks(true);
    const activeTask = Object.values(state.tasks).some((task) => task.status === "running");
    dom.taskIndicator.hidden = !activeTask;
    if (state.view === "analysis" && !dom.detailDialog.open) {
      if (completed) renderAnalysis();
      else if (activeTask) updateAnalysisProgress();
    }
    const clock = document.querySelector("#computer-clock");
    if (clock) clock.textContent = new Date().toLocaleTimeString("ru-RU", { hour: "2-digit", minute: "2-digit" });
    if (completed) updateChrome();
  }

  function initNotebook() {
    dom.notes.value = localStorage.getItem(STORAGE.notes) || "";
    dom.quotes.value = localStorage.getItem(STORAGE.quotes) || "";
    dom.notes.addEventListener("input", saveNotesSoon);
    dom.quotes.addEventListener("input", saveNotesSoon);
    document.querySelectorAll("[data-note-tab]").forEach((button) => {
      button.addEventListener("click", () => {
        document.querySelectorAll("[data-note-tab]").forEach((tab) => {
          const active = tab === button;
          tab.classList.toggle("is-active", active);
          tab.setAttribute("aria-selected", String(active));
        });
        document.querySelectorAll(".notebook-page").forEach((page) => { page.hidden = true; });
        document.querySelector(`#${button.dataset.noteTab}-page`).hidden = false;
      });
    });
    dom.checklistForm.addEventListener("submit", (event) => {
      event.preventDefault();
      const input = document.querySelector("#checklist-input");
      const text = input.value.trim();
      if (!text) return;
      const items = getChecklistItems();
      items.push({ id: `${Date.now()}-${Math.random().toString(16).slice(2)}`, text, done: false });
      setChecklistItems(items);
      input.value = "";
    });
    dom.checklist.addEventListener("change", (event) => {
      const row = event.target.closest("[data-check-id]");
      if (!row) return;
      const items = getChecklistItems();
      const item = items.find((entry) => entry.id === row.dataset.checkId);
      if (item) item.done = event.target.checked;
      setChecklistItems(items);
    });
    dom.checklist.addEventListener("click", (event) => {
      const deleteButton = event.target.closest(".checklist-delete");
      if (!deleteButton) return;
      const row = deleteButton.closest("[data-check-id]");
      setChecklistItems(getChecklistItems().filter((item) => item.id !== row.dataset.checkId));
    });
    renderChecklist();
  }

  function init() {
    applySettings();
    initNotebook();
    document.addEventListener("click", handleClick);
    document.addEventListener("pointerdown", handleInterfaceSound, { passive: true });
    document.addEventListener("submit", handleSubmit);
    document.addEventListener("keydown", handleKeyboard);
    document.querySelector("#new-case-button").addEventListener("click", openNewCaseDialog);
    document.querySelector("#help-button").addEventListener("click", () => showDialog(dom.helpDialog));
    dom.settingsButton.addEventListener("click", () => showDialog(dom.settingsDialog));
    document.querySelector("#random-seed").addEventListener("click", () => {
      document.querySelector("#case-seed").value = randomCaseCode();
    });
    const saveSettingsFromForm = () => {
      const formData = new FormData(dom.settingsForm);
      saveSettings({
        theme: String(formData.get("theme") || DEFAULT_SETTINGS.theme),
        clickSounds: formData.get("clickSounds") === "on",
        effectsVolume: Number(formData.get("effectsVolume") || DEFAULT_SETTINGS.effectsVolume),
        holdDuration: Number(formData.get("holdDuration") || DEFAULT_SETTINGS.holdDuration),
      });
    };
    dom.settingsForm.addEventListener("input", (event) => {
      if (!event.target.matches("input[type='range']")) return;
      saveSettingsFromForm();
    });
    dom.settingsForm.addEventListener("change", saveSettingsFromForm);

    [dom.newCaseDialog, dom.detailDialog, dom.helpDialog, dom.settingsDialog].forEach((dialog) => {
      dialog.addEventListener("click", (event) => {
        if (event.target === dialog) closeDialog(dialog);
      });
    });

    if (loadCase()) {
      renderCurrentView();
      setStatus(`Восстановлено дело ${caseData.number}`);
    } else {
      document.querySelector("#case-seed").value = randomCaseCode();
      showDialog(dom.newCaseDialog);
    }
    ticker = window.setInterval(tick, 1000);
    tick();
  }

  window.addEventListener("beforeunload", () => {
    saveCase();
    window.clearInterval(ticker);
    if (audioSystem?.context && audioSystem.context.state !== "closed") void audioSystem.context.close();
  });

  init();
})();
